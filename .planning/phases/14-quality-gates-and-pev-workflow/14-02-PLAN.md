---
phase: 14-quality-gates-and-pev-workflow
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/framework/hooks/audit-log.js
  - packages/framework/hooks/synapse-startup.js
  - packages/framework/test/unit/hooks.test.ts
autonomous: true
requirements:
  - GATE-05
  - GATE-06

must_haves:
  truths:
    - "audit-log.js logs ALL tool calls (not just Synapse MCP tools) with timestamp, agent identity, tool name, and result summary"
    - "audit-log.js includes token cost estimation per tool call using Math.ceil(chars/4) pattern"
    - "audit-log.js appends structured JSON entries to .synapse-audit.log (one entry per line)"
    - "audit-log.js never blocks tool execution -- PostToolUse hooks are post-hoc, silent-fail on error"
    - "synapse-startup.js injects agent identity and tier authority context from trust.toml + agents.toml into SessionStart additionalContext"
    - "synapse-startup.js injects structured context: 'You are {agent}. Your tier authority: [{tiers}]. Tier 0 decisions require user collaboration.'"
    - "synapse-startup.js falls back to generic instructions if config files are unreadable (graceful degradation)"
    - "Both hooks wrapped in top-level try/catch -- hooks degrade gracefully under any input"
  artifacts:
    - path: "packages/framework/hooks/audit-log.js"
      provides: "PostToolUse hook logging all tool calls with token estimates"
      contains: "tokenEstimate"
    - path: "packages/framework/hooks/synapse-startup.js"
      provides: "SessionStart hook injecting tier identity + work stream detection instructions"
      contains: "tier_authority"
    - path: "packages/framework/test/unit/hooks.test.ts"
      provides: "Updated unit tests for expanded audit and startup hooks"
      min_lines: 200
  key_links:
    - from: "packages/framework/hooks/audit-log.js"
      to: ".synapse-audit.log"
      via: "appendFileSync writes JSON log entries with token estimates"
      pattern: "appendFileSync"
    - from: "packages/framework/hooks/synapse-startup.js"
      to: "packages/framework/config/trust.toml"
      via: "Reads tier_authority to inject agent identity context"
      pattern: "tier_authority"
    - from: "packages/framework/hooks/synapse-startup.js"
      to: "packages/framework/config/agents.toml"
      via: "Reads agent registry to validate agent identity"
      pattern: "agents"
---

<objective>
Expand the audit hook to log all tool calls with token estimates, and enhance the SessionStart hook to inject agent tier identity from trust.toml and agents.toml.

Purpose: GATE-05 requires expanded audit coverage beyond Synapse MCP tools. The audit hook now tracks every tool call with token cost estimates for observability. The SessionStart enhancement (from CONTEXT.md: "Agents learn their tier authority via SessionStart hook injection") ensures agents proactively know their boundaries -- the enforcement hooks (Plan 01) are the backstop, not the primary control.

Output: New audit-log.js (replaces synapse-audit.js functionality with expanded scope), updated synapse-startup.js with tier identity injection, and updated unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-quality-gates-and-pev-workflow/14-CONTEXT.md
@.planning/phases/14-quality-gates-and-pev-workflow/14-RESEARCH.md

@packages/framework/hooks/synapse-audit.js
@packages/framework/hooks/synapse-startup.js
@packages/framework/test/unit/hooks.test.ts
@packages/framework/config/trust.toml
@packages/framework/config/agents.toml
@packages/framework/src/skills.ts

<interfaces>
<!-- Existing audit hook pattern (from synapse-audit.js) -->
PostToolUse hook stdin:
```json
{
  "tool_name": "mcp__synapse__create_task",
  "tool_input": { "project_id": "test", "title": "Test task", "actor": "orchestrator" },
  "tool_response": {"task_id": "t-001"}
}
```

<!-- Token estimation pattern (from skills.ts) -->
```typescript
export function estimateTokens(text: string): number {
  return Math.ceil(text.length / 4);
}
```

<!-- SessionStart hook stdin -->
```json
{
  "cwd": "/home/user/project",
  "session_id": "abc-123"
}
```

<!-- SessionStart hook stdout -->
```json
{
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "... startup instructions ..."
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create expanded audit-log.js and update synapse-startup.js with tier identity</name>
  <files>
    packages/framework/hooks/audit-log.js
    packages/framework/hooks/synapse-startup.js
  </files>
  <behavior>
    audit-log.js:
    - Logs ALL tool calls (remove mcp__synapse__ filter from synapse-audit.js)
    - Each log entry has: ts (ISO string), tool (tool_name), agent (actor or assigned_agent or 'unknown'), project_id (if present), input_tokens (Math.ceil(JSON.stringify(tool_input).length/4)), output_tokens (Math.ceil(JSON.stringify(tool_response).length/4)), input_keys (Object.keys(tool_input))
    - Appends to .synapse-audit.log in process.cwd() (same as existing pattern)
    - Silent-fail on error (PostToolUse cannot block execution)

    synapse-startup.js:
    - Preserves existing work stream detection instructions
    - Adds tier identity injection: reads trust.toml and agents.toml
    - Injects structured context block: "## Agent Identity\nYou are {agent_name}. Your tier authority: [{tiers}]. Tier 0-1 decisions require escalation to the appropriate agent.\n\n## Permitted Synapse Tools\n{tool_list}"
    - If config files unreadable: falls back to existing generic instructions (no crash)
    - Still injects attribution requirement instructions
  </behavior>
  <action>
    **audit-log.js:**
    Create a new PostToolUse hook script based on synapse-audit.js but expanded:
    1. Remove the `if (!toolName.startsWith('mcp__synapse__'))` filter -- log ALL tool calls per GATE-05
    2. Add token estimation: `const tokenEstimate = (str) => Math.ceil((str || '').length / 4);`
    3. Add `input_tokens: tokenEstimate(JSON.stringify(toolInput))` and `output_tokens: tokenEstimate(JSON.stringify(data.tool_response || ''))` to log entry
    4. Keep the same agent identity detection: `toolInput.actor || toolInput.assigned_agent || 'unknown'`
    5. Keep silent-fail pattern (PostToolUse hook -- MUST NOT block)
    6. Keep same log file path pattern (.synapse-audit.log in cwd)

    Note: audit-log.js is a NEW file. The existing synapse-audit.js remains for backward compatibility but settings.template.json will point to audit-log.js in Plan 01's settings update (or in the Plan 02 verification step -- update settings.template.json PostToolUse to use audit-log.js instead of synapse-audit.js).

    **IMPORTANT:** Update settings.template.json PostToolUse section to:
    - Remove the `"matcher": "mcp__synapse__.*"` filter (audit all tools, not just Synapse)
    - Change command from `synapse-audit.js` to `audit-log.js`

    **synapse-startup.js:**
    Extend the existing SessionStart hook:
    1. After the existing try block, add a config-reading section wrapped in its own try/catch
    2. Read packages/framework/config/trust.toml using `readFileSync` + `parseToml` from smol-toml
    3. Read packages/framework/config/agents.toml similarly
    4. For each agent in agents.toml, build an identity block:
       ```
       ## Agent Tier Authority (from trust.toml)

       When you are spawned as an agent, your boundaries are:
       - product-strategist: Tier authority [0, 1] -- can store Tier 0-1 decisions
       - architect: Tier authority [1, 2]
       ...

       Tier 0 (Product Strategy) decisions ALWAYS require user collaboration.
       If a decision is above your tier, escalate to the appropriate agent.
       ```
    5. Append this identity block to the existing additionalContext
    6. If config read fails (file not found, parse error): log warning to stderr, continue with existing generic instructions (graceful degradation per GATE-06)
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && echo '{"tool_name":"Read","tool_input":{"file_path":"/test.ts"}}' | node packages/framework/hooks/audit-log.js && test -f .synapse-audit.log && rm .synapse-audit.log && echo "PASS"</automated>
  </verify>
  <done>audit-log.js logs all tool calls (not just Synapse MCP), synapse-startup.js injects tier identity from config files with graceful fallback</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update hooks.test.ts with expanded audit and startup tier identity tests</name>
  <files>
    packages/framework/test/unit/hooks.test.ts
    packages/framework/settings.template.json
  </files>
  <behavior>
    Expanded audit-log.js tests:
    - Logs Synapse tool call with token estimates (input_tokens and output_tokens fields present and > 0)
    - Logs non-Synapse tool call (e.g., "Read") -- previously ignored, now logged
    - Agent identity still captured from actor field
    - Agent identity still captured from assigned_agent field
    - Agent defaults to 'unknown' when no identity provided
    - Exits 0 on malformed input (silent-fail)

    Updated startup tests:
    - additionalContext includes tier authority information when config files are accessible
    - additionalContext includes "Tier 0" warning language
    - Still includes work stream detection instructions (get_task_tree)
    - Still includes attribution instructions
    - Exits 0 on malformed input (graceful degradation)

    settings.template.json:
    - PostToolUse section has no matcher (logs all tools) and uses audit-log.js
  </behavior>
  <action>
    **Update test/unit/hooks.test.ts:**

    1. Replace AUDIT_HOOK constant path from `synapse-audit.js` to `audit-log.js`
    2. Update existing audit hook tests:
       - "logs Synapse tool call" test: add assertions for input_tokens and output_tokens fields
       - "ignores non-Synapse tool calls" test: REVERSE assertion -- now it SHOULD log non-Synapse calls (Read, Write, etc.)
       - Keep existing agent identity tests (actor, assigned_agent, unknown fallback)
       - Keep malformed input test

    3. Add new audit tests:
       - test: 'logs non-Synapse tool call (e.g., Read) with token estimates' -- confirm file is created and entry has expected fields
       - test: 'input_tokens and output_tokens are positive numbers' -- verify Math.ceil(chars/4) produces reasonable values

    4. Update startup hook tests:
       - Add test: 'additionalContext includes tier authority information' -- run hook from monorepo root cwd (so config files resolve), check stdout JSON for tier_authority keywords
       - Add test: 'additionalContext includes Tier 0 warning' -- check for "Tier 0" in additionalContext
       - Keep existing tests (valid JSON output, attribution instructions, malformed input, empty input)

    **NOTE on cwd for startup tests:** synapse-startup.js now reads config files. Tests must set `cwd` to the monorepo root (`join(import.meta.dir, '../../../..')`) so paths resolve. If config files are missing in test env, the hook gracefully degrades -- test both cases.

    **Update settings.template.json:**
    Change PostToolUse section to remove matcher and use audit-log.js:
    ```json
    "PostToolUse": [
      {
        "hooks": [
          { "type": "command", "command": "node packages/framework/hooks/audit-log.js" }
        ]
      }
    ]
    ```
    (No matcher = fires for all tool calls, per GATE-05.)
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && bun test --cwd packages/framework test/unit/hooks.test.ts</automated>
  </verify>
  <done>All hooks.test.ts tests pass, expanded audit covers all tools with token estimates, startup injects tier identity, settings.template.json updated for audit-log.js</done>
</task>

</tasks>

<verification>
1. `bun test --cwd packages/framework test/unit/hooks.test.ts` -- all tests pass
2. `echo '{"tool_name":"Read","tool_input":{"file_path":"/test.ts"},"tool_response":"contents"}' | node packages/framework/hooks/audit-log.js` -- creates .synapse-audit.log entry (non-Synapse tool logged)
3. Verify .synapse-audit.log entry has input_tokens and output_tokens fields
4. `echo '{}' | node packages/framework/hooks/synapse-startup.js` -- outputs JSON with additionalContext containing "tier_authority" or "Tier" keywords
5. settings.template.json PostToolUse section has no matcher and uses audit-log.js
6. `bun run test:framework` -- all existing framework tests still pass
</verification>

<success_criteria>
- audit-log.js exists and logs ALL tool calls (Synapse and non-Synapse)
- Each audit log entry includes input_tokens and output_tokens using Math.ceil(chars/4)
- synapse-startup.js injects tier authority context from trust.toml
- synapse-startup.js degrades gracefully if config files are unreadable
- Both hooks are wrapped in try/catch (GATE-06)
- Updated tests prove expanded audit and identity injection behaviors
- settings.template.json PostToolUse section updated for audit-log.js (no matcher filter)
</success_criteria>

<output>
After completion, create `.planning/phases/14-quality-gates-and-pev-workflow/14-02-SUMMARY.md`
</output>
