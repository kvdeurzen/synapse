---
phase: 26.7
slug: pause-continue-upgrade-and-remove-synapse
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-13
---

# Phase 26.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Bun test (`bun:test`) |
| **Config file** | none — `bun test` auto-discovers `*.test.ts` |
| **Quick run command** | `bun test --cwd packages/framework packages/framework/test/unit/install.test.ts` |
| **Full suite command** | `bun run test` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `bun test --cwd packages/framework packages/framework/test/unit/install.test.ts`
- **After every plan wave:** Run `bun run test:framework`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 26.7-01-01 | 01 | 1 | install.sh synapse_version | Integration | `bun test --cwd packages/framework packages/framework/test/unit/install.test.ts` | ✅ (add assertion) | ⬜ pending |
| 26.7-01-02 | 01 | 1 | startup hook pause detection | Unit | `bun test --cwd packages/framework packages/framework/test/unit/hooks.test.ts` | ✅ (add test case) | ⬜ pending |
| 26.7-01-03 | 01 | 1 | 4 new command files exist | File existence | `bun test --cwd packages/framework packages/framework/test/unit/install.test.ts` | ✅ (add assertions) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `packages/framework/test/unit/install.test.ts` — add test for `synapse_version` field presence after install
- [ ] `packages/framework/test/unit/hooks.test.ts` — add test for pause snapshot detection in startup hook
- [ ] `packages/framework/test/unit/install.test.ts` — add assertions that 4 new command files are installed

*install.test.ts and hooks.test.ts already exist — need new test cases added, not new files.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Pause snapshot saves full state | Pause correctness | Requires active RPEV session | 1. Start refinement session 2. Run /synapse:pause 3. Verify snapshot file created |
| Continue resumes from snapshot | Continue correctness | Requires paused state | 1. Pause a session 2. Start new session 3. Run /synapse:continue 4. Verify state restored |
| Upgrade applies new version | Upgrade correctness | Requires GitHub tag access | 1. Run /synapse:upgrade 2. Verify changelog shown 3. Verify files updated |
| Remove cleans all Synapse files | Remove correctness | Destructive operation | 1. Run /synapse:remove 2. Verify all Synapse artifacts deleted 3. Verify CLAUDE.md cleaned |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
