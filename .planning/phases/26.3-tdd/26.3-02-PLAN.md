---
phase: 26.3-tdd
plan: 02
type: execute
wave: 2
depends_on: ["26.3-01"]
files_modified:
  - packages/framework/agents/task-auditor.md
  - packages/framework/agents/executor.md
  - packages/framework/agents/validator.md
  - packages/framework/agents/planner.md
autonomous: true
requirements: [TDD-02, TDD-03, TDD-04, TDD-05]

must_haves:
  truths:
    - "Task-auditor triangulates planner requirements, task-designer spec, AND test-designer tests before issuing verdict"
    - "Task-auditor independently runs tests to verify RED state"
    - "Executor MUST NOT modify or delete test-designer tests"
    - "Executor reports structured status: DONE, DONE_WITH_CONCERNS, BLOCKED, NEEDS_CONTEXT"
    - "Executor performs self-review before handoff (completeness, quality, discipline, supplementary tests)"
    - "Validator confirms test-designer tests pass and were not modified by executor"
    - "Planner frames test expectations as test-designer input, not validator guidance"
  artifacts:
    - path: "packages/framework/agents/task-auditor.md"
      provides: "Expanded task-auditor with test triangulation"
      contains: "test-designer"
    - path: "packages/framework/agents/executor.md"
      provides: "Executor with TDD constraints"
      contains: "MUST NOT modify"
    - path: "packages/framework/agents/validator.md"
      provides: "Simplified validator leveraging test-designer tests"
      contains: "test-designer"
    - path: "packages/framework/agents/planner.md"
      provides: "Planner with test expectations reframed"
      contains: "test-designer"
  key_links:
    - from: "packages/framework/agents/task-auditor.md"
      to: "packages/framework/agents/test-designer.md"
      via: "triangulation of test-contract documents"
      pattern: "test-designer.*test-contract"
    - from: "packages/framework/agents/executor.md"
      to: "packages/framework/agents/test-designer.md"
      via: "test immutability constraint"
      pattern: "MUST NOT modify"
    - from: "packages/framework/agents/validator.md"
      to: "packages/framework/agents/test-designer.md"
      via: "confirms immutable tests pass"
      pattern: "test-designer"
---

<objective>
Update task-auditor, executor, validator, and planner prompts to support the TDD pipeline.

Purpose: The test-designer (created in Plan 01) sits between task-designer and task-auditor. All downstream agents must be updated: task-auditor gains triangulation (spec + tests + requirements), executor gains test immutability constraints and structured status reporting, validator is simplified to leverage pre-existing tests, and planner reframes test expectations as test-designer input.

Output: Four updated agent prompts with TDD-aware behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.3-tdd/26.3-CONTEXT.md
@.planning/phases/26.3-tdd/26.3-RESEARCH.md
@.planning/phases/26.3-tdd/26.3-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — the test-designer contract that these agents reference. -->

test-designer Output Contract:
- Test files written to disk at paths from task-designer spec
- Test-contract summary: doc_id `test-designer-test-contract-{task_id}`, provides: test-contract
- Test file paths appended to task description
- Tags: `"|test-designer|test-contract|provides:test-contract|{task_id}|stage:PLANNING|"`

test-designer doc_id convention: `test-designer-test-contract-{task_id}`

Executor structured status values: DONE, DONE_WITH_CONCERNS, BLOCKED, NEEDS_CONTEXT

<!-- Current agent structures that will be modified (key sections only). -->

From task-auditor.md — current 6 verification dimensions:
a. Mock code present
b. File paths identified
c. Integration points
d. Acceptance criteria
e. Dependencies
f. Decision consistency

From executor.md — current Output Contract:
| Output | How | doc_id pattern | provides |
|--------|-----|----------------|----------|
| Git commit(s) | git commit | n/a | n/a |
| Implementation summary | store_document(category: "implementation_note") | `executor-implementation-{task_id}` | implementation |

From validator.md — current verification approach:
- Read task spec and compare to implementation
- Verify alignment with decisions
- Run tests if applicable
- Mark failures with clear findings

From planner.md — current test expectations section (Mandatory Validation Tasks > Per Leaf Task):
"For every leaf task (depth=3), include unit test expectations... What conditions must the tests verify..."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand task-auditor.md with test triangulation</name>
  <files>packages/framework/agents/task-auditor.md</files>
  <action>
Update `packages/framework/agents/task-auditor.md` with the following changes. Preserve ALL existing content; these are additions and modifications to specific sections.

**1. Update opening paragraph (line ~10):**
Add after the current first sentence: "After Phase 26.3, you also triangulate the test-designer's tests against the spec and planner requirements — three artifacts must align before execution starts."

**2. Update description in frontmatter:**
Change to: "Verifies task specs are execution-ready and triangulates spec + tests + requirements. Activates Tier 2-3 decision drafts. Spawned by orchestrator after Test Designer completes."

**3. Add Bash tool to frontmatter tools list:**
The task-auditor now needs to run tests independently. Add `Bash` to the tools line if not already present. Check: the current frontmatter has `tools: Read, Bash, Glob, Grep, ...` — Bash IS already present. Good, no change needed.

**4. Update Input Contract:**
Add a new row:
| test_contract_doc_id | query_documents(provides:test-contract) or task description | YES (after test-designer runs) |

Add a note: "If test-designer has run: query `test-designer-test-contract-{task_id}` to load the test contract. If test contract is missing when expected: HALT with 'Test Designer did not store test contract for task {task_id}'."

**5. Expand Step 2 (Per-Task Spec Verification) — add new dimension g:**

After dimension f (Decision consistency), add:

**g. Test-requirement coverage (TDD triangulation):** Do the test-designer's tests cover ALL planner requirements? Are there tests without matching requirements? Are there requirements without tests?

Steps:
1. Load the test contract: `query_documents(project_id: "{project_id}", doc_id: "test-designer-test-contract-{task_id}", actor: "task-auditor")`
2. Read the actual test files from disk using paths in the test contract
3. Extract @requirement comments from test files
4. Cross-reference with planner's acceptance criteria and test expectations in the task description
5. Check: every plan requirement has at least one @requirement-tagged test
6. Check: every @requirement-tagged test references a real plan requirement (no orphan tests)
7. Flag coverage gaps with specific missing requirements or orphan tests

**6. Add Step 2b: Independent Test Verification (after existing Step 2):**

"Step 2b: Verify RED State Independently"

The task-auditor MUST independently verify that tests fail for the right reasons. Do NOT rely solely on the test-designer's self-reported RED verification.

1. Read test file paths from the test contract document
2. Run each test file via Bash using the project's test runner command (from loaded skill content)
3. Verify all tests FAIL (non-zero exit code)
4. Verify failure reasons are correct: missing implementation (module not found, function not defined), NOT syntax errors or import failures in the test itself
5. If any test passes: flag as concern — implementation may already exist or test is wrong
6. If tests fail for wrong reasons (syntax, imports): REJECTED — route back to test-designer

**7. Update Step 4 (Verdict) — add dimension g to rejection criteria:**

Add: "If dimension g (test-requirement coverage) shows gaps (requirements without tests or orphan tests) → **REJECTED** for that task. Route: back to test-designer if tests are missing; back to planner if requirement is too vague to test."

**8. Update the Verdict Format table:**

Add a `Test Coverage` column after `Decision`:

```
| Task | Mock Code | File Paths | Integration | Criteria | Deps | Decision | Test Coverage | Verdict |
```

**9. Add rejection routing guidance after Blocking Issues section:**

"### Rejection Routing

When issuing REJECTED, specify the target agent for each issue:
- Missing/wrong tests → back to **test-designer**
- Spec inadequate for test coverage → back to **task-designer**
- Requirement too vague to test → back to **planner** (planner owns the contract)
- Decision conflict → back to **task-designer**
- File/dependency issues → back to **task-designer**"

**10. Update Example 1 to show test triangulation:**

Add after the existing example or modify Example 1 to include a test-coverage dimension check showing PASS (all @requirements mapped to tests, all tests mapped to requirements).
  </action>
  <verify>
    <automated>grep -c "test-designer" packages/framework/agents/task-auditor.md && grep -c "triangulat" packages/framework/agents/task-auditor.md && grep "Test Coverage" packages/framework/agents/task-auditor.md</automated>
  </verify>
  <done>task-auditor.md has: (1) updated description mentioning test triangulation, (2) test_contract_doc_id in Input Contract, (3) dimension g for test-requirement coverage, (4) Step 2b for independent RED verification, (5) updated verdict including dimension g, (6) Test Coverage column in verdict table, (7) rejection routing guidance specifying target agent per issue type.</done>
</task>

<task type="auto">
  <name>Task 2: Constrain executor.md with TDD rules and update validator.md and planner.md</name>
  <files>packages/framework/agents/executor.md, packages/framework/agents/validator.md, packages/framework/agents/planner.md</files>
  <action>
**A. executor.md — Add TDD constraints, structured status, self-review:**

Preserve ALL existing content. Add the following new sections and modifications:

**1. Add to opening paragraph (line ~10):**
After "Implement exactly what the task specifies — no scope creep." add: "When test-designer tests exist, your primary objective is to make those tests pass. You MUST NOT modify or delete test-designer's tests."

**2. Add new section after Output Contract: "## TDD Constraints"**

```markdown
## TDD Constraints

### Test Immutability (HARD RULE)

You MUST NOT modify or delete test files written by the test-designer. These are the immutable contract.

- Test files from the test-designer are identified by paths listed in the task description (appended by test-designer) or in the test-contract document
- You CAN add supplementary unit tests in separate test files or in additional describe blocks for internal logic the behavioral tests cannot see
- Supplementary tests follow TDD discipline: write a failing test, verify RED (run it and confirm failure), implement, verify GREEN
- If a test-designer test seems wrong: report BLOCKED (see Status Reporting below) — do NOT fix the test yourself

### Implementation Anti-Patterns (MUST NOT)

1. **Modifying test-designer tests** — This breaks the TDD contract. The test-author and implementer MUST be structurally separate.
2. **Testing mock behavior** — Assert on real behavior, not that a mock was called with certain arguments.
3. **Test-only methods in production** — Don't add methods to production code solely for test access.
4. **Tests without RED** — Every supplementary test must fail before the implementation that makes it pass. If you wrote the code first, delete it and verify the test fails, then re-implement.
```

**3. Add new section after TDD Constraints: "## Status Reporting"**

```markdown
## Status Reporting

Your output document (executor-implementation-{task_id}) MUST include a `## Status` section with one of these values:

| Status | Meaning | When to use |
|--------|---------|-------------|
| DONE | All test-designer tests pass, implementation matches spec | Normal successful completion |
| DONE_WITH_CONCERNS | Tests pass, but you identified a potential issue | Implementation works but has a concern (e.g., discovered unplanned complexity, potential performance issue, spec ambiguity) |
| BLOCKED | Cannot make one or more tests pass | You believe the test encodes a wrong assumption OR you cannot find an implementation approach |
| NEEDS_CONTEXT | Missing information to proceed | The spec or test expectations reference something you cannot find in the codebase |

When reporting BLOCKED, include:
- Which test(s) are failing
- What was attempted
- Why you believe the test encodes a wrong assumption (vs. an implementation gap)
- Suggestion: implementation gap, wrong test assumption, or ambiguous

When reporting DONE_WITH_CONCERNS, include:
- What the concern is
- Why it doesn't block completion
- Recommendation for follow-up
```

**4. Add new section after Status Reporting: "## Self-Review Protocol"**

```markdown
## Self-Review Protocol

Before reporting completion, review your own work across four dimensions:

1. **Completeness:** All test-designer tests pass. No tests skipped or ignored.
2. **Quality:** No hacks or workarounds that technically pass tests but violate the spec's intent. Code is clean and follows project conventions.
3. **Discipline:** No over-engineering beyond what tests require. Implementation is minimal and sufficient.
4. **Supplementary tests:** Any executor-added tests follow TDD (saw RED before GREEN), cover real internal logic, and don't duplicate what test-designer already covers.

If self-review reveals issues: fix them before reporting. If issues are unfixable within scope: report DONE_WITH_CONCERNS.
```

**5. Update "Store Implementation Summary" key tool sequence:**

In the store_document content template, after `## Warnings`, add:
```
## Status\n{DONE|DONE_WITH_CONCERNS|BLOCKED|NEEDS_CONTEXT}\n\n## Self-Review\nCompleteness: {pass/fail}\nQuality: {pass/fail}\nDiscipline: {pass/fail}\nSupplementary tests: {count added, or 'none'}\n\n## Test Results\nTest-designer tests: {N passed}/{M total}\nSupplementary tests: {N passed}/{M total}\n{test runner output summary}
```

---

**B. validator.md — Simplify to leverage test-designer tests:**

Preserve existing content. Add/modify the following:

**1. Update opening paragraph (line ~10):**
After "You can mark tasks as failed if the implementation doesn't match the spec." add: "When test-designer tests exist, your primary verification is confirming those immutable tests pass and were not modified by the executor."

**2. Add new section after Input Contract: "## TDD Verification Protocol"**

```markdown
## TDD Verification Protocol

When the task has a test-contract document (test-designer ran):

### Step A: Verify Test Immutability
1. Load test-contract document: `query_documents(doc_id: "test-designer-test-contract-{task_id}", actor: "validator")`
2. Read test files from disk at the paths listed in the test contract
3. Verify test files were NOT modified by the executor: compare @requirement comments and test function names against the test-contract summary. If tests were deleted, renamed, or had assertions changed: FAIL with "Executor modified immutable test-designer tests"

### Step B: Run Test-Designer Tests
1. Run the test-designer's test files via Bash using the project's test runner
2. ALL tests must PASS (zero exit code)
3. If any test fails: FAIL with specific failure output

### Step C: Spec Compliance Check
1. Read the task spec (from task.spec field)
2. Independently verify implementation matches spec — do NOT take executor's self-report at face value
3. Check: file paths match spec, exports match spec, key behaviors match spec
4. This catches cases where tests pass but implementation diverges from spec intent

### Step D: Review Executor Status
1. Read executor's output document (from task.output_doc_ids)
2. If status is DONE_WITH_CONCERNS: review the concern alongside the implementation. If concern reveals a plan gap, flag in your findings for orchestrator escalation (does not block current task completion unless concern is critical).
```

**3. Update Step 2 (Verify Against Spec) to reference TDD protocol:**

Add at the top of Step 2: "If a test-contract exists for this task, follow the TDD Verification Protocol above FIRST. Then proceed with the remaining spec verification steps below for any acceptance criteria not covered by tests."

---

**C. planner.md — Reframe test expectations as test-designer input:**

Preserve existing content. Modify the following:

**1. Update "Mandatory Validation Tasks > Per Leaf Task: Unit Test Expectations" section (around line ~286-291):**

Replace the existing text with:

```markdown
### Per Leaf Task: Test Expectations (Test-Designer Input)

For every leaf task (depth=3) you create, include a "test expectations" section in that task's description field. These serve as input to the Test Designer agent, which transforms them into executable failing tests:
- What conditions must the tests verify (specific behaviors, edge cases, error paths)
- What the expected inputs and outputs are for each test scenario
- Which boundary conditions or error cases must be tested
- What constitutes "passing" for the Validator agent

Frame these as requirements for the test-designer to implement, not as guidance for the validator. The test-designer will transform this prose into executable assertions with @requirement tracing.

Example:
```
Test expectations (for test-designer):
- signToken('access') returns a JWT decodable with jose, exp = now + 15min
- signToken('refresh') returns JWT with 7-day expiry
- signToken throws when PRIVATE_KEY_PEM env is missing
- Decoded JWT contains sub, email, iat, exp, type claims
- At least 5 tests covering valid sign, TTL values, missing key error
```
```

**2. Add a note in Step 2 (Components/Tasks section) around line ~114:**

After "The Task Designer will later add mock code, exact file paths, and integration points" add: "The Test Designer will then write executable failing tests from the spec and your test expectations."
  </action>
  <verify>
    <automated>grep "MUST NOT modify" packages/framework/agents/executor.md && grep "DONE_WITH_CONCERNS" packages/framework/agents/executor.md && grep "TDD Verification Protocol" packages/framework/agents/validator.md && grep "Test-Designer Input" packages/framework/agents/planner.md</automated>
  </verify>
  <done>executor.md has TDD Constraints section (test immutability hard rule, anti-patterns), Status Reporting section (DONE/DONE_WITH_CONCERNS/BLOCKED/NEEDS_CONTEXT), Self-Review Protocol, and updated implementation summary template. validator.md has TDD Verification Protocol (test immutability check, run tests, spec compliance, executor status review). planner.md has reframed test expectations section labeled as test-designer input with example.</done>
</task>

</tasks>

<verification>
1. task-auditor.md references test-designer and includes dimension g (test-requirement coverage) and Step 2b (independent RED verification)
2. executor.md contains "MUST NOT modify" test immutability constraint, 4-value Status Reporting table, Self-Review Protocol
3. validator.md contains "TDD Verification Protocol" with Steps A-D (immutability, run tests, spec compliance, executor status review)
4. planner.md contains "Test-Designer Input" heading in Mandatory Validation Tasks section
5. All four files maintain existing content — additions are additive, not destructive
</verification>

<success_criteria>
- Four agent prompts updated with TDD-aware behavior
- Task-auditor can triangulate spec + tests + requirements with clear dimension g criteria
- Executor has hard constraints preventing test modification with structured status reporting
- Validator has simplified TDD verification leveraging pre-existing tests
- Planner reframes test expectations as test-designer input
- No existing functionality removed from any agent prompt
</success_criteria>

<output>
After completion, create `.planning/phases/26.3-tdd/26.3-02-SUMMARY.md`
</output>
