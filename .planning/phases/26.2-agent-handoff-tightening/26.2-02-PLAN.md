---
phase: 26.2-agent-handoff-tightening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/framework/agents/_synapse-protocol.md
  - packages/framework/agents/synapse-orchestrator.md
autonomous: true
requirements:
  - HANDOFF-03
  - HANDOFF-04

must_haves:
  truths:
    - "_synapse-protocol.md contains a 5-step Mandatory Context Loading Sequence that every agent follows"
    - "_synapse-protocol.md defines the standard tag vocabulary (agent, type, provides, task, stage)"
    - "_synapse-protocol.md defines the 11-slug provides vocabulary"
    - "Orchestrator has a fixed routing table defining which upstream doc_ids to inject per agent dispatch"
    - "Orchestrator references structured task fields (context_doc_ids, spec, output_doc_ids) not CONTEXT_REFS/SPEC blocks"
  artifacts:
    - path: "packages/framework/agents/_synapse-protocol.md"
      provides: "Unified context loading + tag vocab + provides vocab"
      min_lines: 80
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "Routing table + structured field usage"
      contains: "context_doc_ids"
  key_links:
    - from: "packages/framework/agents/_synapse-protocol.md"
      to: "packages/framework/agents/synapse-orchestrator.md"
      via: "Protocol include reference"
      pattern: "_synapse-protocol"
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "task.context_doc_ids field"
      via: "Routing table doc_id injection"
      pattern: "context_doc_ids"
---

<objective>
Unify context loading in the shared protocol and add upstream doc_id routing to the orchestrator.

Purpose: Replaces 13 per-agent "Task Start Protocol" sections with one shared 5-step sequence. Adds standard tag/provides vocabularies so all agents use consistent document conventions. Gives the orchestrator an explicit routing table for threading upstream outputs to downstream agents via structured task fields.

Output: Updated _synapse-protocol.md and synapse-orchestrator.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.2-agent-handoff-tightening/26.2-CONTEXT.md
@.planning/phases/26.2-agent-handoff-tightening/26.2-RESEARCH.md
@.planning/tighten-agent-handoff-contracts.md

<interfaces>
<!-- Current state of files being modified -->

From packages/framework/agents/_synapse-protocol.md (43 lines):
```markdown
## Synapse MCP Protocol
### Attribution
### Principles
### Error Handling
### Level-Aware Behavior
### Decision Draft Convention
```
Target: ~100-110 lines after additions. Stay under 120.

From packages/framework/agents/synapse-orchestrator.md (394 lines):
- Has SYNAPSE HANDOFF block format section
- Has CONTEXT_REFS parsing instructions (to be replaced with structured field reads)
- Currently references "CONTEXT_REFS block in description" pattern
- Need to add routing table section and replace CONTEXT_REFS references
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unified context loading, tag vocab, and provides vocab to shared protocol</name>
  <files>packages/framework/agents/_synapse-protocol.md</files>
  <action>
    Add 3 new sections to `_synapse-protocol.md` after the existing "Decision Draft Convention" section. Keep the protocol terse -- use tables not prose. Target ~100-110 total lines (currently 43).

    **Section 1: Mandatory Context Loading Sequence** (add after Decision Draft Convention)
    ```markdown
    ### Mandatory Context Loading Sequence

    Every agent MUST complete these 5 steps before beginning agent-specific work:

    | Step | Action | Tool Call |
    |------|--------|-----------|
    | 1 | Parse SYNAPSE HANDOFF block | Extract: project_id, task_id, hierarchy_level, rpev_stage_doc_id, doc_ids, decision_ids |
    | 2 | Load task | `get_task_tree(project_id, root_task_id: task_id, max_depth: 0)` -- read `spec`, `context_doc_ids`, `context_decision_ids` |
    | 3 | Load documents | If task has `context_doc_ids`: parse JSON array, call `get_smart_context(mode: "detailed", doc_ids: [...])`. Else if handoff has doc_ids: use those. Else: `get_smart_context(mode: "overview", max_tokens: {level-appropriate})` |
    | 4 | Load decisions | If task has `context_decision_ids`: parse JSON array, call `query_decisions` for those IDs |
    | 5 | Begin work | Only now begin agent-specific work |

    Do NOT skip steps 1-4. Do NOT begin implementation/analysis before context is loaded.
    ```

    **Section 2: Standard Tag Vocabulary** (for store_document calls)
    ```markdown
    ### Standard Tag Vocabulary

    Every `store_document` call MUST include the full tag set:

    | Tag Type | Format | Example |
    |----------|--------|---------|
    | Agent | `|{actor-name}|` | `|architect|` |
    | Type | `|{document-type}|` | `|architecture|` |
    | Provides | `|provides:{capability}|` | `|provides:architecture|` |
    | Task | `|{task_id}|` | `|task-01J5K...|` |
    | Stage | `|stage:{RPEV-stage}|` | `|stage:PLANNING|` |

    Example: `tags: "|architect|architecture|provides:architecture|task-01J5K...|stage:PLANNING|"`
    ```

    **Section 3: Provides Vocabulary** (fixed slugs for output contracts)
    ```markdown
    ### Provides Vocabulary

    Fixed capability slugs for `provides:` tags and output contracts:

    | Slug | Producer | Description |
    |------|----------|-------------|
    | architecture | architect | Architecture document |
    | decision-draft | architect, planner, task-designer | Decision proposals |
    | plan | planner | Task tree decomposition |
    | task-spec | task-designer | Structured task specification |
    | implementation | executor | Code changes + commit |
    | validation-findings | validator | Pass/fail + findings |
    | debug-diagnosis | debugger | Root cause + fix |
    | research-findings | researcher, product-researcher | Research output |
    | audit-findings | architecture-auditor, plan-auditor, task-auditor | Review scores |
    | integration-report | integration-checker | Cross-component check |
    | code-analysis | codebase-analyst | Index update + findings |

    Use ONLY these slugs in `provides:` tags. Do not invent new ones.
    ```

    Per CONTEXT.md locked decision: "No DOCUMENT HEADER in content -- redundant with existing store_document fields (tags, category, phase)." Do NOT add a document header convention. The tag set IS the metadata.
  </action>
  <verify>
    <automated>grep -c "Mandatory Context Loading Sequence\|Standard Tag Vocabulary\|Provides Vocabulary" packages/framework/agents/_synapse-protocol.md | grep -q "3" && wc -l < packages/framework/agents/_synapse-protocol.md | awk '{if ($1 >= 80 && $1 <= 120) print "OK: " $1 " lines"; else print "WARN: " $1 " lines (target 80-120)"}'</automated>
  </verify>
  <done>
    _synapse-protocol.md has 3 new sections: 5-step context loading sequence, standard tag vocabulary with 5 tag types, provides vocabulary with 11 fixed slugs. Total file is 80-120 lines. No DOCUMENT HEADER convention (per CONTEXT.md decision).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add upstream doc_id routing table and structured field usage to orchestrator</name>
  <files>packages/framework/agents/synapse-orchestrator.md</files>
  <action>
    Modify `synapse-orchestrator.md` to add the routing table and replace CONTEXT_REFS references with structured field usage.

    **Add new section: "Upstream Context Routing"** (after the SYNAPSE HANDOFF block format section):
    ```markdown
    ## Upstream Context Routing

    When dispatching an agent, set `context_doc_ids` and `context_decision_ids` on the task (via `update_task`) using upstream outputs:

    | Agent Being Dispatched | context_doc_ids Source | context_decision_ids Source |
    |------------------------|----------------------|---------------------------|
    | Architecture Auditor | architect's doc_ids from completion report | -- |
    | Planner | architecture doc_id | activated decision_ids from Architecture Auditor |
    | Plan Auditor | plan doc_id + research doc_ids from Planner | -- |
    | Task Designer | plan doc_id | activated decision_ids |
    | Task Auditor | -- (reads spec field directly) | -- |
    | Executor | task's context_doc_ids (set by Planner/Task Designer) | task's context_decision_ids |
    | Validator | executor's output_doc_ids from task field | -- |
    | Integration Checker | children tasks' output_doc_ids | -- |
    | Debugger | validator-findings or integration-findings doc_id | -- |

    **Protocol:** After an agent completes, read its completion report for produced doc_ids. Before dispatching the next agent, call `update_task` to set `context_doc_ids` (JSON array string) on the target task.

    **Do NOT modify context fields already set by Planner/Task Designer.** Only add orchestrator-routed upstream doc_ids to tasks that don't already have them populated.
    ```

    **Replace CONTEXT_REFS references:** Search for all mentions of `CONTEXT_REFS` in synapse-orchestrator.md. Replace instructions about:
    - "embed CONTEXT_REFS block in task description" -> "set context_doc_ids field on task via update_task"
    - "parse CONTEXT_REFS from description" -> "read context_doc_ids field from task"
    - Any "---CONTEXT_REFS---" block formatting -> remove (structured fields replace this)

    **Replace SPEC references:** Search for mentions of writing spec to description:
    - "SPEC block in description" -> "spec field on task"
    - "parse ---SPEC--- from description" -> "read spec field from task"

    **Add structured field reads:** In the section about how the orchestrator reads completed tasks, note:
    - Read `output_doc_ids` from completed executor tasks to pass to validators
    - Read `spec` field to confirm task designer has populated it before dispatching executor
    - If `spec` is null when dispatching executor: HALT and report "Task Designer did not write spec for task {task_id}"

    Keep orchestrator under ~400 lines (currently 394). The routing table adds ~25 lines; removing CONTEXT_REFS/SPEC block formatting should save ~15-20 lines. Net change: ~5-10 lines increase.
  </action>
  <verify>
    <automated>grep -c "Upstream Context Routing" packages/framework/agents/synapse-orchestrator.md | grep -q "1" && grep -c "CONTEXT_REFS" packages/framework/agents/synapse-orchestrator.md | grep -q "0" && echo "OK: routing table present, CONTEXT_REFS removed"</automated>
  </verify>
  <done>
    Orchestrator has an "Upstream Context Routing" table mapping each agent to its upstream doc_id sources. All CONTEXT_REFS references replaced with structured field reads (context_doc_ids, context_decision_ids, spec, output_doc_ids). Orchestrator stays under ~410 lines.
  </done>
</task>

</tasks>

<verification>
- `grep -c "Mandatory Context Loading Sequence" packages/framework/agents/_synapse-protocol.md` returns 1
- `grep -c "Upstream Context Routing" packages/framework/agents/synapse-orchestrator.md` returns 1
- `grep -c "CONTEXT_REFS" packages/framework/agents/synapse-orchestrator.md` returns 0
- `bun run test:framework` -- all framework tests pass (no regressions from .md changes)
</verification>

<success_criteria>
- _synapse-protocol.md contains 5-step context loading, tag vocab (5 types), provides vocab (11 slugs)
- _synapse-protocol.md is 80-120 lines total
- Orchestrator has fixed routing table for all 9 dispatchable agents
- Zero references to CONTEXT_REFS or ---SPEC--- blocks remain in orchestrator
- Orchestrator reads context_doc_ids, spec, output_doc_ids from task fields
- Framework tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26.2-agent-handoff-tightening/26.2-02-SUMMARY.md`
</output>
