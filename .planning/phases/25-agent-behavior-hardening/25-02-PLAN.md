---
phase: 25-agent-behavior-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/synapse/status.md
  - packages/framework/commands/synapse/status.md
  - .claude/commands/synapse/refine.md
  - packages/framework/commands/synapse/refine.md
  - .claude/commands/synapse/init.md
  - packages/framework/commands/synapse/init.md
autonomous: true
requirements: [ABH-04]

must_haves:
  truths:
    - "/synapse:status uses filtered get_task_tree queries (root_task_id per epic, max_depth) instead of fetching the entire tree"
    - "/synapse:status produces consistent output using a fixed markdown template, not agent-discretion formatting"
    - "/synapse:refine trusts code index results and does NOT spawn Explore agent when get_smart_context returns code summaries"
    - "/synapse:refine persists refinement summary at phase boundary (before offering to proceed to Plan)"
    - "/synapse:refine surfaces UX dimension separately for decisions with developer experience impact"
    - "/synapse:init commits scaffolding files after creation"
  artifacts:
    - path: ".claude/commands/synapse/status.md"
      provides: "Structured output template + filtered queries for /synapse:status"
      contains: "root_task_id"
    - path: ".claude/commands/synapse/refine.md"
      provides: "Trust code index, persist at boundary, UX dimension"
      contains: "trust the code index"
    - path: ".claude/commands/synapse/init.md"
      provides: "Commit step for scaffolding"
      contains: "git add"
    - path: "packages/framework/commands/synapse/status.md"
      provides: "Mirror of .claude/ status.md"
      contains: "root_task_id"
    - path: "packages/framework/commands/synapse/refine.md"
      provides: "Mirror of .claude/ refine.md"
      contains: "trust the code index"
    - path: "packages/framework/commands/synapse/init.md"
      provides: "Mirror of .claude/ init.md"
      contains: "git add"
  key_links:
    - from: ".claude/commands/synapse/status.md"
      to: "packages/server/src/tools/get-task-tree.ts"
      via: "get_task_tree with root_task_id and max_depth parameters"
      pattern: "root_task_id.*max_depth"
    - from: ".claude/commands/synapse/status.md"
      to: "packages/framework/commands/synapse/status.md"
      via: "mirror -- identical content"
      pattern: "root_task_id"
---

<objective>
Fix slash command prompts: make /synapse:status use filtered queries and structured output, make /synapse:refine trust the code index and persist at boundaries, and add a commit step to /synapse:init.

Purpose: The Phase 24 E2E run showed status output is inconsistent and unscalable (#5, #33), refine wastes 58k tokens exploring files the index already knows (#6), refinement state is lost on partial sessions (#26), UX decisions are missed (#36), and init leaves scaffolding untracked (#17).

Output: Six updated command .md files (3 commands x 2 mirrors each).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-agent-behavior-hardening/25-CONTEXT.md
@.planning/phases/25-agent-behavior-hardening/25-RESEARCH.md

@.claude/commands/synapse/status.md
@.claude/commands/synapse/refine.md
@.claude/commands/synapse/init.md

<interfaces>
<!-- get_task_tree supports these parameters (from packages/server/src/tools/get-task-tree.ts): -->
<!-- project_id: string (required) -->
<!-- root_task_id: string (required -- subtree root, can be any depth) -->
<!-- max_depth: number (optional, 1-10, default 5 -- relative depth from root) -->
<!-- max_tasks: number (optional, 1-1000, default 200) -->
<!-- filters: { status?, assigned_agent?, is_blocked?, depth? } (optional -- unmatched nodes collapsed) -->
<!-- Returns: { tree: TaskTreeNode, truncated, truncated_count, total_tasks } -->
<!-- TaskTreeNode.rollup: { total_descendants, done_count, blocked_count, in_progress_count, children_all_done, completion_percentage } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite status.md with filtered queries and structured output template</name>
  <files>.claude/commands/synapse/status.md, packages/framework/commands/synapse/status.md</files>
  <action>
Read the current `.claude/commands/synapse/status.md`. Rewrite the Process section with the following changes. Then copy the identical content to `packages/framework/commands/synapse/status.md`.

**Key change: Replace the single unfiltered get_task_tree call with a two-step filtered approach.**

The get_task_tree tool REQUIRES `root_task_id` (it is not optional). The tool does NOT support a "list all epics" call. Therefore the status command needs a way to discover epic task_ids first.

**New Process (replace existing steps 1-5):**

Step 1: Get project overview (unchanged -- call project_overview).

Step 2: Discover epic IDs. Call `get_smart_context(project_id: "{project_id}", mode: "overview", max_tokens: 2000)` to get the project summary which includes top-level epic task IDs. Alternatively, call `query_documents(category: "plan", tags: "rpev-stage")` to find all RPEV stage documents, which include task_ids. Extract all unique epic-level task_ids.

Step 3: For each epic, get a SHALLOW tree. Call `get_task_tree(project_id: "{project_id}", root_task_id: "{epic_task_id}", max_depth: 2)` -- this returns the epic + features + work packages, but NOT leaf tasks. The rollup stats on each node already contain done_count, total_descendants, and completion_percentage. This is O(epics) calls, not O(1), but each call is small.

Step 4: Query RPEV stage documents (same as before -- query_documents with category "plan", tags "rpev-stage").

Step 5: Render using a FIXED template. The agent MUST follow this template exactly -- no reformatting, no alternative layouts:

```
## Synapse Dashboard

### Epics (by priority)

{For each epic, sorted by priority (ascending value = higher priority):}
**{epic.title}** [{RPEV_STAGE from stage doc, or inferred}] ({epic.rollup.completion_percentage}% complete){token_display}
{For each feature child:}
  - {feature.title} [{STATUS}]{token_display} -- {feature.rollup.done_count}/{feature.rollup.total_descendants} done
{End features}

{End epics}

### Needs Your Input

{If any stage docs with pending_approval=true:}
{N} items need your attention:
  1. **{title}** [{level}] {stage} -- {notes from stage doc}
     `/synapse:focus "{title}"` to review

{If any stage docs with failure notes:}
{N} items have issues:
  1. **{title}** [{level}] {stage} -- retries exhausted
     `/synapse:focus "{title}"` for diagnostic report

{If none:}
All clear -- no items need your input.

### Agent Pool

{Render pool section -- same as current, no changes needed.}

### Recent Decisions

{From get_smart_context -- same as current.}

### Suggested Actions

{1-3 contextual suggestions -- same as current.}
```

**Token aggregation:** Keep the existing token aggregation logic (extract from tags `|tokens_used=N|`), but now it operates on the shallow tree (max_depth: 2), which still includes tags for the nodes returned.

**CRITICAL: Both files must be identical.** After writing `.claude/commands/synapse/status.md`, copy the exact same content to `packages/framework/commands/synapse/status.md`.

**Additional fix:** Remove the `semantic_search` call for refinement state -- that's redundant with the query_documents call for rpev-stage documents. Remove `mcp__synapse__semantic_search` from the allowed-tools frontmatter.
  </action>
  <verify>
    <automated>diff .claude/commands/synapse/status.md packages/framework/commands/synapse/status.md && echo "MIRRORS MATCH" || echo "MIRRORS DIFFER"</automated>
    Also verify:
    - grep "root_task_id" .claude/commands/synapse/status.md (filtered queries present)
    - grep "max_depth: 2" .claude/commands/synapse/status.md (shallow tree)
    - grep "## Synapse Dashboard" .claude/commands/synapse/status.md (fixed template)
    - grep -v "semantic_search" .claude/commands/synapse/status.md (semantic_search removed from process, though allowed-tools may still list it for other uses)
  </verify>
  <done>
    status.md uses per-epic filtered get_task_tree calls with max_depth: 2. Output follows a fixed markdown template. Mirrors match. semantic_search removed from process flow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix refine.md (trust code index, persist at boundary, UX dimension) and init.md (commit step)</name>
  <files>.claude/commands/synapse/refine.md, packages/framework/commands/synapse/refine.md, .claude/commands/synapse/init.md, packages/framework/commands/synapse/init.md</files>
  <action>
**refine.md changes:**

Read `.claude/commands/synapse/refine.md`. Make three targeted additions:

**A. Trust code index (fix #6, #26):**

After step 4 "Load context and precedents", add a new rule:

```
**Code Index Trust Rule:** If `get_smart_context` returns code summaries (documents with category "code" or code_chunks data), treat these as SUFFICIENT context for the codebase. Do NOT spawn an Explore agent, do NOT use Read/Glob/Grep to browse source files that the code index already covers. You may read specific files only if:
- The user explicitly asks to see a file
- A decision requires examining exact implementation details not in the summary
- The code index returned no results for a relevant query

This rule saves significant tokens. The Phase 24 E2E run showed an Explore agent consuming 58k tokens reading files the index had already summarized.
```

**B. Persist at phase boundary (fix #26):**

In step 7 "Check readiness", BEFORE the "If ready" branch, add:

```
**Persist before transition:** Before presenting the readiness summary or offering to proceed to Plan, call store_document to save the current refinement state (step 8). This ensures that if the user says "wait" or the session ends, the refinement output is already persisted. Do NOT defer persistence until after the user responds.
```

Also modify step 8 to note: "This step should already have been called in step 7 (persist before transition). If it was, skip the duplicate call. If the session is ending without reaching step 7, call store_document now as a safety net."

**C. Surface UX dimension (fix #36):**

In step 5 "Run brainstorming session", add a new bullet to the questioning patterns:

```
- **UX/DX dimension surfacing:** When a decision has both a technical dimension AND a developer/user experience dimension, surface them separately. Example: "SSH vs deploy" is a Tier 1 architectural decision, but "how does the developer configure the Pi address?" and "what happens when the Pi is unreachable?" are Tier 2 UX decisions that must also be captured. If you detect a decision with DX impact, ask: "This also affects developer experience -- should we capture [specific DX aspect] as a separate decision?"
```

**CRITICAL: Mirror rule.** After updating `.claude/commands/synapse/refine.md`, copy the identical content to `packages/framework/commands/synapse/refine.md`.

---

**init.md changes:**

Read `.claude/commands/synapse/init.md`. Add a new step 9.5 (between current step 9 "Detect skills" and step 10 "Summary"):

```
10. **Commit scaffolding:** After all files are created and configured, commit the Synapse scaffolding to version control:

    ```bash
    git add .synapse/ .claude/
    git commit -m "chore: initialize Synapse project configuration"
    ```

    If the git add or commit fails (e.g., not a git repo, or user has uncommitted changes that conflict), warn the user but do not treat it as a fatal error:
    > "Note: Could not auto-commit Synapse scaffolding. You may want to commit .synapse/ and .claude/ manually."
```

Renumber the old step 10 "Summary" to step 11.

**CRITICAL: Mirror rule.** Copy updated init.md to `packages/framework/commands/synapse/init.md`.
  </action>
  <verify>
    <automated>diff .claude/commands/synapse/refine.md packages/framework/commands/synapse/refine.md && diff .claude/commands/synapse/init.md packages/framework/commands/synapse/init.md && echo "ALL MIRRORS MATCH" || echo "MIRROR MISMATCH"</automated>
    Also verify:
    - grep "Code Index Trust Rule" .claude/commands/synapse/refine.md
    - grep "Persist before transition" .claude/commands/synapse/refine.md
    - grep "UX/DX dimension" .claude/commands/synapse/refine.md
    - grep "git add .synapse/" .claude/commands/synapse/init.md
  </verify>
  <done>
    refine.md has: Code Index Trust Rule (no Explore agent when index has data), persist-before-transition step, UX/DX dimension surfacing in brainstorming. init.md has: commit scaffolding step after file creation. All mirrors match between .claude/ and packages/framework/.
  </done>
</task>

</tasks>

<verification>
1. `diff .claude/commands/synapse/status.md packages/framework/commands/synapse/status.md` -- no differences
2. `diff .claude/commands/synapse/refine.md packages/framework/commands/synapse/refine.md` -- no differences
3. `diff .claude/commands/synapse/init.md packages/framework/commands/synapse/init.md` -- no differences
4. `grep "root_task_id" .claude/commands/synapse/status.md` -- filtered queries present
5. `grep "Code Index Trust Rule" .claude/commands/synapse/refine.md` -- trust code index present
6. `grep "git add .synapse/" .claude/commands/synapse/init.md` -- commit step present
</verification>

<success_criteria>
- /synapse:status fetches per-epic shallow trees (max_depth: 2) instead of one giant unfiltered tree
- /synapse:status output follows a fixed markdown template that agents cannot reformat
- /synapse:refine does not spawn Explore agents when get_smart_context returns code summaries
- /synapse:refine persists refinement state before offering to proceed to Plan (not after)
- /synapse:refine surfaces UX/DX dimension for decisions with developer experience impact
- /synapse:init commits .synapse/ and .claude/ after initialization
- All three command files have matching mirrors in packages/framework/
</success_criteria>

<output>
After completion, create `.planning/phases/25-agent-behavior-hardening/25-02-SUMMARY.md`
</output>
