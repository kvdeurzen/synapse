---
phase: 11-task-hierarchy-tooling
plan: 02
type: tdd
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/tools/update-task.ts
  - src/tools/get-task-tree.ts
  - src/server.ts
  - test/tools/update-task.test.ts
  - test/tools/get-task-tree.test.ts
autonomous: true
requirements:
  - TASK-03
  - TASK-04
  - TASK-05
  - TASK-06

must_haves:
  truths:
    - "An agent can update task status, assigned_agent, priority, is_blocked, is_cancelled, block_reason, and other fields via update_task"
    - "update_task only re-embeds when title or description changes -- status/priority/agent updates skip embedding entirely"
    - "update_task with new dependencies runs cycle detection before committing -- rejects cycles with CYCLE_DETECTED error"
    - "When a task's status changes to done or is_cancelled changes to true, all direct dependents have their is_blocked recomputed"
    - "Cancelling a task unblocks its dependents -- cancelled dependencies do not contribute to is_blocked"
    - "No automatic parent completion -- get_task_tree reports children_all_done: true as a signal but parent status is NOT auto-transitioned"
    - "is_blocked does NOT cascade upward -- a child being blocked does not make the parent blocked"
    - "An agent can retrieve a full task tree rooted at any task via get_task_tree with BFS traversal"
    - "get_task_tree returns nested tree structure with children arrays and rich rollup stats at each node: total_descendants, done_count, blocked_count, in_progress_count, children_all_done, completion_percentage"
    - "get_task_tree uses root_id denormalization for single-query subtree fetch, capped at depth 5 and 200 tasks with truncated indicator"
    - "get_task_tree supports optional filters: status, assigned_agent, is_blocked, depth"
  artifacts:
    - path: "src/tools/update-task.ts"
      provides: "updateTask core logic with selective re-embedding, dependency cycle detection, is_blocked propagation, and registerUpdateTaskTool MCP registration"
      exports: ["updateTask", "registerUpdateTaskTool"]
    - path: "src/tools/get-task-tree.ts"
      provides: "getTaskTree core logic with BFS tree assembly, rollup statistics, truncation, filtering, and registerGetTaskTreeTool MCP registration"
      exports: ["getTaskTree", "registerGetTaskTreeTool"]
    - path: "test/tools/update-task.test.ts"
      provides: "TDD tests for update_task including status transitions, re-embedding, dependency mutations, is_blocked propagation"
      min_lines: 120
    - path: "test/tools/get-task-tree.test.ts"
      provides: "TDD tests for get_task_tree including tree assembly, rollup stats, truncation, filtering"
      min_lines: 100
  key_links:
    - from: "src/tools/update-task.ts"
      to: "src/db/schema.ts"
      via: "imports TaskRowSchema for validation, queries tasks and relationships tables"
      pattern: "import.*TaskRowSchema"
    - from: "src/tools/update-task.ts"
      to: "src/tools/create-task.ts"
      via: "imports detectCycles for dependency mutation validation"
      pattern: "import.*detectCycles"
    - from: "src/tools/update-task.ts"
      to: "src/services/embedder.ts"
      via: "embed() call only when title or description changes"
      pattern: "embed\\("
    - from: "src/tools/update-task.ts"
      to: "src/services/activity-log.ts"
      via: "logActivity() call after mutation"
      pattern: "logActivity\\("
    - from: "src/tools/get-task-tree.ts"
      to: "src/db/schema.ts"
      via: "queries tasks table by root_id for bulk subtree fetch"
      pattern: "root_id"
    - from: "src/server.ts"
      to: "src/tools/update-task.ts"
      via: "registerUpdateTaskTool import and call"
      pattern: "registerUpdateTaskTool"
    - from: "src/server.ts"
      to: "src/tools/get-task-tree.ts"
      via: "registerGetTaskTreeTool import and call"
      pattern: "registerGetTaskTreeTool"
---

<objective>
Implement update_task and get_task_tree MCP tools with TDD, completing the task hierarchy toolset.

Purpose: update_task enables agents to progress tasks through the lifecycle (pending -> ready -> in_progress -> review -> done), manage dependencies with cycle-safe mutations, and propagate is_blocked changes to dependents. get_task_tree enables agents to retrieve the full recursive task hierarchy with rollup statistics for decision-making.

Output: Two working MCP tools: update_task (lifecycle management, dependency mutations, is_blocked propagation) and get_task_tree (BFS tree assembly with rollup stats, truncation, filtering). All three task tools registered in server.ts (24 total tools).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-task-hierarchy-tooling/11-CONTEXT.md
@.planning/phases/11-task-hierarchy-tooling/11-RESEARCH.md
@.planning/phases/11-task-hierarchy-tooling/11-01-SUMMARY.md

@src/db/schema.ts
@src/tools/create-task.ts
@src/tools/task-constants.ts
@src/tools/store-decision.ts
@src/server.ts
@src/services/embedder.ts
@src/services/activity-log.ts
@src/db/batch.ts
@src/types.ts

<interfaces>
<!-- Key types and contracts from Plan 01 and existing codebase. -->

From src/tools/task-constants.ts (created in Plan 01):
```typescript
export const VALID_TASK_STATUSES = ["pending", "ready", "in_progress", "review", "done"] as const;
export type ValidTaskStatus = (typeof VALID_TASK_STATUSES)[number];
export const VALID_TASK_PRIORITIES = ["critical", "high", "medium", "low"] as const;
export type ValidTaskPriority = (typeof VALID_TASK_PRIORITIES)[number];
export const VALID_DEPTHS = [0, 1, 2, 3] as const;
export type ValidDepth = (typeof VALID_DEPTHS)[number];
export const DEPTH_NAMES: Record<number, string> = { 0: "epic", 1: "feature", 2: "component", 3: "task" };
export const VALID_AGENT_ROLES = [
  "executor", "validator", "architect", "decomposer", "plan_reviewer",
  "integration_checker", "debugger", "codebase_analyst", "product_strategist", "researcher",
] as const;
export type ValidAgentRole = (typeof VALID_AGENT_ROLES)[number];
```

From src/db/schema.ts (extended in Plan 01):
```typescript
export const TASKS_SCHEMA = new Schema([...]); // 19 fields including Bool for is_blocked/is_cancelled
export const TaskRowSchema = z.object({...}); // Zod validation with boolean fields
```

From src/tools/create-task.ts (created in Plan 01):
```typescript
export interface CreateTaskResult {
  task_id: string;
  depth: number;
  depth_name: string;
  status: string;
  is_blocked: boolean;
  created_at: string;
  parent_id: string | null;
  root_id: string;
}
export async function createTask(dbPath: string, projectId: string, args: CreateTaskArgs, config: SynapseConfig): Promise<CreateTaskResult>;
export async function detectCycles(db: Connection, projectId: string, proposedEdges: Array<{ from: string; to: string }>): Promise<{ hasCycle: boolean }>;
export function registerCreateTaskTool(server: McpServer, config: SynapseConfig): void;
```

From src/db/schema.ts (RELATIONSHIPS_SCHEMA -- dependency storage):
```typescript
export const RelationshipRowSchema = z.object({
  relationship_id: z.string().min(1),
  project_id: z.string().min(1),
  from_id: z.string().min(1),
  to_id: z.string().min(1),
  type: z.string().min(1),
  source: z.string().min(1),
  created_at: z.string(),
  metadata: z.string().nullable(),
});
```

From src/services/embedder.ts:
```typescript
export async function embed(texts: string[], projectId: string, config: SynapseConfig): Promise<number[][]>;
export function _setFetchImpl(impl: typeof fetch): void;
```

From src/services/activity-log.ts:
```typescript
export async function logActivity(
  db: Connection, projectId: string, action: string,
  targetId: string | null, targetType: string | null,
  metadata?: Record<string, unknown>,
): Promise<void>;
```

From src/server.ts (current state after Plan 01 -- 22 tools registered):
```typescript
registerCreateTaskTool(server, config); toolCount++; // tool 22
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement update_task tool with selective re-embedding, dependency mutations, and is_blocked propagation via TDD</name>
  <files>src/tools/update-task.ts, test/tools/update-task.test.ts</files>
  <behavior>
    - updateTask with { status: "in_progress" } updates status without re-embedding, succeeds even when Ollama is down
    - updateTask with { title: "New title" } re-embeds "Title: {new_title}\n{existing_description}" and updates vector
    - updateTask with { description: "New desc" } re-embeds "Title: {existing_title}\n{new_desc}" and updates vector
    - updateTask with { title: "New", description: "Desc" } re-embeds both together
    - updateTask with { assigned_agent: "executor" } updates agent without re-embedding
    - updateTask with { priority: "high" } updates priority without re-embedding
    - updateTask with { is_cancelled: true } sets is_cancelled flag, then recomputes is_blocked for all tasks that depend on this one (since a cancelled dependency no longer blocks)
    - updateTask with { is_blocked: true, block_reason: "waiting on external input" } allows manual block override
    - updateTask with { status: "done" } sets status=done, then recomputes is_blocked for all direct dependents (tasks where this task appears in their dependencies)
    - updateTask with { dependencies: ["id1", "id2"] } replaces ALL dependencies: deletes old task_depends_on rows, inserts new ones, runs cycle detection on the FINAL state, recomputes is_blocked for this task
    - updateTask with dependencies that would create a cycle throws CYCLE_DETECTED error and makes no changes
    - updateTask on a non-existent task_id throws TASK_NOT_FOUND error
    - updateTask on a task in different project throws TASK_NOT_FOUND error
    - updateTask cannot change depth, parent_id, or root_id (structural fields are immutable after creation)
    - updateTask logs activity with action "update_task" and metadata listing changed fields
    - updateTask with { status: "done" } on a task where is_cancelled=true throws INVALID_TRANSITION error (cannot complete a cancelled task)
    - updateTask sets updated_at to current timestamp on every update
  </behavior>
  <action>
    1. Create `test/tools/update-task.test.ts`:
       - Setup: tmpDir, beforeEach with initProject + mock Ollama, afterEach cleanup (same pattern as create-task.test.ts from Plan 01)
       - Helper: create test tasks using createTask from Plan 01 (epic + feature + tasks with dependencies)
       - Test: "updates status without re-embedding" -- create task, mock embed to throw, call updateTask with { status: "in_progress" }, verify success (embed not called)
       - Test: "re-embeds when title changes" -- create task, call updateTask with { title: "New Title" }, query tasks table, verify vector is non-null and updated_at changed
       - Test: "re-embeds when description changes" -- same pattern with description
       - Test: "does not re-embed for priority update" -- create task, updateTask with { priority: "high" }, verify vector is same as before
       - Test: "updates assigned_agent" -- updateTask with { assigned_agent: "executor" }, verify field is set
       - Test: "recomputes dependents is_blocked when status becomes done" -- create A (pending), create B with deps=[A] (B.is_blocked=true), updateTask A with { status: "done" }, query B, verify B.is_blocked=false
       - Test: "cancelling task unblocks dependents" -- create A (pending), create B with deps=[A] (B.is_blocked=true), updateTask A with { is_cancelled: true }, query B, verify B.is_blocked=false
       - Test: "replaces dependencies with cycle detection" -- create A, B, C (no deps). updateTask C with { dependencies: [A.task_id] } (valid). Then try updateTask A with { dependencies: [C.task_id] } -- cycle A->C, C->A. Should throw CYCLE_DETECTED
       - Test: "rejects update on non-existent task" -- updateTask with task_id="nonexistent", expect TASK_NOT_FOUND
       - Test: "allows manual is_blocked with block_reason" -- updateTask with { is_blocked: true, block_reason: "external dependency" }, verify both fields set
       - Test: "logs activity on update" -- query activity_log after updateTask, verify "update_task" entry
       - Test: "cannot complete a cancelled task" -- create task, updateTask { is_cancelled: true }, then try updateTask { status: "done" }, expect INVALID_TRANSITION
       - Test: "sets updated_at on every update" -- create task, note created_at, updateTask with { priority: "high" }, verify updated_at > created_at

    2. Create `src/tools/update-task.ts`:
       - Imports: McpServer, z, connectDb, createToolLogger, logActivity, embed, SynapseConfig, ToolResult, task constants, detectCycles (from create-task.ts), insertBatch, RelationshipRowSchema, ulid
       - `UpdateTaskInputSchema` Zod object:
         - project_id: z.string().regex(/^[a-z0-9][a-z0-9_-]*$/)
         - task_id: z.string().min(1)
         - status: z.enum(VALID_TASK_STATUSES).optional()
         - title: z.string().min(1).optional()
         - description: z.string().optional()
         - priority: z.enum(VALID_TASK_PRIORITIES).optional().nullable() -- nullable to clear priority
         - assigned_agent: z.enum(VALID_AGENT_ROLES).optional().nullable() -- nullable to unassign
         - estimated_effort: z.string().optional().nullable()
         - is_blocked: z.boolean().optional()
         - is_cancelled: z.boolean().optional()
         - block_reason: z.string().optional().nullable()
         - tags: z.string().optional()
         - phase: z.string().optional().nullable()
         - dependencies: z.array(z.string()).optional() -- if provided, replaces ALL dependencies
       - `UpdateTaskResult` interface: { task_id, status, is_blocked, is_cancelled, updated_at, changed_fields: string[] }
       - `recomputeIsBlocked(db, projectId, taskId)` helper function (can be exported for reuse):
         a. Fetch all task_depends_on relationships where from_id = taskId (this task's dependencies)
         b. If no dependencies, check if task has a manual block_reason. If block_reason is set, leave is_blocked as-is. If no block_reason and no dependencies, set is_blocked = false
         c. If dependencies exist, fetch all dependency tasks. If ANY dependency has status !== "done" AND is_cancelled !== true, set is_blocked = true. Otherwise set is_blocked = false
         d. GUARD: If depIds array is empty, skip the IN-clause query
         e. Update the task's is_blocked and updated_at
       - `recomputeDependentsIsBlocked(db, projectId, taskId)` helper function:
         a. Fetch all task_depends_on relationships where to_id = taskId (tasks that depend on THIS task)
         b. For each dependent: call recomputeIsBlocked(db, projectId, dependent.from_id)
         c. This is DIRECT dependents only -- not transitive
       - `updateTask(dbPath, projectId, args, config)` core function:
         a. Validate input
         b. Connect to DB
         c. Fetch existing task row -- throw TASK_NOT_FOUND if not found
         d. Validate transition: if is_cancelled is already true on existing task AND status="done" is requested, throw INVALID_TRANSITION
         e. Build update values object from provided fields (only include fields that are present in args)
         f. Track changed_fields array for activity log
         g. **If dependencies provided:** (full replacement)
            - Delete all existing task_depends_on relationships for this task (from_id = task_id)
            - Validate all new dependency task_ids exist
            - Run detectCycles with proposed edges (from: task_id, to: each dep_id)
            - If cycle detected, throw CYCLE_DETECTED (no changes committed since we deleted first -- IMPORTANT: delete AFTER cycle check passes. Correct order: 1. build proposed final graph state, 2. run cycle detection, 3. if no cycle: delete old edges then insert new edges)
            - Actually CORRECT order to avoid LanceDB's lack of transactions:
              1. Fetch existing dependency edges for this task
              2. Build the "what if" graph: all existing edges for the project MINUS this task's current edges PLUS the proposed new edges
              3. Run cycle detection on the "what if" graph
              4. If cycle detected: throw error (nothing was changed)
              5. If no cycle: delete old edges, insert new edges
            - After inserting new edges: recompute is_blocked for this task
         h. **If title or description changed:** re-embed
            - Get the final title (args.title or existing task's title) and description (args.description or existing task's description)
            - embedText = "Title: " + finalTitle + "\n" + finalDescription
            - vectors = await embed([embedText], projectId, config) -- fail fast if Ollama down
            - Add vector to update values
         i. **Set updated_at** to new Date().toISOString()
         j. **Apply update** to tasks table: table.update({ where: `task_id = '${taskId}' AND project_id = '${projectId}'`, values: updateValues })
         k. **Propagate is_blocked to dependents** if status or is_cancelled changed:
            - If status changed to "done" OR is_cancelled changed to true: call recomputeDependentsIsBlocked(db, projectId, task_id)
            - This unblocks dependents whose only remaining blocker was this task
         l. **Log activity:** logActivity(db, projectId, "update_task", task_id, "task", { changed_fields })
         m. **Return UpdateTaskResult**
       - `registerUpdateTaskTool(server, config)` MCP registration:
         - Tool name: "update_task"
         - Description: "Update a task in the Synapse task hierarchy. Supports status lifecycle transitions, dependency replacement with cycle detection, selective re-embedding on title/description changes, and automatic is_blocked propagation to dependents."
         - inputSchema with all updateable fields, .describe() on each
         - Handler: parse, call core, return ToolResult

    IMPORTANT IMPLEMENTATION NOTES:
    - Selective re-embedding: ONLY call embed() when title or description is in the update payload. All other field updates (status, priority, assigned_agent, is_blocked, is_cancelled, block_reason, tags, phase, estimated_effort) MUST skip embedding entirely. This means the tool works even when Ollama is down for non-text updates.
    - Dependency replacement is FULL replacement: the new `dependencies` array replaces all existing task_depends_on edges. This is simpler than incremental add/remove and avoids complex partial update logic.
    - is_blocked propagation scope: When task X changes to done/cancelled, recompute is_blocked for ALL tasks that have X in their dependencies (direct dependents only). Each dependent's is_blocked is recomputed from ALL its dependencies, not just the one that changed.
    - LanceDB update() does not return row count: Pre-check that the task exists before updating.
    - Structural fields (depth, parent_id, root_id) are NOT in the UpdateTaskInputSchema -- they are immutable after creation.
  </action>
  <verify>
    <automated>bun test test/tools/update-task.test.ts && bun test test/tools/create-task.test.ts</automated>
  </verify>
  <done>update_task MCP tool handles all field updates with selective re-embedding. Dependency replacement runs cycle detection before committing. Status changes to done/is_cancelled=true propagate is_blocked recomputation to direct dependents. Cancelled dependencies unblock their dependents. Manual is_blocked override with block_reason is supported. Activity logging records all mutations. Invalid transitions are rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement get_task_tree tool with BFS assembly, rollup stats, truncation, filtering, and wire all task tools into server</name>
  <files>src/tools/get-task-tree.ts, test/tools/get-task-tree.test.ts, src/server.ts</files>
  <behavior>
    - getTaskTree with a valid root task_id returns a nested tree with children arrays
    - getTaskTree for an epic returns the full tree: epic -> features -> components -> tasks
    - getTaskTree for a feature returns a sub-tree rooted at that feature
    - Each node has rollup stats: total_descendants, done_count, blocked_count, in_progress_count, children_all_done, completion_percentage
    - children_all_done is true ONLY when ALL direct children have status "done"
    - completion_percentage = round(done_count / total_descendants * 100), 0 if no descendants
    - getTaskTree caps at max_depth 5 (relative to root) and max_tasks 200, returns truncated: true and truncated_count when caps are hit
    - getTaskTree with non-existent root_id throws TASK_NOT_FOUND
    - getTaskTree uses root_id denormalization: single WHERE clause `root_id = '{rootId}' OR task_id = '{rootId}'` fetches entire subtree
    - getTaskTree with status filter shows only nodes matching that status; unmatched nodes appear as collapsed summaries preserving hierarchy
    - getTaskTree with assigned_agent filter shows only nodes assigned to that agent; unmatched nodes collapsed
    - getTaskTree with is_blocked filter shows only blocked/unblocked nodes; unmatched collapsed
    - getTaskTree includes dependency_ids (list of task_ids this task depends on) on each node
    - getTaskTree for a root with no children returns the root node with empty children array and zero rollup stats
    - All three task tools (create_task, update_task, get_task_tree) are registered in server.ts (24 total)
  </behavior>
  <action>
    1. Create `test/tools/get-task-tree.test.ts`:
       - Setup: tmpDir, beforeEach with initProject + mock Ollama + create a test task hierarchy using createTask:
         - Epic "E1" (depth 0)
         - Feature "F1" under E1 (depth 1)
         - Feature "F2" under E1 (depth 1)
         - Component "C1" under F1 (depth 2)
         - Task "T1" under C1 (depth 3)
         - Task "T2" under C1 (depth 3)
         - Task "T3" under F2 (depth 2, component level)
       - Use updateTask (from this plan's Task 1) to set various statuses for rollup testing
       - Test: "returns full epic tree with correct structure" -- getTaskTree(E1.task_id), verify root has 2 children (F1, F2), F1 has child C1, C1 has children T1, T2
       - Test: "returns sub-tree rooted at feature" -- getTaskTree(F1.task_id), verify root=F1 with child C1
       - Test: "computes rollup stats correctly" -- set T1 to done, T2 to in_progress, verify C1 rollup: total_descendants=2, done_count=1, in_progress_count=1, blocked_count=0, children_all_done=false, completion_percentage=50
       - Test: "children_all_done is true when all direct children are done" -- set T1 and T2 both to done, verify C1.children_all_done=true
       - Test: "children_all_done is false with empty children" -- a leaf task with no children: children_all_done should be false (no children = vacuously false, since the signal is for validation eligibility and a leaf with no children has nothing to validate)
       - Test: "returns truncated flag when max_tasks exceeded" -- create >200 tasks (or pass max_tasks=5 to test with fewer), verify truncated=true and truncated_count > 0
       - Test: "returns truncated flag when max_depth exceeded" -- test with max_depth=2 on a depth-4 tree, verify truncation
       - Test: "throws TASK_NOT_FOUND for non-existent root" -- getTaskTree("nonexistent"), expect error
       - Test: "includes dependency_ids on each node" -- create T1 with deps=[T3.task_id], getTaskTree, verify T1 node has dependency_ids containing T3.task_id
       - Test: "filters by status" -- set T1 to done, getTaskTree with status filter "done", verify T1 is fully shown and other nodes are collapsed/summarized
       - Test: "filters by assigned_agent" -- assign T1 to "executor", getTaskTree with assigned_agent="executor", verify T1 shown and others collapsed
       - Test: "root with no children returns empty children array" -- create a lone epic, getTaskTree, verify children=[], total_descendants=0

    2. Create `src/tools/get-task-tree.ts`:
       - Imports: McpServer, z, connectDb, createToolLogger, SynapseConfig, ToolResult, task constants, DEPTH_NAMES
       - `GetTaskTreeInputSchema` Zod object:
         - project_id: z.string().regex(/^[a-z0-9][a-z0-9_-]*$/)
         - root_task_id: z.string().min(1) -- can be any task_id (epic, feature, etc.)
         - max_depth: z.number().int().min(1).max(10).optional().default(5) -- relative depth from root
         - max_tasks: z.number().int().min(1).max(1000).optional().default(200)
         - filters: z.object({
             status: z.enum(VALID_TASK_STATUSES).optional(),
             assigned_agent: z.enum(VALID_AGENT_ROLES).optional(),
             is_blocked: z.boolean().optional(),
             depth: z.number().int().min(0).max(3).optional(),
           }).optional()
       - `TaskTreeNode` interface:
         ```typescript
         interface TaskTreeNode {
           task_id: string;
           title: string;
           description: string;
           depth: number;
           depth_name: string;
           status: string;
           is_blocked: boolean;
           is_cancelled: boolean;
           block_reason: string | null;
           priority: string | null;
           assigned_agent: string | null;
           estimated_effort: string | null;
           tags: string;
           phase: string | null;
           parent_id: string | null;
           dependency_ids: string[];
           created_at: string;
           updated_at: string;
           children: TaskTreeNode[];
           rollup: RollupStats;
           collapsed?: boolean; // true if this node doesn't match filters but is shown for hierarchy context
         }
         ```
       - `RollupStats` interface:
         ```typescript
         interface RollupStats {
           total_descendants: number;
           done_count: number;
           blocked_count: number;
           in_progress_count: number;
           children_all_done: boolean;
           completion_percentage: number;
         }
         ```
       - `GetTaskTreeResult` interface: { tree: TaskTreeNode, truncated: boolean, truncated_count: number, total_tasks: number }

       - `getTaskTree(dbPath, projectId, args)` core function:
         a. Validate input
         b. Connect to DB

         c. **Fetch all tasks for the subtree in ONE query:**
            - Open tasks table
            - Query: `.where("(root_id = '{rootId}' OR task_id = '{rootId}') AND project_id = '{projectId}'").toArray()`
            - This fetches the root task AND all its descendants via root_id denormalization
            - If root task not found in results, throw TASK_NOT_FOUND

         d. **Fetch all dependency relationships for these tasks:**
            - Open relationships table
            - Get all task_ids from the fetched tasks
            - Query: `.where("project_id = '{projectId}' AND type = 'task_depends_on'").toArray()`
            - Build a dependency map: task_id -> [dependency_task_ids] (from from_id -> to_id)

         e. **Build parent->children map in JS:**
            - `const childrenMap = new Map<string | null, TaskRow[]>()` -- maps parent_id to children
            - `const taskMap = new Map<string, TaskRow>()` -- maps task_id to row
            - Iterate all rows, populate both maps

         f. **BFS tree assembly with depth and task caps:**
            - Start from root task_id
            - Use a queue: Array<{ taskId: string, depth: number }>
            - Track tasksIncluded count, truncated flag, truncatedCount
            - For each node in queue:
              - If tasksIncluded >= max_tasks: set truncated=true, increment truncatedCount for remaining, skip
              - If depth >= max_depth: set truncated=true, increment truncatedCount for subtree, skip
              - Create TaskTreeNode from row data
              - Add dependency_ids from dependency map
              - Enqueue children (from childrenMap)
              - Attach children to parent node

         g. **Apply filters (if provided):**
            - After building the full tree, mark nodes that don't match filters as collapsed=true
            - Collapsed nodes: keep in tree for hierarchy context but strip description to "[collapsed]" and children summaries
            - If a node matches a filter, all its ancestors up to root should be shown (not collapsed) for path context
            - Filter matching: status === filter.status, assigned_agent === filter.assigned_agent, is_blocked === filter.is_blocked, depth === filter.depth

         h. **Compute rollup stats bottom-up:**
            - Post-order traversal (after all children are assembled): for each node, compute rollup from its children recursively
            - total_descendants: count of ALL descendants (recursive, not just direct children)
            - done_count: count of descendants with status "done"
            - blocked_count: count of descendants with is_blocked=true
            - in_progress_count: count of descendants with status "in_progress"
            - children_all_done: true IFF this node has direct children AND every direct child has status "done". False if no children (leaf node) or any child is not done.
            - completion_percentage: total_descendants > 0 ? Math.round((done_count / total_descendants) * 100) : 0

         i. **Return GetTaskTreeResult:**
            - tree: the root TaskTreeNode
            - truncated: boolean
            - truncated_count: number of tasks excluded by caps
            - total_tasks: total rows fetched from DB (before caps)

       - `registerGetTaskTreeTool(server, config)` MCP registration:
         - Tool name: "get_task_tree"
         - Description: "Retrieve a task hierarchy tree rooted at any task. Returns nested structure with rollup statistics (total/done/blocked/in_progress counts, children_all_done signal, completion percentage). Supports BFS traversal with depth (5) and task count (200) caps. Optional filters for status, assigned_agent, is_blocked, and depth."
         - inputSchema with .describe() on each field
         - Handler: parse, call core, return ToolResult

    3. Update `src/server.ts`:
       - Add imports for registerUpdateTaskTool and registerGetTaskTreeTool
       - Add registration calls after registerCreateTaskTool:
         ```
         registerUpdateTaskTool(server, config); toolCount++;  // 23
         registerGetTaskTreeTool(server, config); toolCount++;  // 24
         ```
       - Total tools: 24

    IMPORTANT IMPLEMENTATION NOTES:
    - BFS vs DFS for tree assembly: Use BFS (queue) for level-order traversal which naturally respects the max_depth cap. But compute rollup stats AFTER assembly using a recursive post-order traversal (DFS). The two-pass approach: pass 1 = BFS assembly with caps, pass 2 = DFS rollup computation.
    - Root node is ALWAYS included regardless of filters. If root doesn't match filter, it still shows (it's the anchor).
    - children_all_done semantics: This is the signal for Phase 14's PEV loop. A leaf node (no children) returns children_all_done=false because there are no children to be "all done." A node where all direct children are status "done" returns true regardless of grandchildren status.
    - dependency_ids are fetched from the relationships table, not stored on the task row. Each node's dependency_ids = all to_ids from relationships where from_id = this task's task_id and type = "task_depends_on".
    - Collapsed nodes in filtered view: When filters are active, unmatched nodes are set to collapsed=true. They retain task_id, title, depth, status, and children (for hierarchy) but description is replaced with "[collapsed]". This preserves the tree structure while reducing response size.
    - vector field is NEVER included in get_task_tree output. Strip it from all nodes before returning.
    - Rollup stats for collapsed nodes are still computed normally -- they represent the real state of the subtree regardless of filter visibility.
  </action>
  <verify>
    <automated>bun test test/tools/get-task-tree.test.ts && bun test test/tools/update-task.test.ts && bun test test/tools/create-task.test.ts && bun test</automated>
  </verify>
  <done>get_task_tree returns nested tree structure with correct BFS traversal, rollup statistics, depth/task caps with truncation indicators, and filter support. update_task and get_task_tree are registered in server.ts alongside create_task. Full test suite passes with no regressions. All 24 MCP tools are registered. Phase 11 success criteria fully met: 3 task tools (create_task, update_task, get_task_tree) with task hierarchy, cascade signals, cycle detection, and BFS tree retrieval.</done>
</task>

</tasks>

<verification>
1. `bun test test/tools/update-task.test.ts` -- all update_task tests pass
2. `bun test test/tools/get-task-tree.test.ts` -- all get_task_tree tests pass
3. `bun test test/tools/create-task.test.ts` -- create_task tests still pass
4. `bun test` -- full suite passes (no regressions from any phase)
5. `grep -c "registerTool" src/server.ts` or verify toolCount reaches 24
6. `grep -n "registerUpdateTaskTool\|registerGetTaskTreeTool\|registerCreateTaskTool" src/server.ts` -- all three imported and called
7. Phase 11 success criteria check:
   - create_task: creates tasks at depth 0-3 with parent validation, dependencies, embedding (TASK-01, TASK-02, TASK-10)
   - update_task: updates status, assigned_agent, priority, dependencies with cycle detection (TASK-03, TASK-07)
   - get_task_tree: BFS tree with rollup stats, depth/task caps, filters (TASK-05, TASK-06)
   - Cascade: children_all_done signal, no auto-parent completion, is_blocked propagation to dependents (TASK-04)
   - init_project: creates tasks table with indexes (TASK-08)
   - Activity logging: all mutations logged (TASK-09)
   - Embedding: title+description embedded as 768-dim vector (TASK-10)
</verification>

<success_criteria>
- update_task selectively re-embeds only when title or description changes
- update_task dependency replacement runs cycle detection before committing
- update_task propagates is_blocked to direct dependents when status changes to done or is_cancelled changes to true
- Cancelled dependencies unblock dependents (is_cancelled tasks don't contribute to is_blocked)
- No automatic parent completion -- children_all_done is a read-time signal only
- is_blocked does NOT cascade upward to parents
- get_task_tree returns nested tree with children arrays from BFS traversal
- get_task_tree rollup stats are correct: total_descendants, done_count, blocked_count, in_progress_count, children_all_done, completion_percentage
- get_task_tree uses root_id denormalization for single-query subtree fetch
- get_task_tree respects depth (5) and task (200) caps with truncated indicator
- get_task_tree supports status, assigned_agent, is_blocked, depth filters with collapsed nodes for hierarchy preservation
- All three task tools registered in server.ts (24 total tools)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-task-hierarchy-tooling/11-02-SUMMARY.md`
</output>
