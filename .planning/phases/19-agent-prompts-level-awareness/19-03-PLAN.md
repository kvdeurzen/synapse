---
phase: 19-agent-prompts-level-awareness
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - packages/framework/agents/synapse-orchestrator.md
  - packages/framework/agents/decomposer.md
  - packages/framework/agents/executor.md
  - packages/framework/agents/validator.md
autonomous: true
requirements: [AGENT-04, AGENT-05, AGENT-06, AGENT-07, AGENT-10, AGENT-11]

must_haves:
  truths:
    - "Orchestrator Task calls include project_id, task_id, hierarchy_level, rpev_stage_doc_id, doc_ids, decision_ids in a structured SYNAPSE HANDOFF block"
    - "Validator never writes findings into task description -- uses store_document + link_documents + update_task(status only)"
    - "Integration Checker and Plan Reviewer store findings via store_document + link_documents (not in task description)"
    - "Executor stores implementation summaries as documents linked to the task"
    - "Decomposer embeds CONTEXT_REFS block in leaf task descriptions with document_ids and decision_ids"
    - "Executor and Validator parse SYNAPSE HANDOFF block and fetch context_refs doc_ids at task start"
  artifacts:
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "Structured SYNAPSE HANDOFF block format, context_refs parsing"
      contains: "SYNAPSE HANDOFF"
    - path: "packages/framework/agents/decomposer.md"
      provides: "CONTEXT_REFS block embedding in task descriptions"
      contains: "CONTEXT_REFS"
    - path: "packages/framework/agents/validator.md"
      provides: "Findings-as-document pattern replacing description overwrite"
      contains: "validator-findings"
    - path: "packages/framework/agents/executor.md"
      provides: "Implementation summary document storage and context_refs fetch"
      contains: "executor-summary"
  key_links:
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "packages/framework/agents/executor.md"
      via: "SYNAPSE HANDOFF block in Task tool call"
      pattern: "SYNAPSE HANDOFF"
    - from: "packages/framework/agents/decomposer.md"
      to: "packages/framework/agents/synapse-orchestrator.md"
      via: "CONTEXT_REFS block in task description parsed by orchestrator"
      pattern: "CONTEXT_REFS"
    - from: "packages/framework/agents/executor.md"
      to: "Synapse MCP store_document"
      via: "executor-summary-{task_id} document"
      pattern: "executor-summary"
    - from: "packages/framework/agents/validator.md"
      to: "Synapse MCP store_document"
      via: "validator-findings-{task_id} document"
      pattern: "validator-findings"
---

<objective>
Update orchestrator handoff protocol with structured SYNAPSE HANDOFF block, fix validator to store findings as documents instead of overwriting task description, ensure integration-checker and plan-reviewer persist findings as documents, add executor implementation summary storage, add decomposer context_refs population, and add executor/validator context_refs fetch protocol.

Purpose: The handoff protocol ensures subagents receive all needed IDs without searching. The findings-as-document pattern prevents task spec destruction. Context_refs ensure curated context flows from decomposer through orchestrator to executor/validator.

Output: Orchestrator has structured handoff, validator stores findings correctly, all specialist agents persist output as documents, context flows through the full RPEV chain.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-agent-prompts-level-awareness/19-CONTEXT.md
@.planning/phases/19-agent-prompts-level-awareness/19-RESEARCH.md
@.planning/phases/19-agent-prompts-level-awareness/19-01-SUMMARY.md
@.planning/phases/19-agent-prompts-level-awareness/19-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update orchestrator handoff protocol and decomposer context_refs</name>
  <files>
    packages/framework/agents/synapse-orchestrator.md,
    packages/framework/agents/decomposer.md
  </files>
  <action>
**synapse-orchestrator.md -- Update Subagent Handoff Protocol:**

Replace the existing "## Subagent Handoff Protocol" section (currently around line 266-292, containing the `## Synapse Context` template) with:

```markdown
## Subagent Handoff Protocol

Every Task tool call to a subagent MUST include the SYNAPSE HANDOFF block in the prompt. Subagents do NOT inherit session context -- they start fresh. Every handoff must be self-contained.

**Block format (include verbatim in every Task prompt):**

```
--- SYNAPSE HANDOFF ---
project_id: {project_id from session context}
task_id: {task.id}
hierarchy_level: {epic|feature|component|task}
rpev_stage_doc_id: rpev-stage-{task_id}
doc_ids: {comma-separated from CONTEXT_REFS block, or "none"}
decision_ids: {comma-separated from CONTEXT_REFS block, or "none"}
--- END HANDOFF ---
```

**Building the handoff:**
1. Read the task via `get_task_tree(project_id, task_id)`
2. Parse the `---CONTEXT_REFS---` block from the task description:
   - Extract `document_ids:` list
   - Extract `decision_ids:` list
   - If no CONTEXT_REFS block: use "none" for both
3. Determine `hierarchy_level` from task depth: depth 0 = epic, depth 1 = feature, depth 2 = component, depth 3 = task
4. Construct the handoff block with all values filled in
5. Include the handoff block at the TOP of the Task tool prompt, before any other instructions

**Example:**
```
--- SYNAPSE HANDOFF ---
project_id: my-project
task_id: 01HXYZ123ABC
hierarchy_level: task
rpev_stage_doc_id: rpev-stage-01HXYZ123ABC
doc_ids: rpev-stage-01HXYZ_PARENT, architecture-auth-flow
decision_ids: D-47, D-52
--- END HANDOFF ---

Implement the JWT signing utility as specified in the task description. Use RS256 with jose library.
```

**Subagent instructions to include in every Task prompt:**
"Parse the --- SYNAPSE HANDOFF --- block first. Use project_id for all MCP calls. Fetch task spec via get_task_tree(task_id). Fetch context via get_smart_context(doc_ids). Begin work only after loading context."
```

Also update the inline references throughout the orchestrator file:
- In "Progressive Decomposition Protocol > Epic -> Features" step 1, update the handoff description from "Pass: project_id, task_id, rpev_stage_doc_id, epic description..." to: "Pass via SYNAPSE HANDOFF block: project_id, task_id, hierarchy_level=epic, rpev_stage_doc_id, doc_ids, decision_ids from CONTEXT_REFS"
- In "Feature -> Tasks" step 1, same update
- In "Wave Execution Protocol > Executing a Wave" step 1, update "Pass (via Subagent Handoff Protocol):" to reference the structured block: "Include SYNAPSE HANDOFF block in each Task prompt with: project_id, task_id, hierarchy_level=task, rpev_stage_doc_id, doc_ids and decision_ids from CONTEXT_REFS"

**decomposer.md -- Add Context Refs Population:**

Add a new "### Step 5: Attach Context Refs to Leaf Tasks" section in the "Decomposition Protocol" AFTER "### Step 4: Record Decomposition Decisions" and BEFORE "## Key Tool Sequences":

```markdown
### Step 5: Attach Context Refs to Leaf Tasks

After calling get_smart_context and query_decisions during decomposition, attach relevant IDs to each leaf task (depth=3) description. Embed a CONTEXT_REFS block at the end of the task description:

```
{task description and acceptance criteria}

---CONTEXT_REFS---
document_ids: [{relevant_doc_ids from get_smart_context}]
decision_ids: [{relevant_decision_ids from query_decisions}]
---END_CONTEXT_REFS---
```

**What to include:**
- `document_ids`: IDs of documents directly relevant to this task (architecture patterns, research findings, RPEV stage docs). Include the parent feature's rpev-stage doc_id if it exists.
- `decision_ids`: IDs of decisions that constrain this task's implementation (architectural choices, design patterns).

**Rules:**
- Only include IDs you actually found during decomposition -- do not guess
- If no relevant docs/decisions: include empty lists `document_ids: []\ndecision_ids: []`
- The orchestrator parses this block when building handoffs to executor/validator
- Context refs are a convention embedded in description, NOT a DB column
```

Also update the existing "### Step 5: Respect Approval Mode" to become "### Step 6: Respect Approval Mode" (renumber).

Ensure the decomposer's Key Tool Sequences (updated in Plan 02) already shows CONTEXT_REFS in the create_task example. If not, verify it does.
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "SYNAPSE HANDOFF" packages/framework/agents/synapse-orchestrator.md && grep -c "CONTEXT_REFS" packages/framework/agents/decomposer.md</automated>
  </verify>
  <done>
Orchestrator has structured SYNAPSE HANDOFF block format with parsing instructions. All inline references (decomposition, wave execution) updated to reference the block. Decomposer has Step 5: Attach Context Refs with CONTEXT_REFS block format and rules for what to include. Approval Mode renumbered to Step 6.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix validator findings pattern and add executor/validator context_refs fetch</name>
  <files>
    packages/framework/agents/validator.md,
    packages/framework/agents/executor.md
  </files>
  <action>
**validator.md -- Replace findings-as-description with findings-as-document:**

1. In the "Task Validation Protocol > Step 3: Verdict" section, REPLACE the "If any acceptance criterion is not met" block. The current block (around line 85-97) shows:
```
update_task(task_id, status: "failed", description: "VALIDATION FINDING: {clear summary}
Expected: ...
Found: ...
```
REPLACE with:
```markdown
**If any acceptance criterion is not met:**
1. Store findings as a document (NEVER put findings in task description):
   ```
   store_document(
     project_id: "{project_id}",
     doc_id: "validator-findings-{task_id}",
     title: "Validation Findings: {task_title}",
     category: "validation_report",
     status: "active",
     tags: "|validator|findings|{task_id}|",
     content: "## Findings\n{clear summary}\n\n## Expected\n{what the spec requires}\n\n## Found\n{what was implemented}\n\n## Location\n{specific file:line references}\n\n## Test Output\n{relevant test failure output}",
     actor: "validator"
   )
   ```
2. Link findings to the task:
   ```
   link_documents(
     project_id: "{project_id}",
     from_id: "validator-findings-{task_id}",
     to_id: "{task_id}",
     relationship_type: "validates",
     actor: "validator"
   )
   ```
3. Update task status ONLY (no description change):
   ```
   update_task(
     project_id: "{project_id}",
     task_id: "{task_id}",
     status: "failed",
     actor: "validator"
   )
   ```
- The orchestrator will trigger Debugger -> retry based on your findings document
- The Debugger reads your validator-findings-{task_id} document for root-cause analysis
```

2. Also REMOVE or REPLACE the "Fail Task" shortcut in the existing "Key Tool Sequences" section (which was already updated in Plan 02). The old pattern was:
```
**Fail Task:**
`update_task(task_id, status: "failed", description: "VALIDATION FINDING: {explanation}", actor: "validator")`
```
Verify this was already replaced in Plan 02. If a vestige remains, remove it.

3. Update the "Example" section at the bottom. Replace step 6:
```
6. `update_task(status: "failed", description: "VALIDATION FINDING: Token TTL is '1h'...")`
```
With:
```
6. `store_document(project_id: "{project_id}", doc_id: "validator-findings-{task_id}", title: "Validation Findings: JWT signing", category: "validation_report", content: "## Findings\nToken TTL is '1h' (jwt.ts:23) but spec and decision D-47 require 15-min. Tests don't assert TTL value.\n\n## Expected\n15-min TTL per spec and D-47\n\n## Found\n'1h' at jwt.ts:23\n\n## Test Output\nTests pass but don't assert TTL", actor: "validator")`
7. `link_documents(from_id: "validator-findings-{task_id}", to_id: "{task_id}", relationship_type: "validates", actor: "validator")`
8. `update_task(task_id: "{task_id}", status: "failed", actor: "validator")` -- status only, findings in document
```

4. Add `store_document` and `link_documents` to the validator's `tools:` frontmatter if not already present. Check the current frontmatter -- the tools list currently does NOT include store_document or link_documents. Add them:
```yaml
tools: Read, Bash, Glob, Grep, mcp__synapse__get_task_tree, mcp__synapse__get_smart_context, mcp__synapse__query_decisions, mcp__synapse__check_precedent, mcp__synapse__update_task, mcp__synapse__search_code, mcp__synapse__store_document, mcp__synapse__link_documents
```
(These were added to agents.toml in Phase 18 but the frontmatter was not updated.)

5. Add `store_document` and `link_documents` to the integration-checker's and plan-reviewer's `tools:` frontmatter if not already present. Check their current tools lists and add if missing.

**executor.md -- Add Task Start Protocol (context_refs fetch):**

Add a new "## Task Start Protocol" section AFTER the "## Level Context" section and BEFORE "## Core Behaviors":

```markdown
## Task Start Protocol

Every task begins with this sequence:

1. Parse the `--- SYNAPSE HANDOFF ---` block to extract: project_id, task_id, hierarchy_level, rpev_stage_doc_id, doc_ids, decision_ids
2. `get_task_tree(project_id: "{project_id}", task_id: "{task_id}")` -- load full task spec, acceptance criteria, and CONTEXT_REFS block from description
3. If doc_ids from handoff is not "none": `get_smart_context(project_id: "{project_id}", mode: "detailed", doc_ids: [{doc_ids from handoff}])` -- fetch curated context
4. If doc_ids is "none" or empty: `get_smart_context(project_id: "{project_id}", mode: "overview", max_tokens: 3000)` -- fallback to overview
5. Parse any additional doc_ids from the CONTEXT_REFS block in the task description (not already in handoff) and fetch those too
6. Proceed with implementation using loaded context

Do NOT skip steps 1-4. Context from Synapse prevents re-discovering what's already known.
```

**validator.md -- Add Task Start Protocol (context_refs fetch):**

Add a similar "## Task Start Protocol" section AFTER "## Level Context" and BEFORE "## Core Behaviors":

```markdown
## Task Start Protocol

Every validation begins with this sequence:

1. Parse the `--- SYNAPSE HANDOFF ---` block to extract: project_id, task_id, hierarchy_level, rpev_stage_doc_id, doc_ids, decision_ids
2. `get_task_tree(project_id: "{project_id}", task_id: "{task_id}")` -- load task spec and acceptance criteria
3. If doc_ids from handoff is not "none": `get_smart_context(project_id: "{project_id}", mode: "detailed", doc_ids: [{doc_ids from handoff}])` -- fetch curated context including decisions that constrain this task
4. If doc_ids is "none" or empty: `get_smart_context(project_id: "{project_id}", mode: "overview", max_tokens: 3000)` -- fallback
5. `query_decisions(project_id: "{project_id}")` -- find all decisions relevant to this task's domain (if decision_ids from handoff, focus on those)
6. Proceed with validation using loaded context

Do NOT skip steps 1-5. The executor implemented against this context -- validate against the same source of truth.
```

**Constraint updates:**
- In validator.md "## Constraints", update the line "Cannot store decisions" to clarify the new capabilities: remove "Tier authority is empty" if present, and add: "**Can store validation findings** via store_document + link_documents. Cannot store decisions."
- In executor.md "## Constraints", ensure existing content is compatible with the new summary-storage behavior. Add if not present: "**Store an implementation summary** via store_document before marking task done."
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "validator-findings" packages/framework/agents/validator.md && grep -c "Task Start Protocol" packages/framework/agents/executor.md && grep -c "SYNAPSE HANDOFF" packages/framework/agents/executor.md</automated>
  </verify>
  <done>
Validator stores findings via store_document + link_documents (never in task description). Validator and executor have Task Start Protocol with SYNAPSE HANDOFF parsing and context_refs fetch. Validator example updated to show store_document pattern. Validator frontmatter includes store_document and link_documents tools. No instance of "VALIDATION FINDING" in update_task description remains.
  </done>
</task>

</tasks>

<verification>
1. `grep "VALIDATION FINDING" packages/framework/agents/validator.md` -- should return 0 matches in update_task context (may appear in "Warning signs" text)
2. `grep -c "SYNAPSE HANDOFF" packages/framework/agents/synapse-orchestrator.md` -- at least 5 (block definition + references)
3. `grep -c "CONTEXT_REFS" packages/framework/agents/decomposer.md` -- at least 3 (block definition + rules + example)
4. `grep -c "Task Start Protocol" packages/framework/agents/executor.md` -- at least 1
5. `grep -c "Task Start Protocol" packages/framework/agents/validator.md` -- at least 1
6. `grep -c "validator-findings" packages/framework/agents/validator.md` -- at least 3 (in Key Tool Sequences, Task Validation Protocol, Example)
7. `grep -c "executor-summary" packages/framework/agents/executor.md` -- at least 2 (in Key Tool Sequences)
8. `grep "store_document" packages/framework/agents/validator.md | head -1` -- confirms store_document in frontmatter tools
9. Verify integration-checker.md and plan-reviewer.md frontmatter includes store_document + link_documents
</verification>

<success_criteria>
- Orchestrator uses structured `--- SYNAPSE HANDOFF ---` block format with all 6 fields (project_id, task_id, hierarchy_level, rpev_stage_doc_id, doc_ids, decision_ids)
- Validator NEVER writes findings into task description -- exclusively uses store_document + link_documents, then update_task with status only
- Validator frontmatter tools list includes store_document and link_documents
- Integration-checker and plan-reviewer frontmatter tools list includes store_document and link_documents
- Executor stores implementation summaries as documents linked to tasks before marking done
- Decomposer embeds CONTEXT_REFS block in leaf task descriptions
- Executor and validator both have Task Start Protocol that parses SYNAPSE HANDOFF and fetches context_refs
- Old "Fail Task" pattern (update_task with description: "VALIDATION FINDING:...") is fully removed from validator
</success_criteria>

<output>
After completion, create `.planning/phases/19-agent-prompts-level-awareness/19-03-SUMMARY.md`
</output>
