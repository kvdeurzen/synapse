---
phase: 26.3-tdd
plan: "01"
subsystem: testing
tags: [tdd, agent-prompt, test-designer, test-contract, provides-vocabulary]

# Dependency graph
requires:
  - phase: 26.2-agent-handoff-tightening
    provides: task-designer agent with spec field, input/output contract patterns, provides vocabulary

provides:
  - test-designer agent prompt (packages/framework/agents/test-designer.md)
  - test-contract provides vocabulary slug in _synapse-protocol.md
  - agents.toml test-designer registration with opus model and Write/Edit tools
  - trust.toml test-designer tier_authority = []

affects:
  - 26.3-02 (executor update to consume test-contract)
  - 26.3-03 (validator update to run pre-written tests)
  - task-auditor (verifies @requirement coverage from test-contract)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "test-designer agent pattern: convention discovery before writing + RED verification + test-contract storage"
    - "@requirement tracing: every test tagged with verbatim requirement text from spec acceptance criteria"
    - "test-contract provides slug: 12th slug in fixed vocabulary, consumed by task-auditor for coverage verification"

key-files:
  created:
    - packages/framework/agents/test-designer.md
  modified:
    - packages/framework/agents/_synapse-protocol.md
    - packages/framework/config/agents.toml
    - packages/framework/config/trust.toml

key-decisions:
  - "test-designer has Write and Edit tools (unlike task-designer) — writes real test files to disk, not documents"
  - "test-designer model is opus — test design requires deep requirement comprehension per locked decision"
  - "test-designer tier_authority=[] — writes tests, not decisions; draft convention applies if needed"
  - "test-contract is 12th provides vocabulary slug — inserted after task-spec in pipeline ordering"
  - "Convention Discovery is a CRITICAL required step before writing any tests — prevents framework mismatch"
  - "RED verification accepts 'Cannot find module' and 'undefined is not a function' as correct failure modes"

patterns-established:
  - "Convention Discovery first: Glob existing test files, Read 2-3, lock in runner/imports/naming before writing"
  - "@requirement comment on every test: links test to verbatim acceptance criterion from task spec"
  - "Test Contract Storage: store_document + link_documents + update_task (3-step sequence)"
  - "HALT semantics: if task.spec is null, halt immediately rather than guessing at requirements"

requirements-completed: [TDD-01, TDD-07]

# Metrics
duration: 3min
completed: 2026-03-12
---

# Phase 26.3 Plan 01: Test Designer Agent Summary

**test-designer agent prompt with convention discovery, RED verification, @requirement tracing, and test-contract provides slug added as 12th vocabulary entry**

## Performance

- **Duration:** ~3 minutes
- **Started:** 2026-03-12T19:48:06Z
- **Completed:** 2026-03-12T19:51:12Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Created complete test-designer.md agent prompt (396 lines) following task-designer.md patterns with agent-specific additions
- Added test-contract as 12th slug to _synapse-protocol.md Provides Vocabulary table
- Registered test-designer in agents.toml with opus model, testing-strategy role_skill, Write/Edit/Bash/Read/Glob/Grep + MCP tools
- Added test-designer = [] to trust.toml tier_authority (no decision authority)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create test-designer.md agent prompt** - `5d6a7bd` (feat)
2. **Task 2: Update protocol, agents.toml, and trust.toml** - `d3ae1c8` (feat)

## Files Created/Modified

- `packages/framework/agents/test-designer.md` - New test-designer agent prompt (396 lines)
- `packages/framework/agents/_synapse-protocol.md` - Added test-contract as 12th provides slug
- `packages/framework/config/agents.toml` - Registered [agents.test-designer] entry
- `packages/framework/config/trust.toml` - Added test-designer = [] to tier_authority

## Decisions Made

- test-designer gets Write and Edit tools that task-designer lacks — necessary because it writes actual test files to disk rather than spec content to a task field
- Convention Discovery section is marked CRITICAL and precedes any test writing — prevents test framework mismatch that would cause invalid RED failures
- RED verification distinguishes acceptable failures (missing module, undefined function) from unacceptable failures (syntax error in test, wrong assertion setup) — ensures tests fail for the right reason
- HALT if spec is null uses same semantics established in 26.2-03 for task-designer input contracts

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- test-designer agent is fully defined and registered; downstream plans (26.3-02, 26.3-03) can now reference it
- test-contract slug is in the fixed vocabulary — task-auditor can verify @requirement coverage from test-contract documents
- Framework tests continue to pass (114 pass, 0 fail) — no regressions from config changes

---
*Phase: 26.3-tdd*
*Completed: 2026-03-12*
