---
status: complete
phase: 26.7-pause-continue-upgrade-and-remove-synapse
source: 26.7-01-SUMMARY.md, 26.7-02-SUMMARY.md, 26.7-03-SUMMARY.md
started: 2026-03-13T15:00:00Z
updated: 2026-03-13T15:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Pause command file exists and has correct structure
expected: `packages/framework/commands/synapse/pause.md` exists with valid slash command frontmatter (name: synapse:pause), a 9-step process covering graceful/immediate stop modes, and a snapshot schema writing to `.synapse/state/pause-snapshot.json`.
result: pass

### 2. Continue command file exists and has correct structure
expected: `packages/framework/commands/synapse/continue.md` exists with valid slash command frontmatter (name: synapse:continue), an 8-step process loading snapshot, validating tasks/refinements/git divergence against live state, and resuming at any RPEV pipeline stage.
result: pass

### 3. Startup hook detects pause snapshot
expected: `packages/framework/hooks/synapse-startup.js` contains a pause detection block that checks for `.synapse/state/pause-snapshot.json`, parses it, and injects "Paused Work Detected" into contextParts with stage info and `/synapse:continue` prompt. Silent fail on error (try/catch with no throw).
result: pass

### 4. Pause detection tests pass
expected: Running `bun test --cwd packages/framework ./test/unit/hooks.test.ts` passes all tests including 4 new pause-detection cases: pipeline-active snapshot, no-pipeline snapshot, no snapshot file, and corrupt JSON snapshot.
result: pass

### 5. install.sh writes synapse_version to project.toml
expected: Running `bun test --cwd packages/framework ./test/unit/install.test.ts` passes all tests including the synapse_version tracking test that verifies the field is written to project.toml after install.
result: pass

### 6. Upgrade command file exists and has correct structure
expected: `packages/framework/commands/synapse/upgrade.md` exists with valid slash command frontmatter (name: synapse:upgrade), a 10-step process covering version detection from project.toml and GitHub API, changelog display, explicit user confirmation, install.sh invocation, auto-commit with `chore(synapse): upgrade to {version}`, and hot-reload detection for hooks.
result: pass

### 7. Remove command file exists and has correct structure
expected: `packages/framework/commands/synapse/remove.md` exists with valid slash command frontmatter (name: synapse:remove), an 11-step process with artifact inventory, separate LanceDB database deletion prompt, explicit "yes" confirmation gate, surgical `.mcp.json` stripping, surgical `settings.json` stripping using synapseSignatures, CLAUDE.md Gateway Protocol section removal via regex, and self-deletion as the absolute final step.
result: pass

### 8. All framework tests pass (no regressions)
expected: Running `bun run test:framework` passes with no regressions. All existing tests plus new pause-detection and install tests should be green.
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
