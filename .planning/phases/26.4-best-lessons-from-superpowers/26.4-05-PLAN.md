---
phase: 26.4-best-lessons-from-superpowers
plan: "05"
type: execute
wave: 3
depends_on: ["26.4-03", "26.4-04"]
files_modified:
  - packages/framework/agents/architect.md
  - packages/framework/agents/architecture-auditor.md
  - packages/framework/agents/codebase-analyst.md
  - packages/framework/agents/code-quality-reviewer.md
  - packages/framework/agents/debugger.md
  - packages/framework/agents/executor.md
  - packages/framework/agents/integration-checker.md
  - packages/framework/agents/plan-auditor.md
  - packages/framework/agents/planner.md
  - packages/framework/agents/product-researcher.md
  - packages/framework/agents/researcher.md
  - packages/framework/agents/synapse-orchestrator.md
  - packages/framework/agents/task-auditor.md
  - packages/framework/agents/task-designer.md
  - packages/framework/agents/test-designer.md
  - packages/framework/agents/validator.md
autonomous: true
requirements: [BEH-01, BEH-07]

must_haves:
  truths:
    - "All 16 agent files (15 agents + orchestrator) have an Anti-Rationalization section with at least 3 externally-sourced entries"
    - "All 8 doer agents have structured escalation statuses: DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED"
    - "All 6 reviewer agents have structured escalation statuses: APPROVED / REJECTED / NEEDS_REVISION"
    - "Anti-rationalization tables are per-agent inline, not centralized (per locked decision)"
    - "Rationalizations are sourced from external frameworks (Superpowers catalogs, published agentic frameworks), not self-generated"
  artifacts:
    - path: "packages/framework/agents/architect.md"
      provides: "Anti-rationalization table + doer escalation statuses"
      contains: "Anti-Rationalization"
    - path: "packages/framework/agents/executor.md"
      provides: "Anti-rationalization table (escalation statuses already exist from 26.3)"
      contains: "Anti-Rationalization"
    - path: "packages/framework/agents/validator.md"
      provides: "Anti-rationalization table + reviewer escalation statuses"
      contains: "Anti-Rationalization"
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "Anti-rationalization table for orchestrator-specific constraints"
      contains: "Anti-Rationalization"
  key_links:
    - from: "packages/framework/agents/*.md (doer agents)"
      to: "Superpowers verification-before-completion + TDD skills"
      via: "externally-sourced rationalization entries"
      pattern: "Anti-Rationalization"
    - from: "packages/framework/agents/*.md (reviewer agents)"
      to: "Superpowers receiving-code-review skill"
      via: "externally-sourced rationalization entries"
      pattern: "Anti-Rationalization"
---

<objective>
Add anti-rationalization tables and structured escalation statuses to all 16 agent files (15 agents + orchestrator). Anti-rationalization tables close specific escape paths for each agent's critical constraints. Escalation statuses standardize completion reporting.

Purpose: This is the single biggest behavioral enforcement improvement. Currently agent constraints are stated as rules but not defended against circumvention. Anti-rationalization tables explicitly identify how agents might rationalize violating rules and provide rebuttals. Structured escalation statuses make failure routing deterministic across all agents.

Output: All 16 agent files updated with inline anti-rationalization tables and structured escalation statuses.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.4-best-lessons-from-superpowers/26.4-CONTEXT.md
@.planning/phases/26.4-best-lessons-from-superpowers/26.4-RESEARCH.md
@.planning/phases/26.4-best-lessons-from-superpowers/26.4-03-SUMMARY.md
@.planning/phases/26.4-best-lessons-from-superpowers/26.4-04-SUMMARY.md
@".planning/brainstorm output/superpowers-best-practices-analysis.md"

<interfaces>
From RESEARCH.md (Pre-Cataloged External Rationalization Sources):
```
| Agent Category | Primary Rationalization Domain | External Source |
| All doer agents | Premature completion claims, skipping verification | Superpowers verification-before-completion + TDD skill |
| executor | Test immutability violation, scope creep | Superpowers TDD skill (11 rationalizations) |
| validator | Trusting executor report, skipping independent tests | Superpowers subagent-driven-development |
| All reviewer agents | Approval without thorough review, sycophantic agreement | Superpowers receiving-code-review skill |
| architect, planner | Decision draft bypass | Phase 26.1 decision draft protocol |
| orchestrator | Dispatching without verifying TDD gate, context pollution | Superpowers subagent-driven-development |
| researcher | Stating LOW confidence findings as facts | Phase 25-05 confidence tier protocol |
```

From executor.md (existing Status Reporting pattern to replicate):
```markdown
## Status Reporting
Your output document MUST include a `## Status` section with one of these values:
| Status | Meaning | When to use |
| DONE | ... | ... |
| DONE_WITH_CONCERNS | ... | ... |
| BLOCKED | ... | ... |
| NEEDS_CONTEXT | ... | ... |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add anti-rationalization tables to all 16 agent files</name>
  <files>packages/framework/agents/architect.md, packages/framework/agents/architecture-auditor.md, packages/framework/agents/codebase-analyst.md, packages/framework/agents/code-quality-reviewer.md, packages/framework/agents/debugger.md, packages/framework/agents/executor.md, packages/framework/agents/integration-checker.md, packages/framework/agents/plan-auditor.md, packages/framework/agents/planner.md, packages/framework/agents/product-researcher.md, packages/framework/agents/researcher.md, packages/framework/agents/synapse-orchestrator.md, packages/framework/agents/task-auditor.md, packages/framework/agents/task-designer.md, packages/framework/agents/test-designer.md, packages/framework/agents/validator.md</files>
  <action>
    Add an `## Anti-Rationalization` section to each of the 16 agent files. Place it near the end of the file (after output contract, before any final notes). Each table uses the format:

    ```markdown
    ## Anti-Rationalization

    The following rationalizations are attempts to skip critical constraints. They are listed here because they are wrong, not because they are reasonable.

    | Rationalization | Why It's Wrong | What To Do Instead |
    |----------------|----------------|-------------------|
    | "..." | ... | ... |
    ```

    Each agent gets at least 3-5 rationalizations specific to its critical constraints. Use WebFetch or WebSearch to source rationalizations from the Superpowers framework documentation and other published agentic framework best practices. Do NOT generate rationalizations from scratch (locked decision).

    **Per-agent rationalization guidance (from RESEARCH.md Pre-Cataloged Sources):**

    **Doer agents (executor, architect, planner, task-designer, researcher, product-researcher, debugger, codebase-analyst, test-designer):**
    - Source from Superpowers verification-before-completion skill (24 failure memories) and TDD skill (11 rationalizations)
    - Key themes: premature completion claims, skipping verification steps, scope creep, taking shortcuts

    Agent-specific entries:
    - **executor**: "Tests are clearly passing, I don't need to run them" / "This test seems wrong, I should fix it" (test immutability) / "The spec is ambiguous, I'll make a reasonable assumption" (should HALT as NEEDS_CONTEXT)
    - **architect**: "The architecture is obvious, skip the options analysis" / "I can store this decision directly instead of drafting" (decision draft bypass) / "This is just a small feature, no formal architecture needed"
    - **planner**: "The task decomposition is straightforward, skip the requirements trace" / "I can combine these tasks to save time" / "Test expectations are obvious, I don't need to specify them for test-designer"
    - **task-designer**: "The implementation approach is clear from the architecture, minimal spec needed" / "Mock code is overkill for this simple task" / "I can skip file path enumeration since the executor will figure it out"
    - **test-designer**: "The implementation doesn't exist yet so I can't write meaningful tests" (RED phase requires tests before code) / "This is a simple CRUD operation, tests are overkill" / "Convention Discovery is unnecessary since I already know the test framework"
    - **researcher**: "A single web source is sufficient for this finding" (must state LOW confidence) / "This seems right based on my training data" (not a source)
    - **product-researcher**: "The user's intent is clear from the request, no clarifying questions needed" / "I have enough context from the codebase, no need to query Synapse DB"
    - **debugger**: "The fix is obvious, skip root cause analysis" / "Applying the fix will confirm the diagnosis" (confirm first, then fix)
    - **codebase-analyst**: "The index is recent enough, re-indexing would waste time" / "This file hasn't changed, skip it"

    **Reviewer agents (validator, code-quality-reviewer, architecture-auditor, plan-auditor, task-auditor, integration-checker):**
    - Source from Superpowers receiving-code-review and subagent-driven-development skills
    - Key themes: trusting self-reports, approval without thorough review, sycophantic agreement

    Agent-specific entries:
    - **validator**: "The executor says tests pass, I'll verify quickly" (must run tests independently FIRST) / "The implementation looks correct, I don't need to check every spec requirement" / "I already read the executor's summary, let me verify the parts that seem off" (independence violation)
    - **code-quality-reviewer**: (already has entries from Plan 03, verify they exist and add more if needed)
    - **architecture-auditor**: "The architect is experienced, this design is probably fine" / "Minor inconsistencies aren't worth blocking the pipeline" / "I'll approve with a note rather than formally reject" (formal rejection is correct for issues)
    - **plan-auditor**: "The task count looks reasonable, detailed verification is unnecessary" / "The planner accounted for the architecture, I don't need to cross-reference" / "This plan is similar to previous ones, it's probably fine"
    - **task-auditor**: "The spec and tests align superficially, deep triangulation is overkill" / "The planner's test expectations are just guidance, minor mismatches are fine" / "Rejecting would slow down the pipeline"
    - **integration-checker**: "Each task passed validation, integration will be fine" / "The interfaces match, runtime behavior testing is excessive"

    **Orchestrator:**
    - "The pipeline is behind schedule, I should skip the code-quality-reviewer step" / "This task is simple, fresh agent overhead isn't worth it" (reuse temptation) / "The executor didn't produce test artifacts but the implementation looks correct, proceed anyway" (TDD gate violation) / "I can batch these dispatches to save time" (must use Pool Manager Protocol)

    **IMPORTANT:** These are guidance entries. The executor implementing this task MUST use WebFetch or WebSearch to retrieve actual Superpowers documentation for exact rationalization text. The entries above are starting points — refine or replace them with the externally-sourced equivalents found during research. The locked decision is that tables must NOT be self-generated.
  </action>
  <verify>
    <automated>bun -e "const fs=require('fs');const agents=fs.readdirSync('packages/framework/agents').filter(f=>f.endsWith('.md')&&!f.startsWith('_'));const missing=agents.filter(a=>{const c=fs.readFileSync('packages/framework/agents/'+a,'utf8');return !c.includes('Anti-Rationalization')});if(missing.length>0){console.error('Missing Anti-Rationalization in:',missing);process.exit(1)}console.log('All',agents.length,'agents have Anti-Rationalization tables');"</automated>
  </verify>
  <done>
    - All 16 agent .md files contain an Anti-Rationalization section
    - Each table has at least 3 entries specific to the agent's critical constraints
    - Rationalizations are sourced from external frameworks (Superpowers catalogs, published agentic frameworks)
    - Tables are inline per-agent (not centralized in _synapse-protocol.md)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add structured escalation statuses to all agents lacking them</name>
  <files>packages/framework/agents/architect.md, packages/framework/agents/architecture-auditor.md, packages/framework/agents/codebase-analyst.md, packages/framework/agents/debugger.md, packages/framework/agents/integration-checker.md, packages/framework/agents/plan-auditor.md, packages/framework/agents/planner.md, packages/framework/agents/product-researcher.md, packages/framework/agents/researcher.md, packages/framework/agents/synapse-orchestrator.md, packages/framework/agents/task-auditor.md, packages/framework/agents/task-designer.md, packages/framework/agents/test-designer.md, packages/framework/agents/validator.md</files>
  <action>
    Add a `## Status Reporting` section to each agent that does not already have one. The executor already has this section from Phase 26.3 — verify its content matches and skip if already present. The code-quality-reviewer already has its reviewer statuses from Plan 03 — verify and skip if already present.

    **Doer agent status set** (for: executor, architect, planner, task-designer, test-designer, researcher, product-researcher, debugger, codebase-analyst):

    ```markdown
    ## Status Reporting

    Your output document MUST include a `## Status` section with exactly one of:

    | Status | Meaning | When to use |
    |--------|---------|-------------|
    | DONE | Task completed successfully | All deliverables produced, verification passed |
    | DONE_WITH_CONCERNS | Task completed but with noted issues | Deliverables produced but edge cases, tech debt, or risks identified |
    | NEEDS_CONTEXT | Cannot proceed without additional information | Spec is ambiguous, required context is missing, external dependency unclear |
    | BLOCKED | Cannot complete the task | Implementation impossible with current constraints, test encodes wrong requirement, dependency broken |
    ```

    **Reviewer agent status set** (for: validator, code-quality-reviewer, architecture-auditor, plan-auditor, task-auditor, integration-checker):

    ```markdown
    ## Status Reporting

    Your output document MUST include a `## Status` section with exactly one of:

    | Status | Meaning | When to use |
    |--------|---------|-------------|
    | APPROVED | Review passed | All review criteria met, no blocking issues |
    | REJECTED | Review failed | Critical issues found that block progression |
    | NEEDS_REVISION | Changes required | Specific changes needed; routes back to the doer with your feedback |
    ```

    **Orchestrator status:** The orchestrator does not produce output documents — it reports to the gateway. Add a brief note that the orchestrator uses stage document updates and failure reports instead of document-based statuses.

    **Existing status sections:** The executor already has DONE/DONE_WITH_CONCERNS/BLOCKED/NEEDS_CONTEXT from Phase 26.3. Verify the format matches the template above and adjust wording for consistency if needed (do not change semantics). The code-quality-reviewer has APPROVED/REJECTED/NEEDS_REVISION from Plan 03 — verify and skip.

    Place the Status Reporting section in the Output Contract area of each agent (after the output document requirements, before Anti-Rationalization).
  </action>
  <verify>
    <automated>bun -e "const fs=require('fs');const doers=['architect','planner','task-designer','test-designer','researcher','product-researcher','debugger','codebase-analyst','executor'];const reviewers=['validator','code-quality-reviewer','architecture-auditor','plan-auditor','task-auditor','integration-checker'];let ok=true;doers.forEach(a=>{const c=fs.readFileSync('packages/framework/agents/'+a+'.md','utf8');if(!c.includes('DONE_WITH_CONCERNS')){console.error(a+' missing doer statuses');ok=false}});reviewers.forEach(a=>{const c=fs.readFileSync('packages/framework/agents/'+a+'.md','utf8');if(!c.includes('NEEDS_REVISION')){console.error(a+' missing reviewer statuses');ok=false}});if(!ok)process.exit(1);console.log('All agents have structured statuses');"</automated>
  </verify>
  <done>
    - All 8 doer agents (executor, architect, planner, task-designer, test-designer, researcher, product-researcher, debugger, codebase-analyst) have DONE/DONE_WITH_CONCERNS/NEEDS_CONTEXT/BLOCKED status set
    - All 6 reviewer agents (validator, code-quality-reviewer, architecture-auditor, plan-auditor, task-auditor, integration-checker) have APPROVED/REJECTED/NEEDS_REVISION status set
    - Orchestrator has a note about using stage documents instead of document-based statuses
    - Status sections are consistently formatted across all agents
    - Existing executor status section preserved (only adjusted for consistency if needed)
  </done>
</task>

</tasks>

<verification>
- All 16 agent files contain "Anti-Rationalization" section
- All doer agents contain "DONE_WITH_CONCERNS" (confirms doer status set)
- All reviewer agents contain "NEEDS_REVISION" (confirms reviewer status set)
- `bun run test` — full suite still green
</verification>

<success_criteria>
- Every agent has anti-rationalization tables with externally-sourced entries specific to its constraints
- Every pipeline agent has structured escalation statuses in its output contract
- Doer/reviewer status sets are correctly partitioned (no doer with APPROVED, no reviewer with BLOCKED)
- Orchestrator has appropriate note about stage-document-based reporting
</success_criteria>

<output>
After completion, create `.planning/phases/26.4-best-lessons-from-superpowers/26.4-05-SUMMARY.md`
</output>
