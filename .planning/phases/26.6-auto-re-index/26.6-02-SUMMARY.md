---
phase: 26.6-auto-re-index
plan: 02
subsystem: agents
tags: [orchestrator, init, index-codebase, wave-execution, ollama]

# Dependency graph
requires: ["26.6-01"]
provides:
  - Wave-level re-index step in orchestrator between executor completion and validator dispatch
  - Conditional codebase indexing in /synapse:init after init_project (when Ollama available)
  - Single-command project setup — /synapse:init indexes codebase without needing /synapse:map
affects: [synapse-orchestrator, synapse-init-command]

# Tech tracking
tech-stack:
  added: []
  patterns: [Wave-level re-index before validators pattern, Non-blocking indexing failure handling]

key-files:
  created: []
  modified:
    - packages/framework/agents/synapse-orchestrator.md
    - packages/framework/commands/synapse/init.md

key-decisions:
  - "Wave-level re-index inserts as step 2b between await-executors (step 2) and validator-dispatch (step 3) in orchestrator wave execution loop"
  - "Re-index failure is NON-BLOCKING in both orchestrator and init: warn and continue; never halt wave or abort init for indexing failure"
  - "init.md step 7b gates indexing on Ollama availability via curl check for nomic-embed-text model"
  - "Step 11 summary in init.md conditionally shows Next step: /synapse:refine (indexed) vs /synapse:map (not indexed)"
  - "Anti-pattern clarified: Ollama check is only in optional step 7b, not in core init flow (steps 1-7)"

patterns-established:
  - "Non-blocking indexing: always call index_codebase, emit warning on failure, proceed regardless"
  - "Ollama check pattern: curl -sf http://localhost:11434/api/tags | grep -q nomic-embed-text"

requirements-completed: [RIDX-03, RIDX-04]

# Metrics
duration: 2min
completed: 2026-03-13
---

# Phase 26.6 Plan 02: Wire index_codebase into Orchestrator Wave Loop and /synapse:init Summary

**Inserted index_codebase as a non-blocking step 2b between executor completion and validator dispatch in the orchestrator wave loop, and added conditional post-init codebase indexing to /synapse:init so new projects are fully indexed in one command when Ollama is available**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-13T12:36:49Z
- **Completed:** 2026-03-13T12:38:20Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added step 2b to orchestrator wave execution loop: calls `index_codebase(project_id, project_root, actor: "synapse-orchestrator")` after all wave executors complete and before validators are dispatched
- Re-index failure in step 2b emits a warning and proceeds — validators run on potentially stale index rather than blocking the wave
- Existing feature-completion re-index (line ~177) preserved as belt-and-suspenders
- Added `mcp__synapse__index_codebase` to init.md allowed-tools frontmatter — required for the tool call to work in the command context
- Inserted step 7b in init.md: curl-based Ollama availability check, conditional index_codebase call with project_id + project_root + actor
- init.md completes successfully whether Ollama is available or not — indexing is fully optional
- Updated step 11 summary template: added `Codebase:` line and conditional `Next step` (refine vs map) based on indexing outcome
- Clarified init.md anti-patterns: "Do NOT check for Ollama during the core init flow (steps 1-7) — only the optional indexing step (7b) checks Ollama, and it is non-blocking"

## Task Commits

Each task was committed atomically:

1. **Task 1: Add wave-level re-index step to orchestrator** - `c32fbc7` (feat)
2. **Task 2: Add conditional index_codebase to init.md after init_project** - `cf01a47` (feat)

## Files Created/Modified

- `packages/framework/agents/synapse-orchestrator.md` - Added step 2b wave-level re-index between executor completion and validator dispatch
- `packages/framework/commands/synapse/init.md` - Added mcp__synapse__index_codebase to allowed-tools, inserted step 7b, updated step 11 summary and anti-patterns

## Decisions Made

- Step 2b position: after "Await all executor results" (step 2), before "For each completed task: spawn Validator" (step 3) — ensures validators always have fresh index
- Non-blocking failure handling: warn with error message and proceed; indexing failure must never halt a wave or abort init
- Ollama check uses curl + grep pattern: `curl -sf http://localhost:11434/api/tags | grep -q "nomic-embed-text"` — consistent with existing project patterns
- Step 11 "Next step" is now conditional: /synapse:refine when indexed, /synapse:map when not — eliminates friction for first-time setup when Ollama is running

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. Changes apply on next session start.

## Next Phase Readiness

- Validators in every wave will now always receive a fresh code index from wave executors
- New Synapse project setup is a single `/synapse:init` command when Ollama + nomic-embed-text are available
- Both changes are backward compatible — no schema changes, no new dependencies
- Phase 26.6 is complete: RIDX-01 through RIDX-04 all satisfied

---
*Phase: 26.6-auto-re-index*
*Completed: 2026-03-13*

## Self-Check: PASSED

- packages/framework/agents/synapse-orchestrator.md: FOUND (c32fbc7 committed)
- packages/framework/commands/synapse/init.md: FOUND (cf01a47 committed)
- index_codebase in orchestrator step 2b: FOUND (line 151)
- index_codebase before validator dispatch (step 3): FOUND (line 156)
- mcp__synapse__index_codebase in init.md allowed-tools: FOUND (line 9)
- step 7b in init.md: FOUND (line 134)
- Non-blocking handling in both files: FOUND
