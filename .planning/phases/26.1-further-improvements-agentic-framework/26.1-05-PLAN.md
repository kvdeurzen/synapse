---
phase: 26.1-further-improvements-agentic-framework
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - packages/framework/config/agents.toml
  - packages/framework/config/trust.toml
  - packages/framework/workflows/pev-workflow.md
  - packages/framework/workflows/decision-protocol.md
  - packages/framework/workflows/research-decision-flow.md
  - packages/framework/commands/synapse/focus.md
  - packages/framework/commands/synapse/refine.md
autonomous: true
requirements: [FI-05, FI-06]

must_haves:
  truths:
    - "agents.toml has entries for all 14 agents (new + kept), no entries for deleted agents"
    - "trust.toml tier_authority updated: orchestrator = [], new agents have correct tiers"
    - "Old agent files are deleted: product-strategist.md, decomposer.md, plan-reviewer.md"
    - "All workflow files reference new agent names (Planner not Decomposer, etc.)"
    - "Slash commands reference new agent names"
    - "Total agent file count in packages/framework/agents/ is 15 (14 agents + _synapse-protocol.md)"
    - "Framework tests still pass after config changes"
  artifacts:
    - path: "packages/framework/config/agents.toml"
      provides: "Updated agent registry with 14 agents"
      contains: "plan-auditor"
    - path: "packages/framework/config/trust.toml"
      provides: "Updated tier authority for all agents"
      contains: "architecture-auditor"
  key_links:
    - from: "packages/framework/config/agents.toml"
      to: "packages/framework/agents/*.md"
      via: "Every agent .md has a matching toml entry"
      pattern: "agents\\."
    - from: "packages/framework/config/trust.toml"
      to: "packages/framework/config/agents.toml"
      via: "Tier authority keys match agent slugs in agents.toml"
      pattern: "tier_authority"
---

<objective>
Update config files (agents.toml, trust.toml), delete old agent files, and update all references to old agent names across workflows and slash commands.

Purpose: This is the cleanup and wiring plan. Plans 01-04 created all new agent files but left config files, workflows, and commands referencing the old names. This plan brings everything into alignment: config registers the new roster, trust enforces the new authority model, old files are deleted, and all references point to the new names.

Output: Consistent codebase with no references to deleted agents, all configs aligned with new roster
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-further-improvements-agentic-framework/26.1-CONTEXT.md
@.planning/phases/26.1-further-improvements-agentic-framework/26.1-RESEARCH.md
@.planning/phases/26.1-further-improvements-agentic-framework/26.1-01-SUMMARY.md
@.planning/phases/26.1-further-improvements-agentic-framework/26.1-02-SUMMARY.md
@.planning/phases/26.1-further-improvements-agentic-framework/26.1-03-SUMMARY.md
@.planning/phases/26.1-further-improvements-agentic-framework/26.1-04-SUMMARY.md

@packages/framework/config/agents.toml
@packages/framework/config/trust.toml
@packages/framework/workflows/pev-workflow.md
@packages/framework/workflows/decision-protocol.md
@packages/framework/workflows/research-decision-flow.md
@packages/framework/commands/synapse/focus.md

<interfaces>
<!-- New agent roster (14 agents): -->
<!-- synapse-orchestrator, product-researcher, researcher, architect, architecture-auditor -->
<!-- planner, plan-auditor, task-designer, task-auditor -->
<!-- executor, validator, integration-checker, debugger, codebase-analyst -->

<!-- Deleted agents (3): -->
<!-- product-strategist, decomposer, plan-reviewer -->

<!-- agents.toml must have: -->
<!-- - Entry for each of the 14 agents with model, role_skills, allowed_tools -->
<!-- - No entries for deleted agents -->

<!-- trust.toml tier_authority must have: -->
<!-- synapse-orchestrator = [] (reduced from [0,1,2,3]) -->
<!-- product-researcher = [] -->
<!-- architect = [1, 2] (unchanged) -->
<!-- architecture-auditor = [] -->
<!-- planner = [2] (replaces decomposer) -->
<!-- plan-auditor = [] -->
<!-- task-designer = [2, 3] -->
<!-- task-auditor = [] -->
<!-- executor = [3] (unchanged) -->
<!-- validator = [] (unchanged) -->
<!-- integration-checker = [] (unchanged) -->
<!-- researcher = [] (unchanged) -->
<!-- debugger = [] (unchanged) -->
<!-- codebase-analyst = [] (unchanged) -->

<!-- Files with old agent name references: -->
<!-- packages/framework/workflows/pev-workflow.md — "Decomposer", "Plan Reviewer" -->
<!-- packages/framework/workflows/decision-protocol.md — "product-strategist" -->
<!-- packages/framework/workflows/research-decision-flow.md — "decomposer" -->
<!-- packages/framework/commands/synapse/focus.md — "Plan Reviewer", "Decomposer/Planner" -->
<!-- packages/framework/commands/synapse/refine.md — no old names (already checked) -->
<!-- packages/framework/test/unit/gate-hooks.test.ts — may reference old slugs -->
<!-- packages/framework/test/unit/config.test.ts — may reference old slugs -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update agents.toml and trust.toml for new agent roster</name>
  <files>packages/framework/config/agents.toml, packages/framework/config/trust.toml</files>
  <action>
Read both config files first.

**Part A: Rewrite agents.toml**

Replace the full agents.toml with the updated 14-agent roster. For each agent, the entry specifies model, role_skills, and allowed_tools matching the frontmatter of the corresponding .md file.

```toml
# Agent registry -- model assignments and configuration

[agents.synapse-orchestrator]
model = "opus"
role_skills = ["architecture-design", "brainstorming", "defining-requirements"]
allowed_tools = [
  "Read",
  "Write",
  "Edit",
  "Bash",
  "Glob",
  "Grep",
  "Task",
  "SendMessage",
  "mcp__synapse__create_task",
  "mcp__synapse__update_task",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__project_overview",
  "mcp__synapse__store_document",
  "mcp__synapse__update_document",
  "mcp__synapse__query_documents",
  "mcp__synapse__semantic_search",
  "mcp__synapse__link_documents",
  "mcp__synapse__index_codebase",
  "mcp__synapse__search_code",
  "mcp__synapse__get_index_status",
]

[agents.product-researcher]
model = "opus"
role_skills = ["brainstorming", "defining-requirements", "documentation"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__query_decisions",
  "mcp__synapse__check_precedent",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__project_overview",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
  "mcp__synapse__query_documents",
  "mcp__synapse__semantic_search",
]

[agents.researcher]
model = "sonnet"
role_skills = ["defining-requirements", "documentation"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "WebSearch",
  "WebFetch",
  "mcp__synapse__store_document",
  "mcp__synapse__update_document",
  "mcp__synapse__link_documents",
  "mcp__synapse__query_documents",
  "mcp__synapse__semantic_search",
  "mcp__synapse__search_code",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__check_precedent",
  "mcp__context7__resolve-library-id",
  "mcp__context7__query-docs",
]

[agents.architect]
model = "opus"
role_skills = ["testing-strategy", "architecture-design", "security", "brainstorming", "defining-requirements", "documentation"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "Task",
  "mcp__synapse__query_decisions",
  "mcp__synapse__check_precedent",
  "mcp__synapse__create_task",
  "mcp__synapse__update_task",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
  "mcp__synapse__query_documents",
]

[agents.architecture-auditor]
model = "opus"
role_skills = ["architecture-design", "security"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__query_decisions",
  "mcp__synapse__check_precedent",
  "mcp__synapse__query_documents",
  "mcp__synapse__store_decision",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
]

[agents.planner]
model = "opus"
role_skills = ["testing-strategy", "security", "brainstorming", "defining-requirements"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "Task",
  "mcp__synapse__create_task",
  "mcp__synapse__update_task",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
  "mcp__synapse__check_precedent",
  "mcp__synapse__query_decisions",
  "mcp__synapse__query_documents",
]

[agents.plan-auditor]
model = "opus"
role_skills = ["security"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__query_decisions",
  "mcp__synapse__check_precedent",
  "mcp__synapse__query_documents",
  "mcp__synapse__store_decision",
  "mcp__synapse__update_task",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
]

[agents.task-designer]
model = "opus"
role_skills = ["testing-strategy", "architecture-design", "security", "documentation"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__update_task",
  "mcp__synapse__query_decisions",
  "mcp__synapse__check_precedent",
  "mcp__synapse__query_documents",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
  "mcp__synapse__search_code",
]

[agents.task-auditor]
model = "sonnet"
role_skills = ["testing-strategy", "security"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__query_decisions",
  "mcp__synapse__check_precedent",
  "mcp__synapse__query_documents",
  "mcp__synapse__store_decision",
  "mcp__synapse__update_task",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
  "mcp__synapse__search_code",
]

[agents.executor]
model = "sonnet"
tier = 3
role_skills = ["testing-strategy", "security", "documentation"]
allowed_tools = [
  "Read",
  "Write",
  "Edit",
  "Bash",
  "Glob",
  "Grep",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__update_task",
  "mcp__synapse__store_decision",
  "mcp__synapse__check_precedent",
  "mcp__synapse__query_decisions",
  "mcp__synapse__search_code",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
]

[agents.validator]
model = "sonnet"
role_skills = ["testing-strategy", "security"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__query_decisions",
  "mcp__synapse__check_precedent",
  "mcp__synapse__update_task",
  "mcp__synapse__search_code",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
]

[agents.integration-checker]
model = "sonnet"
role_skills = ["testing-strategy", "security"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__query_decisions",
  "mcp__synapse__check_precedent",
  "mcp__synapse__update_task",
  "mcp__synapse__search_code",
  "mcp__synapse__get_index_status",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
]

[agents.debugger]
model = "sonnet"
role_skills = ["testing-strategy", "security"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "mcp__synapse__get_task_tree",
  "mcp__synapse__get_smart_context",
  "mcp__synapse__search_code",
  "mcp__synapse__query_decisions",
  "mcp__synapse__store_document",
  "mcp__synapse__link_documents",
]

[agents.codebase-analyst]
model = "sonnet"
role_skills = ["testing-strategy", "security"]
allowed_tools = [
  "Read",
  "Bash",
  "Glob",
  "Grep",
  "mcp__synapse__index_codebase",
  "mcp__synapse__get_index_status",
  "mcp__synapse__store_document",
  "mcp__synapse__update_document",
  "mcp__synapse__link_documents",
  "mcp__synapse__search_code",
  "mcp__synapse__get_smart_context",
]
```

Key changes from current:
- REMOVED: [agents.product-strategist], [agents.decomposer], [agents.plan-reviewer]
- ADDED: [agents.product-researcher], [agents.architecture-auditor], [agents.planner], [agents.plan-auditor], [agents.task-designer], [agents.task-auditor]
- MODIFIED: [agents.synapse-orchestrator] — removed store_decision, query_decisions, check_precedent, init_project from allowed_tools
- MODIFIED: [agents.architect] — removed store_decision, added query_documents

**Part B: Update trust.toml tier_authority**

Read trust.toml and update ONLY the `[tier_authority]` section. Keep all other sections (domains, approval, rpev.involvement, rpev.domain_overrides, rpev) unchanged.

New tier_authority:
```toml
[tier_authority]
synapse-orchestrator = []
product-researcher = []
architect = [1, 2]
architecture-auditor = []
planner = [2]
plan-auditor = []
task-designer = [2, 3]
task-auditor = []
executor = [3]
validator = []
integration-checker = []
researcher = []
debugger = []
codebase-analyst = []
```

Key changes:
- synapse-orchestrator: [0,1,2,3] -> [] (no decision authority)
- REMOVED: product-strategist, decomposer, plan-reviewer entries
- ADDED: product-researcher, architecture-auditor, planner, plan-auditor, task-designer, task-auditor
- architect = [1, 2] and executor = [3] UNCHANGED
  </action>
  <verify>
    <automated>grep -c 'product-strategist\|decomposer\b' packages/framework/config/agents.toml | awk '{print ($1 == 0) ? "PASS: no old agents" : "FAIL: old agents still present"}' && grep -c 'plan-auditor' packages/framework/config/agents.toml && grep 'synapse-orchestrator' packages/framework/config/trust.toml | grep -c '\[\]'</automated>
  </verify>
  <done>agents.toml has 14 agent entries (no old agents), trust.toml has updated tier_authority with orchestrator = [] and all new agents listed</done>
</task>

<task type="auto">
  <name>Task 2: Delete old agent files and update all references in workflows and commands</name>
  <files>packages/framework/workflows/pev-workflow.md, packages/framework/workflows/decision-protocol.md, packages/framework/workflows/research-decision-flow.md, packages/framework/commands/synapse/focus.md</files>
  <action>
**Part A: Delete old agent files**

Delete the three replaced agent files:
```bash
rm packages/framework/agents/product-strategist.md
rm packages/framework/agents/decomposer.md
rm packages/framework/agents/plan-reviewer.md
```

**Part B: Update workflow files**

Read each workflow file, then update old agent name references:

1. **packages/framework/workflows/pev-workflow.md** — Replace all occurrences:
   - "Decomposer" -> "Planner"
   - "decomposer" -> "planner"
   - "Plan Reviewer" -> "Plan Auditor"
   - "plan-reviewer" -> "plan-auditor"
   - "Plan-Reviewer" -> "Plan Auditor"
   - Add references to the new stages: after Plan Auditor approval, mention Task Designer + Task Auditor stage
   - Update the separation note at the bottom: "Planner and Plan Auditor are separate agents (Planner cannot review its own work)"

2. **packages/framework/workflows/decision-protocol.md** — Replace:
   - "product-strategist" -> "product-researcher" in the "Referenced by:" header
   - Note: The decision protocol still applies to architect. Product Researcher no longer uses it (no store_decision). Update the header to: "Referenced by: architect, planner, task-designer (doer agents that draft decisions)"

3. **packages/framework/workflows/research-decision-flow.md** — Replace:
   - "decomposer" -> "planner" in header and any agent-specific sections
   - "Decomposer" -> "Planner" in body text
   - Update the "Referenced by:" to include task-designer if appropriate

**Part C: Update slash commands**

Read each command file, then update:

1. **packages/framework/commands/synapse/focus.md** — Replace:
   - "Plan Reviewer" -> "Plan Auditor" (line ~116)
   - "Decomposer/Planner" -> "Planner" (line ~137)
   - Any other references to old agent names

2. **packages/framework/commands/synapse/refine.md** — Check for any references to "Product Strategist" and replace with "Product Researcher" if found. Based on the grep, refine.md does not currently reference old agent names by slug, but verify and update the conceptual references if any exist (e.g., "the strategist reviews..." -> "the Product Researcher analyzes...").

**Part D: Check for remaining references**

After all updates, run a final check:
```bash
grep -r 'product-strategist\|decomposer\|plan-reviewer' packages/framework/ --include='*.md' --include='*.toml' -l
```
The only results should be test files (if any). Agent files, configs, workflows, and commands should have zero hits.

Also check test files:
```bash
grep -r 'product-strategist\|decomposer\|plan-reviewer' packages/framework/test/ -l
```
If test files reference old agent names, update them too. The test files may reference agent slugs in their test data.
  </action>
  <verify>
    <automated>test ! -f packages/framework/agents/product-strategist.md && test ! -f packages/framework/agents/decomposer.md && test ! -f packages/framework/agents/plan-reviewer.md && grep -rl 'product-strategist\|decomposer\b' packages/framework/config/ packages/framework/workflows/ packages/framework/commands/ packages/framework/agents/ 2>/dev/null | wc -l | awk '{print ($1 == 0) ? "PASS: no old refs" : "FAIL: " $1 " files still have old refs"}'</automated>
  </verify>
  <done>Old agent files deleted (product-strategist.md, decomposer.md, plan-reviewer.md), all workflow and command references updated to new agent names, no stale references in config/workflows/commands/agents directories</done>
</task>

<task type="auto">
  <name>Task 3: Final structural verification and test run</name>
  <files></files>
  <action>
Run a comprehensive structural verification of the entire phase's output:

1. **Agent file count:** `ls packages/framework/agents/*.md | wc -l` should be 15 (14 agents + _synapse-protocol.md)

2. **mcpServers check:** `grep -L 'mcpServers' packages/framework/agents/*.md` — only _synapse-protocol.md should appear (it's the shared include, not an agent file). All 14 agent .md files must have mcpServers.

3. **agents.toml coverage:** Every .md file name (minus extension, minus underscore-prefixed) should have a matching [agents.{name}] entry in agents.toml. Verify:
```bash
for f in packages/framework/agents/*.md; do
  name=$(basename "$f" .md)
  [[ "$name" == _* ]] && continue
  grep -q "agents.$name" packages/framework/config/agents.toml || echo "MISSING: $name"
done
```

4. **trust.toml coverage:** Every agent slug in agents.toml should have a tier_authority entry in trust.toml. Verify:
```bash
grep '^\[agents\.' packages/framework/config/agents.toml | sed 's/\[agents\.\(.*\)\]/\1/' | while read name; do
  grep -q "^$name" packages/framework/config/trust.toml || echo "MISSING tier: $name"
done
```

5. **No old name references:** `grep -rl 'product-strategist\|decomposer\b\|plan-reviewer' packages/framework/ --include='*.md' --include='*.toml'` should return nothing (empty output).

6. **Full test suite:** `bun run test` — all tests must pass. If framework config tests reference old agent names, they should have been updated in Task 2.

7. **CLAUDE.md gateway check:** `grep -c 'Synapse Gateway Protocol' CLAUDE.md` returns 1.

Report any issues found. If tests fail due to old agent name references in test fixtures, fix the test data.
  </action>
  <verify>
    <automated>bun run test</automated>
  </verify>
  <done>Full structural verification passes: 15 agent .md files, all have mcpServers, agents.toml has 14 entries matching file names, trust.toml has tier_authority for all 14, no old name references, all tests green</done>
</task>

</tasks>

<verification>
1. Agent files: `ls packages/framework/agents/*.md | wc -l` = 15
2. Old files gone: `test ! -f packages/framework/agents/product-strategist.md` etc.
3. Config aligned: agents.toml has 14 [agents.*] sections, trust.toml has 14 tier_authority entries
4. No stale references: `grep -rl 'product-strategist\|decomposer\b\|plan-reviewer' packages/framework/ --include='*.md' --include='*.toml'` returns empty
5. Tests pass: `bun run test` exits 0
</verification>

<success_criteria>
- agents.toml registers all 14 agents with correct tool allowlists
- trust.toml tier_authority reflects new authority model (orchestrator = [])
- Old agent files (product-strategist, decomposer, plan-reviewer) are deleted
- All workflows and commands reference new agent names
- No stale references to old agent names anywhere in the framework
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-further-improvements-agentic-framework/26.1-05-SUMMARY.md`
</output>
