---
phase: 26.1-further-improvements-agentic-framework
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/framework/agents/synapse-orchestrator.md
autonomous: true
requirements: [FI-02]

must_haves:
  truths:
    - "Orchestrator prompt is reduced from ~700 lines to ~250-350 lines"
    - "Orchestrator does NOT make decisions (no store_decision calls)"
    - "Orchestrator does NOT handle failure retries — it reports failures to gateway"
    - "Orchestrator does NOT interact with the user directly"
    - "Orchestrator retains Pool Manager Protocol, wave execution, stage documents, handoff protocol"
    - "Orchestrator references new agent names (Planner not Decomposer, Plan Auditor not Plan-Reviewer, Product Researcher not Product Strategist)"
  artifacts:
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "Reduced-scope orchestrator as pure dispatcher"
      min_lines: 200
  key_links:
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "CLAUDE.md"
      via: "Orchestrator reports to gateway, does not handle user interaction"
      pattern: "report.*gateway|gateway.*decides"
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "packages/framework/agents/planner.md"
      via: "Spawns Planner instead of Decomposer"
      pattern: "planner|Planner"
---

<objective>
Rewrite the synapse-orchestrator.md from ~700 lines to ~250-350 lines by removing responsibilities that now belong to the gateway.

Purpose: The orchestrator is currently the largest and most complex agent prompt because it does everything — session management, user interaction, decision making, failure handling, and dispatch. Per the user's locked decision, the orchestrator becomes a pure dispatcher + stage tracker. The gateway (CLAUDE.md main session) takes over user communication, decision routing, and failure escalation.

Output: Significantly reduced synapse-orchestrator.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-further-improvements-agentic-framework/26.1-CONTEXT.md
@.planning/phases/26.1-further-improvements-agentic-framework/26.1-RESEARCH.md

@packages/framework/agents/synapse-orchestrator.md

<interfaces>
<!-- Current orchestrator is 701 lines. These sections STAY: -->
<!-- - Pool Manager Protocol (slot assignment, dispatch tick, queue management) -->
<!-- - Wave Execution Protocol (within each wave) -->
<!-- - RPEV Stage Document management (read/write stage docs) -->
<!-- - Subagent Handoff Protocol -->
<!-- - Checkpoint Format (emit to gateway, not to user directly) -->
<!-- - Statusline State File Protocol -->
<!-- - PR Workflow -->
<!-- - Tree-Integrity Check -->
<!-- - Rollback Protocol -->

<!-- These sections are REMOVED (gateway responsibility): -->
<!-- - Session Startup Protocol -->
<!-- - Work Stream Creation -->
<!-- - Involvement Matrix resolution (receives already-resolved mode from gateway) -->
<!-- - Failure Escalation Protocol (orchestrator reports failures, doesn't handle them) -->
<!-- - User interaction patterns -->
<!-- - Core Responsibilities #1 (session startup) and #4 (decision tracking) -->

<!-- Agent name changes to reference in prompt: -->
<!-- - "Decomposer" -> "Planner" -->
<!-- - "Plan Reviewer" -> "Plan Auditor" -->
<!-- - "Product Strategist" -> "Product Researcher" (not spawned by orchestrator) -->
<!-- - NEW: "Architecture Auditor", "Task Designer", "Task Auditor" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite synapse-orchestrator.md as pure dispatcher</name>
  <files>packages/framework/agents/synapse-orchestrator.md</files>
  <action>
Rewrite `packages/framework/agents/synapse-orchestrator.md` from scratch as a pure dispatcher + stage tracker. Read the current file first. Target: ~250-350 lines (down from 701).

**Frontmatter changes:**
- description: Update to "Pure dispatcher — assigns tasks to pool slots, manages RPEV stage transitions, reports completions/failures to gateway. Spawned by gateway for execution pipeline management."
- tools: REMOVE `mcp__synapse__store_decision`, `mcp__synapse__query_decisions`, `mcp__synapse__check_precedent`, `mcp__synapse__init_project`. The orchestrator no longer makes decisions or initializes projects.
- Keep: Task, SendMessage, Read, Write, Edit, Bash, Glob, Grep, and all other MCP tools (create_task, update_task, get_task_tree, get_smart_context, project_overview, store_document, update_document, query_documents, semantic_search, link_documents, index_codebase, search_code, get_index_status)
- model: opus (unchanged)
- mcpServers: ["synapse"] (unchanged, MANDATORY)

**Identity statement:** "You are the Synapse Orchestrator. You are a pure dispatcher and stage tracker, spawned by the gateway to manage the execution pipeline. You assign work to pool slots, manage RPEV stage transitions, and report completions and failures back to the gateway. You do NOT make decisions, interact with the user, or handle failure retries."

**Sections to KEEP (carry over from current, with updates):**
1. MCP Usage — update actor examples to remove store_decision, check_precedent. Remove decision tools from tool table.
2. Output Budget — keep exactly as-is (the terse templates)
3. Progressive Decomposition Protocol — update agent names: "Decomposer" -> "Planner", "Plan Reviewer" -> "Plan Auditor". Add new stages: after Plan Auditor passes, spawn Task Designer for leaf task specs, then Task Auditor for spec review. Also spawn Architecture Auditor after Architect completes (if gateway provided architectural work for the epic).
4. Research Document Discovery — keep, update "Decomposer" -> "Planner"
5. Wave Execution Protocol — keep exactly as-is (feature branches, executor dispatch, validator checks, integration checker)
6. PR Workflow — keep exactly as-is
7. Tree-Integrity Check — keep exactly as-is
8. Pool Manager Protocol — keep all of it (state doc, session start recovery, priority algorithm, dispatch loop, on task completion, token usage, anti-patterns)
9. Stage Document Management — keep exactly as-is
10. Stage Gate Check Protocol — keep exactly as-is
11. Subagent Handoff Protocol — keep exactly as-is
12. Checkpoint Format — keep, but change "emit to user" to "emit to gateway"
13. Statusline State File Protocol — keep exactly as-is
14. Rollback Protocol — keep exactly as-is
15. `{{include: _synapse-protocol.md}}` — keep at bottom

**Sections to REMOVE entirely:**
1. Session Startup Protocol — gateway responsibility
2. Work Stream Creation — gateway responsibility
3. Involvement Matrix section — orchestrator receives the resolved involvement mode from gateway in the handoff, does not resolve it itself. Remove the full involvement matrix explanation. Add a brief note: "The gateway resolves the involvement mode and includes it in the orchestrator handoff. Act on the mode directly — do not re-resolve."
4. Parallel Work Streams section — gateway manages this
5. Failure Escalation Protocol (Task-Level, Feature-Level, Epic-Level, Escalation Ladder) — replace with a brief "Failure Reporting" section: "When a task fails after executor retry: store failure context in stage document, set pending_approval: true, emit failure report to gateway. The gateway decides retry/debug/escalate. You do NOT decide what to do about failures."

**Core Responsibilities (rewrite to 5 items):**
1. Agent Dispatch: Route tasks to specialist agents via pool slots
2. Stage Tracking: Persist RPEV stage documents at every transition
3. Wave Management: Execute task waves within features, respecting pool limits
4. Failure Reporting: Report failures to gateway with diagnostic context
5. State Maintenance: Write pool-state and statusline state files

**Updated Progressive Decomposition Protocol — new pipeline order:**
The orchestrator now manages this expanded pipeline per the locked decisions:

For each epic received from gateway:
1. Spawn **Architect** to design implementation architecture (files, layers, interfaces, Tier 1-2 decision drafts)
2. Spawn **Architecture Auditor** to review architectural proposal and activate decision drafts
3. If Architecture Auditor rejects: respawn Architect with feedback (max 3 cycles)
4. Spawn **Planner** (was Decomposer) to create executable task tree from approved architecture
5. Spawn **Plan Auditor** (was Plan Reviewer) to verify task tree with 8-dimension analysis
6. If Plan Auditor rejects: respawn Planner with feedback (max 3 cycles)

For each feature when it becomes active (JIT):
7. Spawn **Task Designer** to write detailed task specs with mock code
8. Spawn **Task Auditor** to verify specs are execution-ready
9. If Task Auditor rejects: respawn Task Designer with feedback (max 3 cycles)
10. Proceed to wave execution (Executor + Validator pairs, unchanged)

**RPEV agent roles (updated list):**
- **Architect** — Designs implementation architecture; creates epic/feature structure; drafts Tier 1-2 decisions
- **Architecture Auditor** — Reviews architectural proposals; activates approved decision drafts
- **Planner** — Creates executable task tree from approved architecture (replaces Decomposer)
- **Plan Auditor** — Goal-backward verification of task tree; blocks deficient plans (replaces Plan Reviewer)
- **Task Designer** — Writes detailed task specifications with mock code, file paths, integration points
- **Task Auditor** — Verifies task specs are execution-ready; activates Tier 2-3 decision drafts
- **Executor** — Implements leaf tasks per spec
- **Validator** — Verifies implementation matches spec and decisions
- **Integration Checker** — Cross-task integration at feature/epic boundaries
- **Debugger** — Root-cause analysis on failures
- **Researcher** — Technical investigation (spawned by Architect or Planner when needed)
  </action>
  <verify>
    <automated>wc -l packages/framework/agents/synapse-orchestrator.md | awk '{print ($1 >= 200 && $1 <= 400) ? "PASS: " $1 " lines" : "FAIL: " $1 " lines (expected 200-400)"}'</automated>
  </verify>
  <done>synapse-orchestrator.md is rewritten as a pure dispatcher (~250-350 lines), with no decision-making, no user interaction, no failure handling — all routed to gateway. References new agent names (Planner, Plan Auditor, Architecture Auditor, Task Designer, Task Auditor). Pool Manager, wave execution, stage docs, handoff, PR workflow, rollback all preserved.</done>
</task>

</tasks>

<verification>
1. `wc -l packages/framework/agents/synapse-orchestrator.md` shows 200-400 lines (down from 701)
2. `grep -c 'store_decision' packages/framework/agents/synapse-orchestrator.md` returns 0 (no decision-making)
3. `grep -c 'Session Startup Protocol' packages/framework/agents/synapse-orchestrator.md` returns 0 (removed)
4. `grep -c 'Failure Escalation' packages/framework/agents/synapse-orchestrator.md` returns 0 (replaced with reporting)
5. `grep -c 'Pool Manager Protocol' packages/framework/agents/synapse-orchestrator.md` returns 1 (preserved)
6. `grep -c 'Planner' packages/framework/agents/synapse-orchestrator.md` returns at least 1 (new name)
7. `grep -c 'Decomposer' packages/framework/agents/synapse-orchestrator.md` returns 0 (old name gone)
8. `grep -c 'mcpServers' packages/framework/agents/synapse-orchestrator.md` returns 1 (preserved)
9. `bun run test:framework` passes
</verification>

<success_criteria>
- Orchestrator is a pure dispatcher: no decision tools, no user interaction, no failure handling
- Line count reduced by ~50%+ (from 701 to 200-400)
- All kept sections (pool manager, wave execution, stage docs, handoff, PR workflow, rollback) are preserved and functional
- New agent names used throughout (Planner, Plan Auditor, Architecture Auditor, Task Designer, Task Auditor)
- mcpServers: ["synapse"] present in frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-further-improvements-agentic-framework/26.1-02-SUMMARY.md`
</output>
