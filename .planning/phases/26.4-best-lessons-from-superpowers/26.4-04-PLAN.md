---
phase: 26.4-best-lessons-from-superpowers
plan: "04"
type: execute
wave: 3
depends_on: ["26.4-01", "26.4-03"]
files_modified:
  - packages/framework/agents/synapse-orchestrator.md
  - packages/framework/agents/validator.md
  - packages/framework/agents/executor.md
autonomous: true
requirements: [BEH-01, BEH-05, BEH-06]

must_haves:
  truths:
    - "Orchestrator mandates fresh agent per task in Pool Manager Protocol — explicitly forbids reusing agents across tasks"
    - "Orchestrator handoff includes full task spec text inline for work-package-level tasks, not just doc_id references"
    - "Orchestrator dispatches code-quality-reviewer after validator PASS, before proceeding to next wave"
    - "Orchestrator handles NEEDS_REVISION routing from code-quality-reviewer back to executor with retry limits"
    - "Validator loads spec and runs tests FIRST, forms independent assessment, THEN reads executor output document"
    - "Executor output contract requires exact verification commands run, full output, and pass/fail determination"
  artifacts:
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "Updated orchestrator with fresh-agent mandate, inline spec, code-quality-reviewer dispatch, NEEDS_REVISION routing"
      contains: "Fresh Agent Per Task"
    - path: "packages/framework/agents/validator.md"
      provides: "Updated validator with verify-first independence sequence"
      contains: "form independent assessment"
    - path: "packages/framework/agents/executor.md"
      provides: "Updated executor with verification commands in output contract"
      contains: "verification commands"
  key_links:
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "packages/framework/agents/code-quality-reviewer.md"
      via: "dispatch after validator PASS"
      pattern: "code-quality-reviewer"
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "packages/framework/config/trust.toml"
      via: "max_revision_retries controls NEEDS_REVISION cycle limits"
      pattern: "max_revision_retries"
    - from: "packages/framework/agents/validator.md"
      to: "executor output document"
      via: "reads executor report LAST, after own independent verification"
      pattern: "THEN read executor"
---

<objective>
Update the orchestrator with fresh-agent mandate, inline spec handoffs, and code-quality-reviewer dispatch. Update the validator for verify-first independence. Update the executor to require verification commands in its output contract.

Purpose: These are the core behavioral changes from Superpowers practices #3 (verification gate), #4 (two-stage review dispatch), #5 (controller-curated context), and #6 (fresh agent per task). Together they close the gap between pipeline structure and runtime behavior.

Output: Updated synapse-orchestrator.md, validator.md, executor.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.4-best-lessons-from-superpowers/26.4-CONTEXT.md
@.planning/phases/26.4-best-lessons-from-superpowers/26.4-RESEARCH.md
@.planning/phases/26.4-best-lessons-from-superpowers/26.4-01-SUMMARY.md
@.planning/phases/26.4-best-lessons-from-superpowers/26.4-03-SUMMARY.md
@packages/framework/agents/synapse-orchestrator.md
@packages/framework/agents/validator.md
@packages/framework/agents/executor.md
@packages/framework/agents/code-quality-reviewer.md
@packages/framework/config/trust.toml

<interfaces>
From packages/framework/agents/synapse-orchestrator.md (current Wave Execution Protocol):
```markdown
## Wave Execution Protocol
### Executing a Wave
1. For each task in the wave, spawn an Executor subagent via Task tool
2. Await all executor results before proceeding
3. For each completed task: spawn Validator subagent
4. If ANY validation fails: HALT the wave
5. If all validations pass: proceed to the next wave
6. After all feature waves complete: spawn Integration Checker
```

From packages/framework/agents/validator.md (current context loading):
```markdown
## Input Contract
(Steps 1-5 from _synapse-protocol.md context loading sequence)
```

From packages/framework/agents/executor.md (current Output Contract):
```markdown
## Output Contract
Must produce BEFORE reporting completion:
| Output | How | doc_id pattern | provides |
| Git commit(s) | git commit | n/a | n/a |
| Implementation summary | store_document | executor-implementation-{task_id} | implementation |
| Register output | update_task(output_doc_ids) | n/a | n/a |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update orchestrator with fresh-agent mandate, inline spec, and code-quality-reviewer dispatch</name>
  <files>packages/framework/agents/synapse-orchestrator.md</files>
  <action>
    Update `packages/framework/agents/synapse-orchestrator.md` with three additions:

    **Addition 1: Fresh Agent Per Task mandate**

    Add a new section `## Fresh Agent Per Task` after the Core Responsibilities section (before Progressive Decomposition Protocol). Content:

    ```markdown
    ## Fresh Agent Per Task

    Every task dispatch MUST use a fresh agent instance. When an agent completes a task, it reports results and terminates. The next task gets a new agent instance, even for the same role.

    Rules:
    - Do NOT reuse an executor agent across multiple tasks in the same wave
    - Do NOT pass accumulated context from one task's agent to the next
    - Each Task tool call creates an independent agent with only its SYNAPSE HANDOFF context
    - Continuity between tasks is managed by YOU (the orchestrator) via stage documents and pool state, not by carrying agent state
    ```

    **Addition 2: Controller-curated context (inline spec)**

    Update the Wave Execution Protocol section. In the executor dispatch step, change the handoff construction to include the full task spec text inline:

    Add a note after the SYNAPSE HANDOFF block instruction:
    ```markdown
    **Controller-Curated Context Rule:** For work-package-level tasks (depth=3), include the full task spec text inline in the Task prompt AFTER the SYNAPSE HANDOFF block. Do NOT rely solely on doc_id references for the primary task spec — the executor should not need to load its own spec. Reserve doc_id references for supplementary context the executor might need to look up.

    Format:
    ```
    <TASK_SPEC>
    {paste the full spec field content here}
    </TASK_SPEC>
    ```
    ```

    **Addition 3: Code-quality-reviewer dispatch after validator**

    Update the Wave Execution Protocol section. After step 3 (validator) and before step 4 (HALT on validation fail), insert a new step:

    ```markdown
    3b. If validator PASSES: spawn **Code Quality Reviewer** for the task
       - Include SYNAPSE HANDOFF block with task_id, project_id, hierarchy_level
       - Include executor's implementation summary doc_id in handoff doc_ids
       - Read code-quality-reviewer's output document status:
         - APPROVED: proceed to next task/wave
         - NEEDS_REVISION: route back to executor with reviewer feedback. Read max_revision_retries from trust.toml [rpev] section. Track retry count. If retries exhausted: emit failure report to gateway.
         - REJECTED: treat as validation failure — HALT wave and emit failure report to gateway
    ```

    Also update the RPEV Agent Roles list to add:
    ```markdown
    - **Code Quality Reviewer** — Reviews implementation quality (craftsmanship, security, performance) after validator confirms spec compliance
    ```

    Also update the Upstream Context Routing table (if it exists) with a code-quality-reviewer row.
  </action>
  <verify>
    <automated>bun -e "const c=require('fs').readFileSync('packages/framework/agents/synapse-orchestrator.md','utf8');let ok=true;['Fresh Agent Per Task','Controller-Curated Context','code-quality-reviewer','NEEDS_REVISION','max_revision_retries','TASK_SPEC','Code Quality Reviewer'].forEach(s=>{if(!c.includes(s)){console.error('Missing:',s);ok=false}});if(!ok)process.exit(1);console.log('All orchestrator updates present');"</automated>
  </verify>
  <done>
    - Fresh Agent Per Task section present with explicit rules
    - Controller-Curated Context Rule present with inline TASK_SPEC format
    - Code-quality-reviewer dispatch step present after validator PASS
    - NEEDS_REVISION routing with max_revision_retries cycle limit present
    - RPEV Agent Roles list includes Code Quality Reviewer
  </done>
</task>

<task type="auto">
  <name>Task 2: Update validator for verify-first independence and executor for verification gate</name>
  <files>packages/framework/agents/validator.md, packages/framework/agents/executor.md</files>
  <action>
    **Part A: Update validator.md for verify-first independence**

    This is the key behavioral change from Superpowers Practice #3 ("Do Not Trust the Report"). The validator MUST form its own verdict BEFORE consulting what the executor claimed.

    Update the validator's context loading and verification sequence. Find the Key Tool Sequences or Input Contract section and restructure to:

    ```markdown
    ## Validation Sequence (Independence Rule)

    You MUST form your own independent assessment BEFORE reading the executor's implementation summary. This prevents anchoring on the executor's self-report.

    **Step 1: Load task spec**
    - Parse SYNAPSE HANDOFF block
    - get_task_tree(root_task_id: task_id, max_depth: 0) — read spec, context_doc_ids
    - HALT if spec is null

    **Step 2: Run tests FIRST**
    - TDD Steps A-B: Check test file immutability (git diff), run tests, record pass/fail
    - This is your primary verdict input — tests passing or failing

    **Step 3: Read code independently**
    - Read the implementation files directly (from git diff or task spec file paths)
    - Check spec compliance: does the code match what the spec requires?
    - Form your own PASS/FAIL assessment with reasoning

    **Step 4: Form independent assessment**
    - Based on steps 2-3 ONLY, determine: PASS or FAIL
    - Write your assessment BEFORE proceeding to step 5

    **Step 5: THEN read executor output**
    - NOW read the executor's implementation summary from output_doc_ids
    - Compare your independent assessment to the executor's claims
    - Note any discrepancies (executor claims DONE but tests fail, executor flags concerns you didn't find, etc.)
    - Discrepancies do NOT override your independent verdict — they are noted for the orchestrator
    ```

    Make sure to add a clear warning:
    ```markdown
    **CRITICAL: Do NOT read the executor's implementation summary (output_doc_ids) before Step 5.** Reading it earlier anchors your assessment on the executor's claims. Your value is independent verification.
    ```

    **Part B: Update executor.md verification gate**

    Update the executor's Output Contract section to require verification evidence. Add after the existing output table:

    ```markdown
    ### Verification Evidence (Required)

    Your implementation summary document MUST include a `## Verification` section with:

    1. **Exact commands run** — The literal commands you executed to verify the implementation (e.g., `bun test --cwd packages/server`, `curl -X POST /api/auth/login`)
    2. **Full output** — The complete stdout/stderr from each verification command (not summarized, not truncated)
    3. **Pass/fail determination** — Your explicit assessment: PASS or FAIL for each command

    Red flags that indicate false completion claims (from Superpowers verification-before-completion skill):
    - Words like "should", "probably", "seems to", "likely" in your verification section
    - Verification section says "tests pass" without showing the actual test output
    - Verification commands are described but not actually run
    - Output is summarized instead of shown in full

    If you cannot run verification commands (e.g., no test file exists, command requires external service): state this explicitly in the Verification section with reason. Do NOT fabricate verification output.
    ```
  </action>
  <verify>
    <automated>bun -e "const fs=require('fs');const v=fs.readFileSync('packages/framework/agents/validator.md','utf8');const e=fs.readFileSync('packages/framework/agents/executor.md','utf8');let ok=true;['form independent assessment','THEN read executor','Do NOT read the executor'].forEach(s=>{if(!v.includes(s)){console.error('Validator missing:',s);ok=false}});['Verification Evidence','Exact commands run','Full output','should.*probably'].forEach(s=>{if(!e.includes(s.replace('.*',''))){console.error('Executor missing:',s);ok=false}});if(!ok)process.exit(1);console.log('Validator independence and executor verification gate present');"</automated>
  </verify>
  <done>
    - Validator has 5-step independence sequence: load spec, run tests, read code, form assessment, THEN read executor output
    - Validator has CRITICAL warning against reading executor report before Step 5
    - Executor output contract requires Verification Evidence section with exact commands, full output, and pass/fail
    - Executor has red flags list for false completion claims sourced from Superpowers
  </done>
</task>

</tasks>

<verification>
- Orchestrator has fresh-agent mandate, inline spec rule, and code-quality-reviewer dispatch
- Validator has verify-first independence sequence with CRITICAL warning
- Executor has verification evidence requirement in output contract
- `bun run test` — full suite still green
</verification>

<success_criteria>
- Fresh agent per task is an explicit mandate in the orchestrator
- Controller-curated context includes inline spec for work-package tasks
- Two-stage review pipeline is wired: validator PASS triggers code-quality-reviewer dispatch
- Validator forms independent verdict before reading executor report
- Executor must provide verification evidence (commands, output, pass/fail)
</success_criteria>

<output>
After completion, create `.planning/phases/26.4-best-lessons-from-superpowers/26.4-04-SUMMARY.md`
</output>
