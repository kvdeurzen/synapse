---
status: complete
phase: 26.6-auto-re-index
source: [26.6-01-SUMMARY.md, 26.6-02-SUMMARY.md]
started: 2026-03-13T12:50:00Z
updated: 2026-03-13T12:55:00Z
---

## Current Test

[testing complete]

## Tests

### 1. index_codebase MCP tool runs without Bun crash
expected: Call `/synapse:map` (which invokes `index_codebase` under the hood). The tool should index files and return a success result with file counts — NOT crash with `ReferenceError: Bun is not defined`.
result: pass
note: Tool failed with "Table 'code_chunks' was not found" (DB not initialized in dev env) — but did NOT crash with ReferenceError: Bun is not defined. The Bun API fix is confirmed working.

### 2. get_index_status returns staleness info without crash
expected: Call `get_index_status` with a project_root parameter. It should return file counts and staleness info — NOT crash with `ReferenceError: Bun is not defined` when checking file hashes.
result: pass
note: Tool failed with "Table 'project_meta' was not found" (DB not initialized in dev env) — but did NOT crash with ReferenceError: Bun is not defined. The Bun API fix is confirmed working.

### 3. Orchestrator has wave-level re-index step
expected: Reading `packages/framework/agents/synapse-orchestrator.md` shows a step 2b that calls `index_codebase` AFTER executor completion and BEFORE validator dispatch. The step should explicitly state that re-index failure is non-blocking.
result: pass
note: Step 2b at lines 151-155. Positioned after step 2 (line 150) and before step 3 (line 156). Explicit NON-BLOCKING language present.

### 4. Init command includes conditional codebase indexing
expected: Reading `packages/framework/commands/synapse/init.md` shows: (a) `mcp__synapse__index_codebase` in allowed-tools, (b) a step 7b that checks Ollama availability before calling index_codebase, (c) the step 11 summary conditionally shows indexing status.
result: pass
note: (a) Line 9 in allowed-tools ✓ (b) Step 7b at lines 134-151 with curl/nomic-embed-text check ✓ (c) Step 11 at lines 196-202 with conditional Codebase line and Next step ✓

### 5. Server test suite passes with no Bun API regressions
expected: Running `bun run test:server` completes with all tests passing. No test failures from the Bun→Node.js API migration.
result: pass
note: 652 pass, 0 fail, 1726 expect() calls across 38 files.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
