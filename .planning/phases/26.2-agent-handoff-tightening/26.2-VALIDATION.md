---
phase: 26.2
slug: agent-handoff-tightening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-10
---

# Phase 26.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Bun test (built-in) |
| **Config file** | none (bun test auto-discovers) |
| **Quick run command** | `bun test --cwd packages/server test/tools/create-task.test.ts` |
| **Full suite command** | `bun run test` |
| **Estimated runtime** | ~80 seconds (server), ~120 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run `bun run test:server`
- **After every plan wave:** Run `bun run test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 80 seconds

---

## Per-Task Verification Map

| Area | Behavior | Test Type | Automated Command | File Exists | Status |
|------|----------|-----------|-------------------|-------------|--------|
| Schema | tasks table has 23 fields | unit | `bun test --cwd packages/server test/db/schema.test.ts` | YES (update count) | ⬜ pending |
| create_task | new fields accepted, default null | unit | `bun test --cwd packages/server test/tools/create-task.test.ts` | YES (add tests) | ⬜ pending |
| update_task | spec/output_doc_ids updatable | unit | `bun test --cwd packages/server test/tools/update-task.test.ts` | YES (add tests) | ⬜ pending |
| get_task_tree | new fields appear in node output | unit | `bun test --cwd packages/server test/tools/get-task-tree.test.ts` | YES (add tests) | ⬜ pending |
| Agent roles | 14 roles valid | unit | `bun test --cwd packages/server test/tools/create-task.test.ts` | YES (add test) | ⬜ pending |
| Framework | Protocol + agent contracts readable | smoke | `bun run test:framework` | YES | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. New test cases added within existing test files.

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| Gateway protocol injected at session start | Requires Claude Code session | Start new session in test project, verify gateway-protocol.md content appears in additionalContext |
| Agent .md contract sections readable | Markdown structure | Grep for `## Input Contract` / `## Output Contract` in all agent files |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 80s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
