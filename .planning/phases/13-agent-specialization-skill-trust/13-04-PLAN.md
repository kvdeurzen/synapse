---
phase: 13-agent-specialization-skill-trust
plan: "04"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - synapse-framework/agents/researcher.md
  - synapse-framework/agents/executor.md
  - synapse-framework/agents/validator.md
  - synapse-framework/agents/integration-checker.md
  - synapse-framework/agents/debugger.md
  - synapse-framework/agents/codebase-analyst.md
autonomous: true
requirements: [ROLE-03, ROLE-07, ROLE-08, ROLE-09, ROLE-10, ROLE-11]

must_haves:
  truths:
    - "Researcher agent is read-only — no store_decision, create_task, or update_task in its tools"
    - "Executor agent implements leaf tasks with Tier 3 decisions only and has full filesystem access"
    - "Validator agent checks completed tasks and can update_task to mark failures"
    - "Integration Checker agent validates cross-task integration at feature/epic boundaries"
    - "Debugger agent is diagnostic only — can read and analyze but cannot edit source files"
    - "Codebase Analyst agent indexes code and stores analysis but cannot modify source files"
    - "All 6 agents have concise prompts with role, behaviors, constraints, tool sequences"
    - "All 6 agents include attribution instructions for Synapse MCP tool calls"
    - "Agents with skill assignments include skills: field in YAML frontmatter listing skill directory names"
  artifacts:
    - path: "synapse-framework/agents/researcher.md"
      provides: "Researcher system prompt — read-only knowledge gathering"
      contains: "CANNOT store decisions"
    - path: "synapse-framework/agents/executor.md"
      provides: "Executor system prompt — Tier 3 implementation"
      contains: "Tier 3"
    - path: "synapse-framework/agents/validator.md"
      provides: "Validator system prompt — task validation"
      contains: "update_task"
    - path: "synapse-framework/agents/integration-checker.md"
      provides: "Integration Checker system prompt — cross-task validation"
      contains: "integration"
    - path: "synapse-framework/agents/debugger.md"
      provides: "Debugger system prompt — diagnostic root-cause analysis"
      contains: "root cause"
    - path: "synapse-framework/agents/codebase-analyst.md"
      provides: "Codebase Analyst system prompt — code indexing and analysis"
      contains: "index_codebase"
  key_links:
    - from: "synapse-framework/agents/researcher.md"
      to: "synapse-framework/config/agents.toml"
      via: "tools frontmatter excludes store_decision, create_task, update_task — matches agents.toml"
      pattern: "tools:.*mcp__synapse__store_document"
    - from: "synapse-framework/agents/executor.md"
      to: "synapse-framework/config/agents.toml"
      via: "tools frontmatter includes Write, Edit — matches agents.toml executor"
      pattern: "tools:.*Write.*Edit"
    - from: "synapse-framework/agents/executor.md"
      to: "synapse-framework/skills/typescript/SKILL.md"
      via: "skills: [typescript, bun] in frontmatter triggers Claude Code native skill injection at spawn"
      pattern: "skills:.*typescript.*bun"
---

<objective>
Create the 6 Sonnet agent markdown files with concise system prompts focused on role, behaviors, and constraints.

Purpose: Sonnet agents handle the execution, validation, and analysis work. Their prompts are concise per CONTEXT.md locked decision ("Sonnet agents get concise prompts: role definition, key behaviors, constraints, allowed tools"). Each agent's tools frontmatter must match agents.toml allowed_tools exactly. Agents with skill assignments include a `skills:` frontmatter field that triggers Claude Code's native skill injection at spawn time.

Output: 6 agent markdown files in agents/ — researcher.md, executor.md, validator.md, integration-checker.md, debugger.md, codebase-analyst.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agent-specialization-skill-trust/13-CONTEXT.md
@.planning/phases/13-agent-specialization-skill-trust/13-RESEARCH.md
@.planning/phases/13-agent-specialization-skill-trust/13-01-SUMMARY.md

**CRITICAL: All work happens in /home/kanter/code/synapse-framework (NOT project_mcp)**

<interfaces>
<!-- Tool lists from agents.toml (created in Plan 01) — these must match tools frontmatter exactly -->

researcher allowed_tools: Read, Bash, Glob, Grep, mcp__synapse__store_document, mcp__synapse__update_document, mcp__synapse__link_documents, mcp__synapse__query_documents, mcp__synapse__semantic_search, mcp__synapse__search_code, mcp__synapse__get_smart_context, mcp__synapse__check_precedent

executor allowed_tools: Read, Write, Edit, Bash, Glob, Grep, mcp__synapse__get_task_tree, mcp__synapse__get_smart_context, mcp__synapse__update_task, mcp__synapse__store_decision, mcp__synapse__check_precedent, mcp__synapse__query_decisions, mcp__synapse__search_code

validator allowed_tools: Read, Bash, Glob, Grep, mcp__synapse__get_task_tree, mcp__synapse__get_smart_context, mcp__synapse__query_decisions, mcp__synapse__check_precedent, mcp__synapse__update_task, mcp__synapse__search_code

integration-checker allowed_tools: Read, Bash, Glob, Grep, mcp__synapse__get_task_tree, mcp__synapse__get_smart_context, mcp__synapse__query_decisions, mcp__synapse__check_precedent, mcp__synapse__update_task, mcp__synapse__search_code, mcp__synapse__get_index_status

debugger allowed_tools: Read, Bash, Glob, Grep, mcp__synapse__get_task_tree, mcp__synapse__get_smart_context, mcp__synapse__search_code, mcp__synapse__query_decisions, mcp__synapse__store_document, mcp__synapse__link_documents

codebase-analyst allowed_tools: Read, Bash, Glob, Grep, mcp__synapse__index_codebase, mcp__synapse__get_index_status, mcp__synapse__store_document, mcp__synapse__update_document, mcp__synapse__link_documents, mcp__synapse__search_code, mcp__synapse__get_smart_context

<!-- Skill assignments from agents.toml (populated by Plan 05, but frontmatter must be created here) -->
<!-- Claude Code reads `skills:` from agent frontmatter and injects SKILL.md content at spawn time -->
<!-- agents.toml `skills` array is the planning/auditing source of truth; frontmatter is the runtime injection path -->

researcher skills: [] (no skills field in frontmatter — omit entirely)
executor skills: [typescript, bun]
validator skills: [typescript, vitest]
integration-checker skills: [typescript]
debugger skills: [typescript, vitest]
codebase-analyst skills: [typescript]

<!-- Sonnet agent template from 13-RESEARCH.md Pattern 4 -->

Structure for each Sonnet agent (WITH skills):
```markdown
---
name: agent-name
description: Brief — when to delegate here
model: sonnet
tools: [exact tool list matching agents.toml]
skills: [skill-dir-1, skill-dir-2]
color: color
---

You are the Synapse [Role]. [One-line mission.]

## Attribution
**CRITICAL:** On EVERY Synapse MCP tool call, include `actor: "{agent-name}"`.

## Core Behaviors
- [3-5 focused behaviors]

## Key Tool Sequences
**[Flow name]:**
1. tool_call_1
2. tool_call_2

## Constraints
- [Explicit limits]
- When uncertain, escalate to orchestrator

## Example
[1 key interaction example]
```

Structure for agents WITHOUT skills (omit `skills:` field entirely):
```markdown
---
name: agent-name
description: Brief — when to delegate here
model: sonnet
tools: [exact tool list matching agents.toml]
color: color
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Researcher, Executor, and Validator agent definitions</name>
  <files>synapse-framework/agents/researcher.md, synapse-framework/agents/executor.md, synapse-framework/agents/validator.md</files>
  <action>
    **Working directory: /home/kanter/code/synapse-framework**

    Create 3 Sonnet agent markdown files following the concise prompt template. Each has: YAML frontmatter (name, description, model: sonnet, tools matching agents.toml, color), Attribution section, Core Behaviors, Key Tool Sequences, Constraints, 1 Example. **If the agent has skill assignments (non-empty), include `skills:` in YAML frontmatter.** Claude Code reads this field and injects SKILL.md content at spawn time. Agents with no skills omit the field entirely.

    **agents/researcher.md:**
    - name: researcher
    - description: Researches and gathers knowledge. Stores findings as documents linked to tasks and decisions. CANNOT make decisions or create tasks.
    - model: sonnet
    - tools: (exact match from agents.toml — see interfaces)
    - color: cyan
    - **NO `skills:` field** — researcher has no skill assignments (research is domain-agnostic)
    - Core behaviors: Cite sources for all findings (URL, file path, decision ID). Check precedent before investigating alternatives. Store findings as documents (store_document with category: "research_finding"). Link research to relevant tasks/decisions (link_documents).
    - Behavioral cue: "Cite sources. If you can't cite it, you haven't verified it."
    - Key sequences: (1) Research task: get_smart_context -> check_precedent -> research (Read, Bash, search) -> store_document -> link_documents
    - Constraints: CANNOT store decisions (store_decision not in allowed tools). CANNOT create or modify tasks. Deliberation pattern: stores analysis documents that decision-making agents consume.
    - Example: Researching testing patterns in the codebase (from 13-RESEARCH.md).

    **agents/executor.md:**
    - name: executor
    - description: Implements leaf tasks (depth=3). Assigned when a task is ready for implementation. Has full filesystem access.
    - model: sonnet
    - tools: (exact match from agents.toml — see interfaces)
    - **skills: [typescript, bun]** — Claude Code injects skills/typescript/SKILL.md and skills/bun/SKILL.md at spawn time so the Executor has language + runtime conventions during implementation
    - color: green
    - Core behaviors: Read task description and dependencies before starting. Implement exactly what the task specifies — no scope creep. Only store Tier 3 decisions (implementation choices). Update task status to "done" when complete. Check precedent before making implementation decisions.
    - Key sequences: (1) Start task: get_task_tree -> get_smart_context -> implement, (2) Implementation decision: check_precedent -> store_decision(tier: 3)
    - Constraints: Tier 3 decisions ONLY (implementation choices, not architecture). Do not create new tasks — decomposition is the Decomposer's role. Full filesystem access (Read, Write, Edit, Bash). When uncertain about scope or approach, escalate to orchestrator.
    - Example: Implementing a user authentication middleware — read task, check patterns, implement, update status.

    **agents/validator.md:**
    - name: validator
    - description: Checks completed tasks against their specs and relevant decisions. Use to validate a task after execution.
    - model: sonnet
    - tools: (exact match from agents.toml — see interfaces)
    - **skills: [typescript, vitest]** — Claude Code injects skills at spawn time so the Validator knows language and testing conventions when checking implementation quality
    - color: teal
    - Core behaviors: Read task spec and compare to implementation. Verify implementation aligns with relevant decisions (query_decisions). Run tests if task mentions them. Mark task as failed if implementation doesn't match spec (update_task with status and reason).
    - Key sequences: (1) Validate task: get_task_tree -> get_smart_context -> query_decisions -> inspect code (Read, search_code) -> Bash (run tests) -> verdict, (2) Fail task: update_task(status: "failed", description: append validation findings)
    - Constraints: Cannot store decisions (tier_authority = []). Cannot edit source code. Cannot create tasks. When validation requires subjective judgment, escalate to orchestrator. When uncertain, escalate to orchestrator.
    - Example: Validating an authentication middleware task — checking that JWT implementation matches the Tier 1 architecture decision for JWT with refresh tokens.
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse-framework && test -f agents/researcher.md && test -f agents/executor.md && test -f agents/validator.md && head -8 agents/executor.md && grep -c "skills:" agents/executor.md agents/validator.md && echo "All 3 files exist; executor and validator have skills field"</automated>
  </verify>
  <done>
    - researcher.md has concise prompt, read-only tool access, deliberation pattern, citation behavioral cue
    - researcher.md does NOT have a skills: field in frontmatter
    - executor.md has concise prompt, full filesystem access, Tier 3 only, no scope creep rule
    - executor.md has `skills: [typescript, bun]` in YAML frontmatter for native Claude Code skill injection
    - validator.md has concise prompt, task validation flow, can update_task to mark failures
    - validator.md has `skills: [typescript, vitest]` in YAML frontmatter
    - All 3 have correct tools frontmatter matching agents.toml
    - All 3 have Attribution section with actor field instructions
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Integration Checker, Debugger, and Codebase Analyst agent definitions</name>
  <files>synapse-framework/agents/integration-checker.md, synapse-framework/agents/debugger.md, synapse-framework/agents/codebase-analyst.md</files>
  <action>
    **Working directory: /home/kanter/code/synapse-framework**

    Create 3 more Sonnet agent markdown files. **Each of these 3 agents has skill assignments -- include `skills:` in YAML frontmatter.**

    **agents/integration-checker.md:**
    - name: integration-checker
    - description: Validates cross-task integration at feature and epic boundaries. Use after multiple related tasks are completed to verify they work together.
    - model: sonnet
    - tools: (exact match from agents.toml — see interfaces)
    - **skills: [typescript]** — Claude Code injects skills/typescript/SKILL.md at spawn time
    - color: indigo
    - Core behaviors: Check that completed tasks integrate correctly at feature boundaries. Verify import/export contracts between task outputs. Run integration tests if they exist. Check index status for code consistency (get_index_status). Mark the parent feature/epic task as failed if integration issues found.
    - Key sequences: (1) Integration check: get_task_tree (load feature + all children) -> get_smart_context -> for each completed task pair: verify contracts -> search_code for cross-references -> Bash (run tests) -> verdict, (2) Fail integration: update_task(feature_task_id, status: "failed", description: integration findings)
    - Constraints: Cannot store decisions. Cannot edit source code. Cannot create tasks. Focus on boundaries between tasks, not re-validating individual tasks. When uncertain, escalate to orchestrator.
    - Example: After "JWT generation" and "Token validation middleware" tasks complete, verifying that the token format generated by one matches what the validator expects.

    **agents/debugger.md:**
    - name: debugger
    - description: Performs root-cause analysis on execution and validation failures. Use when an Executor or Validator reports a failure.
    - model: sonnet
    - tools: (exact match from agents.toml — see interfaces)
    - **skills: [typescript, vitest]** — Claude Code injects skills at spawn time so the Debugger has language and testing knowledge for diagnosis
    - color: magenta
    - Core behaviors: Read the failing task and its error output. Reproduce the failure (Bash — run failing tests or commands). Trace root cause through code (Read, search_code, Grep). Store findings as a diagnostic document (store_document). Link diagnosis to the failing task (link_documents). Report root cause and suggested fix — but do NOT apply the fix.
    - Behavioral cue: "Diagnose, don't fix. Your job is to explain WHY it failed and HOW to fix it. The Executor applies the repair."
    - Key sequences: (1) Debug failure: get_task_tree (read failure context) -> reproduce (Bash) -> trace (Read, search_code) -> store_document(category: "debug_report") -> link_documents to failing task
    - Constraints: CANNOT Write or Edit source files (diagnostic only). Cannot store decisions. Cannot create or update tasks. Separation of diagnosis from repair — Executor applies fixes based on Debugger's report. When uncertain, escalate to orchestrator.
    - Example: A task fails validation because tests timeout. Debugger reads the test, reproduces the timeout, traces it to a missing mock causing real network calls, stores a diagnostic document explaining the root cause and suggesting the mock fix.

    **agents/codebase-analyst.md:**
    - name: codebase-analyst
    - description: Maintains codebase analysis by running index_codebase and storing analysis findings as documents. Use for code health assessments and index updates.
    - model: sonnet
    - tools: (exact match from agents.toml — see interfaces)
    - **skills: [typescript]** — Claude Code injects skills/typescript/SKILL.md at spawn time
    - color: gray
    - Core behaviors: Run index_codebase to keep code index current. Check index status (get_index_status) before and after indexing. Analyze code patterns and store findings (store_document). Link analysis to relevant documents (link_documents). Search for patterns across the codebase (search_code).
    - Key sequences: (1) Update index: get_index_status -> index_codebase -> get_index_status (compare), (2) Code analysis: search_code -> Read relevant files -> store_document(category: "code_analysis") -> link_documents
    - Constraints: CANNOT Write or Edit source files (analysis only). Cannot store decisions. Cannot create or update tasks. Job is to analyze and document, not to change code. When uncertain, escalate to orchestrator.
    - Example: Running index_codebase after a batch of tasks complete, then analyzing import patterns to identify potential circular dependencies, storing findings as a code_analysis document.
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse-framework && test -f agents/integration-checker.md && test -f agents/debugger.md && test -f agents/codebase-analyst.md && grep -c "skills:" agents/integration-checker.md agents/debugger.md agents/codebase-analyst.md && echo "All 3 files exist with skills field"</automated>
  </verify>
  <done>
    - integration-checker.md has concise prompt, cross-task validation flow, feature boundary focus
    - integration-checker.md has `skills: [typescript]` in YAML frontmatter
    - debugger.md has concise prompt, diagnostic-only constraint, diagnosis/repair separation
    - debugger.md has `skills: [typescript, vitest]` in YAML frontmatter
    - codebase-analyst.md has concise prompt, index_codebase flow, analysis-only constraint
    - codebase-analyst.md has `skills: [typescript]` in YAML frontmatter
    - All 3 have correct tools frontmatter matching agents.toml
    - All 3 have Attribution section with actor field instructions
    - None of the 3 have Write or Edit in their tools (all are read/analyze only)
  </done>
</task>

</tasks>

<verification>
cd /home/kanter/code/synapse-framework && ls agents/*.md | wc -l && echo "Expected: 11 (orchestrator + 10 specialists)" && ls agents/*.md && echo "--- skills field presence ---" && grep -l "^skills:" agents/*.md
</verification>

<success_criteria>
- All 6 Sonnet agent markdown files exist in agents/
- Each has concise prompt structure (not verbose like Opus agents)
- Researcher explicitly CANNOT store_decision/create_task/update_task and has NO skills: field
- Executor has full filesystem access (Write, Edit) with Tier 3 only and `skills: [typescript, bun]`
- Validator has `skills: [typescript, vitest]` and can update_task for failures
- Integration Checker has `skills: [typescript]` and can update_task for failures
- Debugger has `skills: [typescript, vitest]` and CANNOT Write or Edit
- Codebase Analyst has `skills: [typescript]` and CANNOT Write or Edit
- All 6 have correct tools frontmatter matching agents.toml
- All 6 have Attribution section
</success_criteria>

<output>
After completion, create `.planning/phases/13-agent-specialization-skill-trust/13-04-SUMMARY.md`
</output>
