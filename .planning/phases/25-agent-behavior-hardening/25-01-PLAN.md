---
phase: 25-agent-behavior-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/framework/agents/synapse-orchestrator.md
  - packages/framework/agents/executor.md
  - packages/framework/agents/decomposer.md
autonomous: true
requirements: [ABH-01, ABH-02, ABH-03]

must_haves:
  truths:
    - "RPEV stages have explicit gate checks -- orchestrator verifies stage document before proceeding to next stage"
    - "Orchestrator delegates task status bookkeeping to subagents -- executor marks its own tasks done, orchestrator only updates stage docs and pool state"
    - "Executors create atomic commits per task with task_id in message, orchestrator verifies commit exists before accepting completion"
    - "Parent tasks only marked done after get_task_tree confirms all children done"
    - "Plan stage spawns researcher before decomposer and plan_reviewer after decomposer"
    - "Orchestrator uses a terse progress template, not free-form narration"
  artifacts:
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "RPEV stage gates, delegate bookkeeping, git workflow, terse output, research+reviewer steps, re-index, plan document, pool state writes, context clearing"
      contains: "Stage Gate Check Protocol"
    - path: "packages/framework/agents/executor.md"
      provides: "Git commit protocol, actor attribution on every MCP call"
      contains: "Git Commit Protocol"
    - path: "packages/framework/agents/decomposer.md"
      provides: "store_document for plan rationale, actor attribution on every MCP call"
      contains: "store_document"
  key_links:
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "packages/framework/agents/executor.md"
      via: "orchestrator verifies executor's commit via git log --grep"
      pattern: "git log.*grep.*task"
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "mcp__synapse__get_task_tree"
      via: "tree-integrity check before parent completion"
      pattern: "children_all_done"
---

<objective>
Harden the orchestrator, executor, and decomposer agent prompts to enforce RPEV stage discipline, delegate bookkeeping, add git workflow, constrain output verbosity, and add missing research/review steps.

Purpose: The Phase 24 E2E run showed the orchestrator blends RPEV stages without gates, does too much bookkeeping, creates no branches/commits, narrates excessively, and skips the researcher and plan_reviewer. These 16 DEGRADED issues (Group A) are the highest-impact fixes.

Output: Three updated agent .md files with enforced protocols.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-agent-behavior-hardening/25-CONTEXT.md
@.planning/phases/25-agent-behavior-hardening/25-RESEARCH.md
@.planning/phases/24-e2e-validation/24-FAILURE-LOG.md

@packages/framework/agents/synapse-orchestrator.md
@packages/framework/agents/executor.md
@packages/framework/agents/decomposer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden synapse-orchestrator.md with stage gates, delegation, git workflow, terse output, and missing steps</name>
  <files>packages/framework/agents/synapse-orchestrator.md</files>
  <action>
Read the current orchestrator prompt (525 lines). Add the following NEW sections and modify existing sections. Target: stay under 800 lines total. Use compact, rule-based language (tables for protocols, max 5 lines per rule). Do NOT add narrative explanations -- the orchestrator is an LLM that follows rules, not a human that needs rationale.

**1. Add "Stage Gate Check Protocol" section (after "Stage Document Management"):**

```
## Stage Gate Check Protocol

Before EVERY stage transition (REFINING->PLANNING, PLANNING->EXECUTING, EXECUTING->VALIDATING, VALIDATING->DONE):

| Step | Action | On Failure |
|------|--------|------------|
| 1 | query_documents(category: "plan", tags: "\|rpev-stage\|") | HALT: "No stage documents found" |
| 2 | Find doc where content.task_id == current_task_id | HALT: "Stage doc missing for task {task_id}" |
| 3 | Verify content.stage == expected_current_stage | HALT: "Stage mismatch: expected {X}, found {Y}" |
| 4 | Write updated stage doc with new stage | Continue |
| 5 | Suggest /clear if transitioning between major stages | Continue |

Gate failures are NON-RECOVERABLE. Do not retry or work around a gate failure. Report and stop.
```

**2. Add "Output Budget" section (after Attribution):**

```
## Output Budget

You MUST use these fixed templates. No free-form narration.

**Per dispatch cycle (task assigned/completed):**
> [{STAGE}] {task_title} -- Slot {letter} | {done}/{total} tasks

**Per stage transition (max 5 lines):**
> Stage: {PREV} -> {NEXT} | Gate: PASSED
> {1-line summary of what happens next}
> Suggest: /clear for fresh context

**Wave checkpoint (after wave completes):**
Use the existing Checkpoint Format section -- that is your ONLY wave output.

**NEVER:** Narrate the remaining pipeline. Explain dependency graphs. Repeat what you will do next. Restate task descriptions.
```

**3. Modify "Wave Execution Protocol" -> "Executing a Wave" section:**

In "Before wave execution" add:
- "Create a feature branch: `git checkout -b feat/{epic_slug}/{feature_slug}` before dispatching any executors for this feature."

In "On Task Completion" subsection (already exists), ADD these items between the current step 1 and the existing pool-state update:
- Step 2 (new): "Commit verification: Run `git log --oneline --grep='task:{task_id}'` to verify executor committed. If no commit found, the task is NOT complete -- re-queue with note 'no commit found'."

**4. Modify "Core Responsibilities" -- replace item 5:**

Change "Agent Routing: Delegate to specialist agents..." to:
"Agent Routing: Delegate to specialist agents. You NEVER call update_task to set a leaf task's status to 'done' -- executors and validators own their own task status updates."

**5. Add to "Progressive Decomposition Protocol" -> "Epic -> Features" section:**

Before step 1 ("Spawn Decomposer"), insert:
"0. Spawn Researcher subagent via Task tool to gather domain knowledge relevant to the epic. Pass SYNAPSE HANDOFF with project_id and task_id. The researcher stores findings as documents -- these doc_ids feed into the Decomposer's handoff."

After step 2 ("Spawn Plan Reviewer"), add:
"2b. Spawn Plan Reviewer subagent to verify the Decomposer's feature list against stored decisions (from query_decisions). Plan Reviewer checks: completeness, testability, no circular deps, AND consistency with all DECIDED items from Refine."

Do the same for "Feature -> Tasks" section: insert researcher spawn before decomposer.

**6. Modify "Wave Execution Protocol" section -- add re-index step:**

After "After feature completion: Update stage document: stage: VALIDATING", add:
"Before validation: Run index_codebase(project_id: '{project_id}') to update code index with changes from execution."

**7. Modify "On Task Completion" section:**

Remove any existing step where the orchestrator calls `update_task(status: "done")` on a leaf task. The orchestrator should ONLY mark parent tasks (features, epics) as done, and ONLY after tree-integrity verification:

Add:
```
## Tree-Integrity Check (before marking ANY parent done)

1. get_task_tree(project_id: "{project_id}", root_task_id: "{parent_task_id}")
2. Verify: tree.rollup.children_all_done == true
3. If false: DO NOT mark parent done. List incomplete children and continue dispatching.
4. If true: update_task(task_id: "{parent_task_id}", status: "done", actor: "synapse-orchestrator")
```

**8. Strengthen "Pool Manager Protocol" -- add pool-state writes:**

In the Pool State Document section, change "should" language to "MUST":
"You MUST write the pool-state document on EVERY slot assignment change."

Add explicit trigger list:
"Write pool-state after: (a) assigning a task to a slot, (b) clearing a slot on task completion, (c) session start recovery, (d) cancellation via /synapse:focus."

**9. Add Decomposer plan document instruction:**

This goes in Plan 01 Task 2 (decomposer.md changes) -- see below.

**10. Add dependency resolution via update_task:**

In the "Dispatch Loop Pseudocode" section, before step 3:
"2.5. Dependency resolution: For each task where all dependencies are now 'done', call update_task(task_id: '{blocked_task_id}', is_blocked: false, actor: 'synapse-orchestrator'). Do not track unblocking in your own context -- update the task system."

**11. Context clearing:**

In the stage transition template (Output Budget section), the "/clear" suggestion is already included. No additional section needed.

**Attribution strengthening:**

In the Attribution section, add:
"Every MCP call you make MUST include `actor: 'synapse-orchestrator'`. Every Task tool call to a subagent MUST include the actor field in the SYNAPSE HANDOFF context so subagents can pass it through."

IMPORTANT: Keep all additions compact. Prefer tables over paragraphs. Prefer bullet lists over prose. The orchestrator prompt must stay under 800 lines.
  </action>
  <verify>
    <automated>wc -l packages/framework/agents/synapse-orchestrator.md | awk '{if ($1 > 800) print "FAIL: " $1 " lines (max 800)"; else print "PASS: " $1 " lines"}'</automated>
    Verify by grep that all key sections exist:
    - grep "Stage Gate Check Protocol" packages/framework/agents/synapse-orchestrator.md
    - grep "Output Budget" packages/framework/agents/synapse-orchestrator.md
    - grep "Tree-Integrity Check" packages/framework/agents/synapse-orchestrator.md
    - grep "git checkout -b feat/" packages/framework/agents/synapse-orchestrator.md
    - grep "git log --oneline --grep" packages/framework/agents/synapse-orchestrator.md
    - grep "Spawn Researcher" packages/framework/agents/synapse-orchestrator.md
    - grep "index_codebase" packages/framework/agents/synapse-orchestrator.md
    - grep "NEVER call update_task.*done.*leaf" packages/framework/agents/synapse-orchestrator.md
  </verify>
  <done>
    synapse-orchestrator.md contains: Stage Gate Check Protocol, Output Budget, Tree-Integrity Check, feature branch creation, commit verification, researcher before decomposer, plan_reviewer after decomposer, re-index before validation, delegate bookkeeping rule, pool-state MUST writes, dependency resolution via update_task, context clearing suggestion. Total under 800 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add git commit protocol to executor.md and plan document storage to decomposer.md</name>
  <files>packages/framework/agents/executor.md, packages/framework/agents/decomposer.md</files>
  <action>
**executor.md changes:**

Read the current executor.md. Add a new "Git Commit Protocol" section after "Core Behaviors" and before "Key Tool Sequences". Also update "Key Tool Sequences" to include the commit step.

Add section:
```
## Git Commit Protocol (MANDATORY)

After implementing the task and verifying tests pass, BEFORE storing the implementation summary:

1. Stage changed files: `git add {specific files changed}` (prefer explicit file list over `git add -A`)
2. Commit with task_id: `git commit -m "feat({task_title_slug}): {one-line summary} [task:{task_id}]"`
   - Use conventional commit format (feat/fix/refactor/test/docs)
   - Include [task:{task_id}] suffix for traceability
3. Verify commit: `git log --oneline -1` -- confirm the commit appears
4. Include the commit SHA in your implementation summary document (in the "## Files changed" section)

If `git add` or `git commit` fails: HALT. Report the error to orchestrator. Do not mark the task done without a commit.
```

Update "Key Tool Sequences" -> "Store Implementation Summary" to insert the git steps between implementation and store_document:

Change the sequence from:
```
1. store_document(...)
2. link_documents(...)
3. update_task(...)
```
To:
```
0. Git commit: git add {files} && git commit -m "feat({slug}): {summary} [task:{task_id}]"
1. store_document(... content includes commit SHA ...)
2. link_documents(...)
3. update_task(...)
```

Also update the Example section at the bottom to include the git commit step between step 7 (run tests) and step 8 (store_decision).

**Attribution strengthening in executor.md:**

Update the Attribution section to be more explicit:
```
## Attribution

**CRITICAL:** On EVERY Synapse MCP tool call, you MUST include `actor: "executor"` as a parameter. This is not optional. Calls without actor are logged as "unknown" and break audit attribution.

Examples:
- update_task(..., actor: "executor")
- store_document(..., actor: "executor")
- store_decision(..., actor: "executor")
- get_smart_context(..., actor: "executor")
- check_precedent(..., actor: "executor")
- search_code(..., actor: "executor")
```

**decomposer.md changes:**

Read the current decomposer.md. Add a "Plan Document Storage" step to the Decomposition Protocol, after Step 5 (Attach Context Refs) and before Step 6 (Respect Approval Mode):

```
### Step 5b: Store Plan Document

After decomposition is complete, store the plan rationale as a queryable document:

store_document(
  project_id: "{project_id}",
  doc_id: "plan-{parent_task_id}",
  title: "Plan: {parent_title} Decomposition",
  category: "plan",
  status: "active",
  tags: "|plan|decomposition|{level}|",
  content: "## Decomposition Rationale\n{why features were split this way}\n\n## Execution Order\n{wave assignments and dependency reasoning}\n\n## Effort Estimates\n{per-task rough size}\n\n## Key Risks\n{what might go wrong}",
  actor: "decomposer"
)
link_documents(
  project_id: "{project_id}",
  from_id: "plan-{parent_task_id}",
  to_id: "{parent_task_id}",
  relationship_type: "decomposes",
  actor: "decomposer"
)

This document is queryable by the orchestrator and plan_reviewer. It persists the plan rationale that would otherwise be lost as ephemeral terminal output.
```

Also add store_document and link_documents to the decomposer's tool list in frontmatter (currently not listed):
- Add `mcp__synapse__store_document` and `mcp__synapse__link_documents` to the tools line in the frontmatter.

**Attribution strengthening in decomposer.md:**

Update the Attribution section to match the executor pattern -- list every tool with explicit actor:
```
## Attribution

**CRITICAL:** On EVERY Synapse MCP tool call, you MUST include `actor: "decomposer"` as a parameter. This is not optional. Calls without actor are logged as "unknown" and break audit attribution.

Examples:
- create_task(..., actor: "decomposer")
- update_task(..., actor: "decomposer")
- store_decision(..., actor: "decomposer")
- store_document(..., actor: "decomposer")
- get_smart_context(..., actor: "decomposer")
- check_precedent(..., actor: "decomposer")
```
  </action>
  <verify>
    <automated>grep -c "Git Commit Protocol" packages/framework/agents/executor.md && grep -c "store_document.*plan-" packages/framework/agents/decomposer.md && grep -c "mcp__synapse__store_document" packages/framework/agents/decomposer.md</automated>
    Expected: 1, 1, 1 (or more). All three patterns present.
  </verify>
  <done>
    executor.md has Git Commit Protocol section with conventional commit format + task_id traceability. Key Tool Sequences updated with git step before store_document. decomposer.md has Plan Document Storage step storing rationale as category:"plan" document. decomposer.md frontmatter includes store_document and link_documents tools. Both agents have strengthened Attribution sections listing every tool with explicit actor.
  </done>
</task>

</tasks>

<verification>
1. `wc -l packages/framework/agents/synapse-orchestrator.md` -- under 800 lines
2. `grep -c "Stage Gate Check Protocol\|Output Budget\|Tree-Integrity Check\|Git Commit Protocol\|Plan Document Storage" packages/framework/agents/synapse-orchestrator.md packages/framework/agents/executor.md packages/framework/agents/decomposer.md` -- all 5 patterns found across the 3 files
3. `grep "actor:" packages/framework/agents/executor.md | wc -l` -- at least 6 (one per tool example)
4. `grep "actor:" packages/framework/agents/decomposer.md | wc -l` -- at least 6 (one per tool example)
5. No files outside `packages/framework/agents/` were modified
</verification>

<success_criteria>
- synapse-orchestrator.md enforces RPEV stage gates with explicit halt-on-failure
- Orchestrator no longer updates leaf task status -- delegation rule is explicit
- Feature branch creation happens before executor dispatch
- Executor commits per task with task_id; orchestrator verifies commits
- Plan stage includes researcher before decomposer and plan_reviewer after
- Re-index runs before Validate stage
- Decomposer stores plan rationale as a document
- Terse output budget limits orchestrator verbosity to fixed templates
- Tree-integrity check required before any parent task is marked done
- Pool-state document writes are mandatory (MUST, not should)
- All 3 agents have strengthened actor attribution on every MCP call
</success_criteria>

<output>
After completion, create `.planning/phases/25-agent-behavior-hardening/25-01-SUMMARY.md`
</output>
