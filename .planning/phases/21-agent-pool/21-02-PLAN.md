---
phase: 21-agent-pool
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - packages/framework/commands/synapse/status.md
  - packages/framework/commands/synapse/focus.md
autonomous: true
requirements: [POOL-03, POOL-04]
must_haves:
  truths:
    - "/synapse:status displays Agent Pool section with active agents, idle slots, and queued items"
    - "/synapse:status displays token usage aggregates on epic and feature lines"
    - "/synapse:focus agent A shows agent detail view with type, task, epic, running time, stage, and recent activity"
    - "/synapse:focus agent A offers Cancel action with requeue/skip choice"
    - "When no pool-state document exists, status shows a fallback message and focus shows 'no agent assigned'"
  artifacts:
    - path: "packages/framework/commands/synapse/status.md"
      provides: "Live Agent Pool section and token aggregates"
      contains: "pool-state"
    - path: "packages/framework/commands/synapse/focus.md"
      provides: "Agent-based focus with detail view and cancel action"
      contains: "agent [A-Z]"
  key_links:
    - from: "packages/framework/commands/synapse/status.md"
      to: "pool-state-[project_id] document"
      via: "query_documents with tags pool-state"
      pattern: "pool-state"
    - from: "packages/framework/commands/synapse/status.md"
      to: "task tree tags"
      via: "parse tokens_used from task tags for aggregation"
      pattern: "tokens_used"
    - from: "packages/framework/commands/synapse/focus.md"
      to: "pool-state-[project_id] document"
      via: "query_documents with tags pool-state for slot lookup"
      pattern: "pool-state"
    - from: "packages/framework/commands/synapse/focus.md"
      to: "update_task"
      via: "cancel action requeues (status ready) or skips (status done + skipped tag)"
      pattern: "update_task"
---

<objective>
Implement pool visibility in /synapse:status and agent interaction in /synapse:focus so users can see pool activity and interact with running agents.

Purpose: This plan makes the pool visible and controllable. Users see which agents are active, what they are working on, how many items are queued, and token usage per epic/feature. Users can focus on a specific agent slot to see details and cancel if needed.

Output: Updated status.md with live pool section + token aggregates, updated focus.md with agent A/B/C interaction.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-agent-pool/21-CONTEXT.md
@.planning/phases/21-agent-pool/21-RESEARCH.md
@.planning/phases/21-agent-pool/21-01-SUMMARY.md

@packages/framework/commands/synapse/status.md
@packages/framework/commands/synapse/focus.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

Pool state document schema (defined in orchestrator by Plan 01):
```json
{
  "project_id": "string",
  "max_slots": 3,
  "slots": {
    "A": {
      "task_id": "01HXYZ123ABC",
      "task_title": "JWT token refresh",
      "agent_type": "executor",
      "epic_title": "Auth System",
      "epic_id": "01HABC456DEF",
      "started_at": "2026-03-06T12:00:00.000Z",
      "rpev_stage": "EXECUTING",
      "recent_tool_calls": [
        {"tool": "Read", "arg": "src/auth/jwt.ts", "at": "2026-03-06T12:01:30.000Z"},
        {"tool": "Edit", "arg": "src/auth/jwt.ts", "at": "2026-03-06T12:02:10.000Z"}
      ]
    } | null,
    "B": null,
    "C": null
  },
  "queue": [
    {"task_id": "01HQ001", "task_title": "JWT logout", "epic_id": "01HABC456DEF", "type": "executor"}
  ],
  "tokens_by_task": {
    "01HDONE001": 48230,
    "01HDONE002": 12500
  },
  "last_updated": "2026-03-06T12:03:05.000Z"
}
```

Pool state document query pattern:
```
mcp__synapse__query_documents({
  project_id: "[project_id]",
  category: "plan",
  tags: "|pool-state|"
})
```

Token storage in task tags pattern:
```
|tokens_used=48230|
```
Parse with regex: /\|tokens_used=(\d+)\|/

From status.md (current Agent Pool stub, lines 79-81):
```markdown
### Agent Pool
[Phase 21 stub] Agent pool not yet active. When available, this section will show
active agents and their current tasks.
```

From focus.md (current deferred message, lines 157-158):
```markdown
**Deferred features:**
- **Agent-based focus** (`/synapse:focus agent C`) is deferred to the Agent Pool phase.
```

From focus.md allowed-tools (line 2-15):
```yaml
allowed-tools:
  - Read
  - mcp__synapse__get_task_tree
  - mcp__synapse__semantic_search
  - mcp__synapse__get_smart_context
  - mcp__synapse__check_precedent
  - mcp__synapse__store_document
  - mcp__synapse__store_decision
  - mcp__synapse__create_task
  - mcp__synapse__query_documents
  - mcp__synapse__update_task
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Agent Pool stub in status.md with live pool section and token aggregates</name>
  <files>packages/framework/commands/synapse/status.md</files>
  <action>
Replace the current Agent Pool stub section (lines 79-81 in the existing status.md) with a live pool state display. Also add token aggregate display to the epic/feature lines.

**Replace the Agent Pool stub with this new section in step 5 of the Process:**

After the "### Needs Your Input" section and before "### Recent Decisions", replace:
```
### Agent Pool
[Phase 21 stub] Agent pool not yet active. When available, this section will show
active agents and their current tasks.
```

With the following instructions:

```markdown
### Agent Pool

Query the pool-state document:
```
mcp__synapse__query_documents({
  project_id: "[project_id]",
  category: "plan",
  tags: "|pool-state|"
})
```

If a pool-state document is found, parse its JSON content and render:

```
### Agent Pool ({active_count}/{max_slots} active, {queue_length} queued)
- **A** [{agent_type}] {task_title} (Epic: {epic_title}) -- {minutes_running}m
- **B** [{agent_type}] {task_title} (Epic: {epic_title}) -- {minutes_running}m
- **C** idle
Queued ({queue_length}): {top 3 queue item titles}, +{remaining} more
```

Where:
- active_count = number of non-null slots
- For each non-null slot: show letter, agent_type in brackets, task_title, parent epic, and running time computed as (now - started_at) in minutes
- For each null slot: show "idle"
- Queue display: show count + first 3 queued item titles by priority order. If more than 3 items, append "+N more"
- If queue is empty, omit the "Queued" line

Use `/synapse:focus agent [letter]` to inspect an agent.

If no pool-state document exists:
```
### Agent Pool
Agent pool not yet active. The orchestrator will start the pool when work is dispatched.
```

**Add token aggregates to epic/feature display:**

In step 2 (Get task tree), after extracting epics and features, add token aggregation instructions:

For each task in the tree, check its `tags` field for the pattern `|tokens_used=N|` using regex `/\|tokens_used=(\d+)\|/`. Sum token counts:
- Per feature: sum all child task tokens
- Per epic: sum all child feature tokens

Format as `Xk tokens` (divide by 1000, round to nearest integer). Only show token count if > 0.

Update the epic/feature display format in step 5 to include token aggregates:

```
**Epic: {title}** [{STAGE}] ({percent}% complete){token_display}
  - Feature: {title} [{STATUS}]{token_display} -- {done}/{total} tasks done
```

Where `{token_display}` is:
- If total tokens > 0: ` -- {N}k tokens used`
- If total tokens = 0: omit entirely

Example:
```
**Epic: Auth System** [EXECUTING] (65% complete) -- 142k tokens used
  - Feature: Login flow [DONE] -- 48k tokens
  - Feature: JWT refresh [EXECUTING] -- 31k tokens (2/4 tasks done)
  - Feature: Session mgmt [QUEUED]
```
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "pool-state" packages/framework/commands/synapse/status.md && grep -c "tokens_used" packages/framework/commands/synapse/status.md && grep -c "Phase 21 stub" packages/framework/commands/synapse/status.md | grep "^0$" && grep -c "queued" packages/framework/commands/synapse/status.md</automated>
  </verify>
  <done>status.md queries pool-state document for live pool section, displays active agents with type/task/epic/time, shows queue count and top 3 items, aggregates token usage from task tags onto epic/feature lines, and has no remaining Phase 21 stub.</done>
</task>

<task type="auto">
  <name>Task 2: Add agent-based focus to focus.md with detail view and cancel action</name>
  <files>packages/framework/commands/synapse/focus.md</files>
  <action>
Add `agent [A-Z]` argument parsing to focus.md and implement the agent detail view with cancel action. Also remove the "deferred to Phase 21" message.

**Step 1 - Parse input (update existing):**

Add a third input mode to the existing parse step. The current step handles Name-based and Path-shorthand. Add:

```markdown
- **Agent-based**: `/synapse:focus agent A` or `/synapse:focus agent B`
  Detect: argument matches `/^agent\s+[A-Z]$/i`
  Action: Load pool-state document, find the slot matching the letter, display agent detail view (step 8)
```

Update the detection logic description: check agent-based FIRST (before name-based), since "agent A" would otherwise be treated as a name search.

**Step 8 (new) - Agent detail view:**

Add a new step 8 after the existing step 7 (pending approval handling):

```markdown
8. **Handle agent-based focus:** When the argument matches `/^agent\s+[A-Z]$/i`:

   a. Extract the slot letter (uppercase).

   b. Query the pool-state document:
      ```
      mcp__synapse__query_documents({
        project_id: "[project_id]",
        category: "plan",
        tags: "|pool-state|"
      })
      ```

   c. If no pool-state document found:
      ```
      Agent pool is not yet active. The orchestrator will start the pool when work is dispatched.
      Use `/synapse:status` for current project state.
      ```

   d. Parse the pool-state document JSON. Look up the requested slot letter in the `slots` map.

   e. If the slot is null (idle):
      ```
      ## Agent {letter}: idle

      No task currently assigned to this slot.
      Use `/synapse:status` to see overall pool activity.
      ```

   f. If the slot has an assigned task, render the detail view:
      ```
      ## Agent {letter}: [{agent_type}]

      **Task:** {task_title}
      **Epic:** {epic_title}
      **Running:** {minutes}m {seconds}s
      **Stage:** {rpev_stage}

      ### Recent Activity
      {For each entry in recent_tool_calls (last 3-5), newest first:}
      - {tool} {arg} ({time_ago} ago)

      {If recent_tool_calls is empty:}
      No activity recorded yet.

      ### Actions
      A) Cancel this agent
      B) Back to status (`/synapse:status`)
      ```

      Computing running time: subtract `started_at` from current time.
      Computing time_ago for tool calls: subtract `at` from current time.

   g. If the slot letter exceeds max_slots (e.g., user asks for Agent D when max_slots=3):
      ```
      Agent pool has {max_slots} slots (A through {last_letter}). Slot {letter} does not exist.
      Use `/synapse:status` to see active agents.
      ```

   h. **Cancel action flow:** If the user selects Cancel (option A):
      1. Confirm: "Cancel Agent {letter} running '{task_title}'? This will stop the agent."
      2. After user confirms, prompt: "What should happen to the task?"
         - **Requeue**: "Returning task to queue. It will be picked up by the next available slot."
           Call `mcp__synapse__update_task({ project_id, task_id: [slot.task_id], status: "ready", actor: "synapse-orchestrator" })`
           Note: the executor's git worktree is discarded automatically (isolation: "worktree" handles cleanup).
         - **Skip**: "Marking task as skipped."
           Call `mcp__synapse__update_task({ project_id, task_id: [slot.task_id], status: "done", tags: "[existing_tags]|skipped=true|", actor: "synapse-orchestrator" })`
      3. Update pool-state document: set the slot to null, remove from queue if present
         Call `mcp__synapse__store_document` with the updated pool-state JSON
      4. Confirm: "Agent {letter} cancelled. Task '{task_title}' has been [requeued/skipped]. The pool manager will fill the slot on next dispatch."
```

**Remove deferred message:**

Delete the entire "**Deferred features:**" paragraph near the end of the file (lines 157-158) that says agent-based focus is deferred to Phase 21. This is no longer deferred -- it is implemented.

**Do NOT change** the existing steps 1-7 (except adding agent-based detection in step 1), the allowed-tools list, the frontmatter, or the Attribution section.
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "agent.*A-Z" packages/framework/commands/synapse/focus.md && grep -c "pool-state" packages/framework/commands/synapse/focus.md && grep -c "Cancel this agent" packages/framework/commands/synapse/focus.md && grep -c "deferred to.*Agent Pool" packages/framework/commands/synapse/focus.md | grep "^0$" && grep -c "requeue" packages/framework/commands/synapse/focus.md</automated>
  </verify>
  <done>focus.md has agent-based argument detection, queries pool-state for slot lookup, renders agent detail view (type, task, epic, time, stage, recent activity), offers cancel with requeue/skip, and the Phase 21 deferral message is removed.</done>
</task>

</tasks>

<verification>
1. status.md contains "pool-state" query, token aggregation logic, live pool section format, and no "Phase 21 stub"
2. focus.md contains "agent [A-Z]" detection, pool-state query, agent detail view, cancel flow, and no "deferred to Agent Pool" message
3. Both commands reference the pool-state-[project_id] document schema defined in Plan 01
4. Token display format matches the user decision: "Nk tokens used" on completed nodes
5. Cancel flow matches the user decision: requeue or skip choice
</verification>

<success_criteria>
- /synapse:status has live Agent Pool section querying pool-state document (active agents, idle slots, queue count + top 3)
- /synapse:status shows token aggregates on epic/feature lines (Nk tokens format)
- /synapse:focus agent [A-Z] shows detail view with agent type, task, epic, running time, RPEV stage, recent tool calls
- /synapse:focus agent [A-Z] offers Cancel action with requeue/skip choice
- No "Phase 21 stub" or "deferred to Agent Pool" messages remain
</success_criteria>

<output>
After completion, create `.planning/phases/21-agent-pool/21-02-SUMMARY.md`
</output>
