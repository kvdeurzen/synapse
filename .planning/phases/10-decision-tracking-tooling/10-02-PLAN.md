---
phase: 10-decision-tracking-tooling
plan: 02
type: tdd
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/tools/query-decisions.ts
  - src/tools/check-precedent.ts
  - src/server.ts
  - test/tools/query-decisions.test.ts
  - test/tools/check-precedent.test.ts
autonomous: true
requirements:
  - DEC-03
  - DEC-04
  - DEC-08

must_haves:
  truths:
    - "An agent can query decisions by tier, status, subject, tags, and decision_type via query_decisions with results ranked by relevance"
    - "An agent can call check_precedent with a proposed decision and receive a has_precedent boolean plus matching decisions with similarity scores"
    - "check_precedent pre-filters by decision_type and uses 0.85+ similarity threshold"
    - "check_precedent returns top 5 matching decisions ranked by similarity"
    - "Superseded and revoked decisions are excluded from precedent results by default; include_inactive flag shows them"
  artifacts:
    - path: "src/tools/query-decisions.ts"
      provides: "queryDecisions core logic and registerQueryDecisionsTool MCP registration"
      exports: ["queryDecisions", "registerQueryDecisionsTool"]
    - path: "src/tools/check-precedent.ts"
      provides: "checkPrecedent core logic and registerCheckPrecedentTool MCP registration"
      exports: ["checkPrecedent", "registerCheckPrecedentTool"]
    - path: "test/tools/query-decisions.test.ts"
      provides: "TDD tests for query_decisions filtering and result shape"
      min_lines: 60
    - path: "test/tools/check-precedent.test.ts"
      provides: "TDD tests for check_precedent similarity matching and filtering"
      min_lines: 60
  key_links:
    - from: "src/tools/query-decisions.ts"
      to: "src/db/schema.ts"
      via: "imports DecisionRowSchema for validation"
      pattern: "import.*schema"
    - from: "src/tools/check-precedent.ts"
      to: "src/services/embedder.ts"
      via: "embed() call for proposed rationale vector"
      pattern: "embed\\("
    - from: "src/tools/check-precedent.ts"
      to: "src/db/schema.ts"
      via: "queries decisions table via LanceDB vector search"
      pattern: "decisions"
    - from: "src/server.ts"
      to: "src/tools/query-decisions.ts"
      via: "registerQueryDecisionsTool import and call"
      pattern: "registerQueryDecisionsTool"
    - from: "src/server.ts"
      to: "src/tools/check-precedent.ts"
      via: "registerCheckPrecedentTool import and call"
      pattern: "registerCheckPrecedentTool"
---

<objective>
Implement query_decisions and check_precedent MCP tools with TDD, completing the decision tracking toolset.

Purpose: query_decisions enables agents to browse/filter stored decisions. check_precedent enables agents to verify whether a proposed decision has existing precedent before storing it -- the "check before storing" workflow.

Output: Two working MCP tools: query_decisions (filter/browse decisions) and check_precedent (semantic precedent matching). All three decision tools registered in server.ts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-decision-tracking-tooling/10-CONTEXT.md
@.planning/phases/10-decision-tracking-tooling/10-01-SUMMARY.md

@src/db/schema.ts
@src/tools/store-decision.ts
@src/tools/decision-constants.ts
@src/tools/query-documents.ts
@src/tools/semantic-search.ts
@src/tools/search-utils.ts
@src/server.ts
@src/services/embedder.ts
@src/types.ts

<interfaces>
<!-- Key types and contracts from Plan 01 and existing codebase. -->

From src/tools/decision-constants.ts (created in Plan 01):
```typescript
export const VALID_TIERS = [0, 1, 2, 3] as const;
export const TIER_NAMES: Record<number, string> = {
  0: "product_strategy",
  1: "architecture",
  2: "functional_design",
  3: "execution",
};
export const VALID_DECISION_TYPES = ["architectural", "module", "pattern", "convention", "tooling"] as const;
export const VALID_DECISION_STATUSES = ["active", "superseded", "revoked"] as const;
```

From src/db/schema.ts (extended in Plan 01):
```typescript
export const DECISIONS_SCHEMA = new Schema([...]); // 17 fields including 768-dim vector
export const DecisionRowSchema = z.object({...}); // Zod validation
```

From src/tools/store-decision.ts (created in Plan 01):
```typescript
export interface StoreDecisionResult {
  decision_id: string;
  tier: number;
  tier_name: string;
  status: string;
  created_at: string;
  has_precedent: boolean;
}
export async function storeDecision(dbPath: string, projectId: string, args: StoreDecisionArgs, config: SynapseConfig): Promise<StoreDecisionResult>;
```

From src/tools/query-documents.ts (pattern to follow for query_decisions):
```typescript
// Uses connectDb, openTable, .query().where() pattern for filter predicates
// Returns array of results with metadata
```

From src/tools/semantic-search.ts (pattern for vector search in check_precedent):
```typescript
// Uses embed() to vectorize query, then table.search(vector).distanceType("cosine")
// Applies post-search filters and score normalization
```

From src/tools/search-utils.ts:
```typescript
export function normalizeVectorScore(distance: number): number; // cosine distance -> 0-1 similarity
export function buildSearchPredicate(projectId: string, filters: Record<string, unknown>): string;
```

From src/services/embedder.ts:
```typescript
export async function embed(texts: string[], projectId: string, config: SynapseConfig): Promise<number[][]>;
export function getOllamaStatus(): "ok" | "unreachable" | "model_missing";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement query_decisions tool with TDD</name>
  <files>src/tools/query-decisions.ts, test/tools/query-decisions.test.ts</files>
  <behavior>
    - queryDecisions with no filters returns all active decisions for the project, ordered by created_at descending
    - queryDecisions with tier filter (e.g., tier: 1) returns only tier-1 decisions
    - queryDecisions with status filter returns decisions matching that status
    - queryDecisions with decision_type filter returns only matching types
    - queryDecisions with subject filter does case-insensitive substring match on subject field
    - queryDecisions with tags filter matches pipe-separated tag substring
    - queryDecisions with multiple filters applies AND logic
    - queryDecisions with limit and offset supports pagination (default limit: 20, max: 100)
    - Each result includes: decision_id, subject, choice, context, rationale, tier, tier_name, decision_type, status, actor, phase, tags, superseded_by, created_at, updated_at
    - queryDecisions excludes superseded/revoked by default; include_inactive: true shows all statuses
  </behavior>
  <action>
    1. Create `test/tools/query-decisions.test.ts`:
       - Setup: tmpDir, beforeEach with initProject + store 3-5 test decisions (different tiers, types, statuses) using storeDecision from Plan 01
       - NOTE: Since storeDecision requires embedding (Ollama), tests should either:
         (a) Use _setFetchImpl to mock Ollama with fake 768-dim vectors, OR
         (b) Directly insert rows into the decisions table via insertBatch (bypassing embed). Prefer (b) for query tests since we're testing query logic, not embedding.
       - Test: "returns all active decisions with no filters"
       - Test: "filters by tier"
       - Test: "filters by decision_type"
       - Test: "filters by status"
       - Test: "filters by subject substring (case-insensitive)"
       - Test: "filters by tags"
       - Test: "excludes superseded/revoked by default"
       - Test: "includes inactive when include_inactive is true"
       - Test: "respects limit parameter"
       - Test: "returns correct result shape with all fields"

    2. Create `src/tools/query-decisions.ts` following the query-documents.ts pattern:
       - `QueryDecisionsInputSchema` Zod object:
         - project_id: string (slug regex)
         - tier: z.number().int().min(0).max(3).optional()
         - status: z.enum(VALID_DECISION_STATUSES).optional()
         - decision_type: z.enum(VALID_DECISION_TYPES).optional()
         - subject: z.string().optional() -- substring match
         - tags: z.string().optional() -- tag match
         - phase: z.string().optional()
         - include_inactive: z.boolean().optional().default(false)
         - limit: z.number().int().min(1).max(100).optional().default(20)
         - offset: z.number().int().min(0).optional().default(0)
       - `QueryDecisionsResult` interface: { results: DecisionSummary[], total: number }
       - `DecisionSummary` interface: all decision fields except vector
       - `queryDecisions(dbPath, projectId, args)` core function:
         a. Connect to DB, open decisions table
         b. Build WHERE predicate: start with `project_id = '{projectId}'`
         c. If !include_inactive, add `AND status = 'active'` (default behavior)
         d. If tier provided, add `AND tier = {tier}`
         e. If decision_type provided, add `AND decision_type = '{decision_type}'`
         f. If status provided (overrides include_inactive logic for explicit status filter), add `AND status = '{status}'`
         g. Execute query with .where(predicate).limit(limit + offset).toArray()
         h. Post-filter in JS: subject substring match (case-insensitive), tags match (LanceDB WHERE doesn't support LIKE well)
         i. Sort by created_at descending
         j. Apply offset/limit in JS
         k. Map results to DecisionSummary (strip vector field)
         l. Return { results, total }
       - `registerQueryDecisionsTool(server, config)` MCP registration with clear description

    IMPORTANT: LanceDB SQL WHERE support is limited. For subject substring and tags matching, fetch a broader result set and filter in JS (same pattern used in query-documents.ts). Keep the SQL predicate for indexed fields (project_id, tier, decision_type, status) and do string matching in JS.
  </action>
  <verify>
    <automated>bun test test/tools/query-decisions.test.ts</automated>
  </verify>
  <done>query_decisions MCP tool returns filtered decisions by tier, status, decision_type, subject, tags. Excludes inactive by default. Results include all decision fields except vector. Pagination via limit/offset works correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement check_precedent tool with TDD and wire all tools into server</name>
  <files>src/tools/check-precedent.ts, test/tools/check-precedent.test.ts, src/server.ts</files>
  <behavior>
    - checkPrecedent with a proposed decision that has no similar existing decisions returns { has_precedent: false, matches: [], proposed: { subject, decision_type, tier } }
    - checkPrecedent with a proposed decision similar to an existing one returns { has_precedent: true, matches: [{ decision_id, subject, choice, rationale, tier, tier_name, decision_type, status, similarity_score }], proposed: {...} }
    - checkPrecedent pre-filters by decision_type before vector search
    - checkPrecedent uses 0.85 fixed similarity threshold
    - checkPrecedent returns at most 5 matches, ranked by similarity descending
    - checkPrecedent excludes superseded/revoked decisions by default
    - checkPrecedent with include_inactive: true includes superseded/revoked with status label
    - checkPrecedent embeds the proposed rationale via embed service for vector comparison
    - checkPrecedent works when decisions table is empty (returns has_precedent: false)
    - All three decision tools (store_decision, query_decisions, check_precedent) are registered in server.ts
  </behavior>
  <action>
    1. Create `test/tools/check-precedent.test.ts`:
       - Setup: tmpDir, beforeEach with initProject
       - For tests requiring existing decisions with vectors: use _setFetchImpl from embedder.ts to mock Ollama returning consistent fake 768-dim vectors. Store decisions via storeDecision (which calls embed internally).
       - For the "similar" test: store a decision about "Use TypeScript for type safety", then check precedent with a proposed decision about "Adopt TypeScript for compile-time checks" -- same decision_type. The mock embedder should return vectors close enough (>0.85 cosine similarity) for these. ALTERNATIVE: Insert decisions directly with known vectors and test the vector search logic.
       - Test: "returns has_precedent false when table is empty"
       - Test: "returns has_precedent false when no decisions match decision_type"
       - Test: "returns has_precedent true with matching decisions above 0.85 threshold"
       - Test: "returns at most 5 matches"
       - Test: "excludes superseded decisions by default"
       - Test: "includes inactive decisions when include_inactive is true"
       - Test: "result shape includes similarity_score, decision_id, subject, choice"
       - Test: "pre-filters by decision_type before vector search"

    2. Create `src/tools/check-precedent.ts`:
       - `CheckPrecedentInputSchema` Zod object:
         - project_id: string (slug regex)
         - subject: z.string().min(1) -- what is being decided
         - rationale: z.string().min(1) -- justification for the proposed choice
         - decision_type: z.enum(VALID_DECISION_TYPES) -- for pre-filtering
         - tier: z.number().int().min(0).max(3).optional() -- informational, included in response
         - include_inactive: z.boolean().optional().default(false)
       - `PrecedentMatch` interface: { decision_id, subject, choice, rationale, tier, tier_name, decision_type, status, similarity_score, created_at }
       - `CheckPrecedentResult` interface: { has_precedent: boolean, matches: PrecedentMatch[], proposed: { subject, decision_type, tier } }
       - `checkPrecedent(dbPath, projectId, args, config)` core function:
         a. Validate input
         b. Connect to DB, open decisions table
         c. Check if table has any rows; if empty, return { has_precedent: false, matches: [], proposed }
         d. Embed the proposed rationale: `await embed([args.rationale], projectId, config)` -- if Ollama down, return graceful degradation (has_precedent: false with a warning, since this is a READ operation. Per existing pattern: read operations continue without embeddings. But check_precedent fundamentally needs embeddings to work. If Ollama is unreachable, catch OllamaUnreachableError and return { has_precedent: false, matches: [], proposed, warning: "Ollama unreachable -- cannot perform semantic precedent search" })
         e. Build WHERE predicate: `project_id = '{projectId}' AND decision_type = '{args.decision_type}'`
         f. If !include_inactive: add `AND status = 'active'`
         g. Execute vector search: `table.search(vector).distanceType("cosine").where(predicate).limit(5).toArray()`
         h. Convert cosine distance to similarity score using normalizeVectorScore from search-utils.ts (similarity = 1 - distance, or use the existing normalizer)
         i. Filter results to similarity >= 0.85
         j. Map to PrecedentMatch objects (include tier_name from TIER_NAMES lookup)
         k. Sort by similarity_score descending
         l. Return { has_precedent: matches.length > 0, matches, proposed: { subject: args.subject, decision_type: args.decision_type, tier: args.tier } }
       - `registerCheckPrecedentTool(server, config)` MCP registration

    3. Update `src/server.ts`:
       - Add imports for registerQueryDecisionsTool and registerCheckPrecedentTool
       - Add registration calls after registerStoreDecisionTool (already added in Plan 01)
       - Increment toolCount for each (total should be 21 tools: 18 existing + 3 new)

    IMPORTANT on vector search: LanceDB's `.search(vector)` requires the table to have at least one row with a non-null vector. Check for empty table first to avoid runtime errors. The `normalizeVectorScore` function in search-utils.ts converts LanceDB's cosine distance (0 = identical, 2 = opposite) to a 0-1 similarity score. The 0.85 threshold means cosine distance must be <= 0.15.
  </action>
  <verify>
    <automated>bun test test/tools/check-precedent.test.ts && bun test test/tools/store-decision.test.ts && bun test</automated>
  </verify>
  <done>check_precedent returns { has_precedent, matches, proposed } with correct 0.85 threshold. query_decisions and check_precedent are registered in server.ts alongside store_decision. Full test suite passes with no regressions. All 21 MCP tools are registered.</done>
</task>

</tasks>

<verification>
1. `bun test test/tools/query-decisions.test.ts` -- all query_decisions tests pass
2. `bun test test/tools/check-precedent.test.ts` -- all check_precedent tests pass
3. `bun test` -- full suite passes (no regressions from any phase)
4. `grep -c "registerTool" src/server.ts` -- should show 21 tool registrations (or grep for toolCount)
5. `grep -n "registerQueryDecisionsTool\|registerCheckPrecedentTool\|registerStoreDecisionTool" src/server.ts` -- all three imported and called
6. Phase 10 success criteria check:
   - store_decision: stores with tier, subject, choice, rationale, returns decision_id (DEC-01)
   - embed: rationale embedded as 768-dim vector (DEC-02)
   - query_decisions: filter by tier, status, subject, tags, decision_type (DEC-03)
   - check_precedent: has_precedent boolean + matches with similarity (DEC-04, DEC-08)
   - lifecycle: active -> superseded -> revoked (DEC-05)
   - init_project: creates decisions table (DEC-06)
   - activity_log: all mutations logged (DEC-07)
</verification>

<success_criteria>
- query_decisions filters by tier, status, decision_type, subject, tags correctly
- query_decisions excludes inactive by default, includes with flag
- check_precedent embeds proposed rationale and performs vector search
- check_precedent pre-filters by decision_type
- check_precedent uses fixed 0.85 similarity threshold
- check_precedent returns at most 5 matches with similarity scores
- check_precedent handles empty table and Ollama-down gracefully
- All three tools registered in server.ts (21 total)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-decision-tracking-tooling/10-02-SUMMARY.md`
</output>
