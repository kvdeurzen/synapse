---
phase: 26.6-auto-re-index
plan: 02
type: execute
wave: 2
depends_on: ["26.6-01"]
files_modified:
  - packages/framework/agents/synapse-orchestrator.md
  - packages/framework/commands/synapse/init.md
autonomous: true
requirements:
  - RIDX-03
  - RIDX-04

must_haves:
  truths:
    - "Orchestrator calls index_codebase between wave executor completion and validator dispatch"
    - "Wave-level re-index failure is non-blocking — validators proceed with stale index"
    - "/synapse:init indexes the codebase when Ollama is available — no separate /synapse:map needed"
    - "/synapse:init init completes successfully even when Ollama is unavailable — indexing is skipped with a user-visible note"
    - "Existing feature-completion re-index in orchestrator stays intact as belt-and-suspenders"
  artifacts:
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "Wave-level re-index step between executor completion and validator dispatch"
      contains: "index_codebase"
    - path: "packages/framework/commands/synapse/init.md"
      provides: "Conditional codebase indexing after init_project"
      contains: "index_codebase"
  key_links:
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "index_codebase MCP tool"
      via: "direct call between wave steps 2 and 3"
      pattern: "index_codebase.*project_id.*project_root.*actor"
    - from: "packages/framework/commands/synapse/init.md"
      to: "index_codebase MCP tool"
      via: "conditional call after init_project (step 7b)"
      pattern: "index_codebase"
    - from: "packages/framework/commands/synapse/init.md"
      to: "Ollama health check"
      via: "curl check before indexing"
      pattern: "nomic-embed-text"
---

<objective>
Wire `index_codebase` into the orchestrator wave execution loop and into `/synapse:init` so that (a) validators always search fresh code after executors make changes, and (b) new project setup is a single command without needing a separate `/synapse:map` step.

Purpose: Stale code index was identified as a DEGRADED issue in Phase 24 E2E failure log (#30). Validators see outdated code after executors change files. Also, requiring a separate `/synapse:map` after init adds friction to first-time setup.

Output: Two updated prompt files — orchestrator with wave-level re-index, init.md with conditional post-init indexing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.6-auto-re-index/26.6-CONTEXT.md
@.planning/phases/26.6-auto-re-index/26.6-RESEARCH.md
@.planning/phases/26.6-auto-re-index/26.6-01-SUMMARY.md

<interfaces>
<!-- Key insertion points the executor needs. Extracted from codebase. -->

From packages/framework/agents/synapse-orchestrator.md (wave execution, lines 150-162):
```markdown
2. Await all executor results before proceeding
3. For each completed task: spawn Validator subagent to check output against spec and decisions
   - Include test-contract doc_id (test-designer-test-contract-{task_id}) in validator's handoff doc_ids so validator can verify test immutability
3b. If validator PASSES: spawn **Code Quality Reviewer** for the task
```
The re-index step inserts between step 2 and step 3.

The existing feature-completion re-index at line 172:
```markdown
After feature completion:
- Run `index_codebase(project_id: '{project_id}')` to update code index
```
This stays in place as belt-and-suspenders.

From packages/framework/commands/synapse/init.md:
- Step 7 ends with DB registration via init_project
- Step 8 offers CLAUDE.md amendment
- Step 11 is the summary with "Next step: Run /synapse:map"
- allowed-tools frontmatter currently lists: Read, Write, Bash, mcp__synapse__init_project
- Anti-patterns section says "Do NOT check for Ollama during init" (refers to core setup flow; the new step 7b is post-setup and opt-in)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wave-level re-index step to orchestrator</name>
  <files>packages/framework/agents/synapse-orchestrator.md</files>
  <action>
Read `packages/framework/agents/synapse-orchestrator.md` and insert a new step 2b between step 2 ("Await all executor results before proceeding") and step 3 ("For each completed task: spawn Validator subagent").

Insert the following content after step 2:

```markdown
2b. **Re-index before validators:** After all wave executors complete, refresh the code index so validators and downstream agents search current code:
    `index_codebase(project_id: '{project_id}', project_root: '{project_root}', actor: "synapse-orchestrator")`
    - Log: "Re-indexing codebase after wave {N} executors..."
    - If `success: false`: emit a warning — "Re-index failed: {error}. Proceeding with validators on potentially stale index."
    - Re-index failure is NON-BLOCKING — validators proceed regardless. Do NOT halt the wave for indexing failures.
```

Keep the existing feature-completion re-index near line 172 ("After feature completion: Run index_codebase...") untouched — it stays as belt-and-suspenders.

Also add `index_codebase` to the orchestrator's tool references if there is a section listing orchestrator-used tools (check frontmatter or tool sections). The orchestrator already has MCP server access via mcpServers: ["synapse"], so the tool is accessible.

Use discretion for exact wording. The key constraints are:
- Position: AFTER wave executors complete, BEFORE validator dispatch
- Failure handling: warn and continue, never block
- Actor attribution: "synapse-orchestrator"
  </action>
  <verify>
    <automated>grep -n "index_codebase" /home/kanter/code/synapse/packages/framework/agents/synapse-orchestrator.md | head -10</automated>
  </verify>
  <done>synapse-orchestrator.md has index_codebase call between step 2 (await executors) and step 3 (spawn validators); existing feature-completion re-index unchanged; failure is non-blocking</done>
</task>

<task type="auto">
  <name>Task 2: Add conditional index_codebase to init.md after init_project</name>
  <files>packages/framework/commands/synapse/init.md</files>
  <action>
Read `packages/framework/commands/synapse/init.md` and make three changes:

**Change 1 — Add `mcp__synapse__index_codebase` to allowed-tools frontmatter:**

The current frontmatter has:
```yaml
allowed-tools:
  - Read
  - Write
  - Bash
  - mcp__synapse__init_project
```

Add `mcp__synapse__index_codebase` to the list. Without this, the tool call will fail silently in the command.

**Change 2 — Insert step 7b after step 7 (DB registration):**

After step 7 ("Register with Synapse DB") and before step 8 ("Offer CLAUDE.md amendment"), insert:

```markdown
7b. **Index codebase (conditional):** After init_project succeeds, silently check if Ollama is available with the required model:

    ```bash
    curl -sf http://localhost:11434/api/tags | grep -q "nomic-embed-text" && echo "ready" || echo "unavailable"
    ```

    If `ready`:
    - Inform user: "Indexing your codebase... (this may take a minute)"
    - Call `mcp__synapse__index_codebase` with:
      - `project_id`: the confirmed project_id
      - `project_root`: absolute path to the project root (derived from `.synapse/config/project.toml` location)
      - `actor`: "synapse-orchestrator"
    - On success: report "Codebase indexed: {files_indexed} files."
    - On failure: warn "Codebase indexing skipped — run /synapse:map when Ollama is available." (non-fatal — continue init)

    If `unavailable`:
    - Skip silently. The step 11 summary will note: "Codebase: not indexed — run /synapse:map to enable semantic search."
```

**Change 3 — Update step 11 summary:**

In the summary template (step 11), change the "Next step" line. Currently it says:
```
Next step: Run /synapse:map to index your codebase for semantic search.
```

Replace with conditional output:
- If indexing succeeded in step 7b: Remove the "Next step: Run /synapse:map" line entirely (or change to "Next step: Run /synapse:refine to start planning work.")
- If indexing was skipped (Ollama unavailable or failed): Keep "Codebase: not indexed — run /synapse:map when Ollama is available."

Add a `Codebase:` line to the summary template showing the indexing result:
```
Codebase: {{N files indexed | not indexed — run /synapse:map when Ollama is available}}
```

**Change 4 — Update anti-patterns section:**

The current anti-pattern says "Do NOT check for Ollama during init". This referred to the core setup flow. Update the anti-pattern to clarify the distinction:

```markdown
- Do NOT check for Ollama during the core init flow (steps 1-7) — only the optional indexing step (7b) checks Ollama, and it is non-blocking
```
  </action>
  <verify>
    <automated>grep -n "index_codebase\|nomic-embed-text\|mcp__synapse__index_codebase" /home/kanter/code/synapse/packages/framework/commands/synapse/init.md | head -10</automated>
  </verify>
  <done>init.md has index_codebase in allowed-tools; step 7b conditionally indexes after init_project; step 11 summary reflects indexing status; anti-patterns updated to clarify Ollama check scope</done>
</task>

</tasks>

<verification>
1. `grep "index_codebase" packages/framework/agents/synapse-orchestrator.md` shows at least 2 matches (wave-level + feature-completion)
2. `grep "index_codebase" packages/framework/commands/synapse/init.md` shows matches for both allowed-tools and step 7b
3. `grep "mcp__synapse__index_codebase" packages/framework/commands/synapse/init.md` shows the tool in allowed-tools
4. The orchestrator's wave re-index step appears BEFORE the validator dispatch step
5. The init.md indexing is gated on Ollama availability and is non-blocking on failure
</verification>

<success_criteria>
- Orchestrator has a wave-level re-index step between executor completion and validator dispatch
- Re-index failure in orchestrator is non-blocking (warn and continue)
- init.md calls index_codebase conditionally when Ollama is available
- init.md completes successfully even without Ollama — indexing is skipped gracefully
- init.md has mcp__synapse__index_codebase in allowed-tools frontmatter
- Existing feature-completion re-index in orchestrator is preserved
- Step 11 summary in init.md reflects whether indexing occurred
</success_criteria>

<output>
After completion, create `.planning/phases/26.6-auto-re-index/26.6-02-SUMMARY.md`
</output>
