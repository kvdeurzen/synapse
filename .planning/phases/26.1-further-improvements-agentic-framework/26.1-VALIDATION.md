---
phase: 26.1
slug: further-improvements-agentic-framework
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-10
---

# Phase 26.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Bun test (built-in) |
| **Config file** | none — runs via `bun test` |
| **Quick run command** | `bun run test:framework` |
| **Full suite command** | `bun run test` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `bun run test:framework`
- **After every plan wave:** Run `bun run test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

This phase produces only markdown and TOML files — no executable code changes. Validation is structural/behavioral.

| Behavior | Test Type | Automated Command | Status |
|----------|-----------|-------------------|--------|
| All new agent files have `mcpServers: ["synapse"]` | structural | `grep -l 'mcpServers' packages/framework/agents/*.md` | ⬜ pending |
| agents.toml has entries for all new slugs, no old slugs | structural | TOML parse check | ⬜ pending |
| trust.toml tier_authority updated for all agents | structural | TOML parse check | ⬜ pending |
| CLAUDE.md contains gateway protocol section | structural | `grep -c 'Synapse Gateway Protocol' CLAUDE.md` | ⬜ pending |
| Old agent files deleted (product-strategist, decomposer, plan-reviewer) | structural | `ls` check | ⬜ pending |
| New agent file count matches expected roster (14 agents) | structural | `ls` count | ⬜ pending |
| Framework tests still pass after config changes | automated | `bun run test:framework` | ⬜ pending |
| Full test suite green | automated | `bun run test` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test framework installation or test stub creation needed — this phase produces markdown and TOML files, not executable code.

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| Agent prompt quality and role separation | Prompt content is subjective | Review each agent .md for correct role boundaries, no scope creep |
| Gateway protocol correctness in CLAUDE.md | Natural language protocol | Verify gateway owns user communication, orchestrator is dispatcher-only |
| Doer+reviewer pair completeness | Cross-file structural review | Each pipeline stage has exactly one doer and one reviewer agent |
| Decision draft→activate flow documented | Cross-agent protocol | Verify doer agents draft decisions, reviewer agents activate them |
| _synapse-protocol.md compatibility | Integration check | Verify shared protocol still works with new agent roles |

---

## Validation Sign-Off

- [ ] All tasks have structural verification commands
- [ ] Framework tests pass after config updates
- [ ] Full test suite green at phase gate
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
