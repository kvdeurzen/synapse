---
phase: 22-install-script
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/user-journey.md
autonomous: true
requirements:
  - INST-04

must_haves:
  truths:
    - "A user reading docs/user-journey.md can follow the complete path from install to running their first RPEV workflow"
    - "The document covers install.sh usage including flags, global vs local mode, and re-install behavior"
    - "All 5 slash commands are documented with purpose, usage examples, and expected output"
    - "Configuration options (project.toml, trust.toml, agents.toml) are explained with practical examples"
    - "Troubleshooting section covers common issues (Ollama not running, smoke test failure, hook path errors)"
  artifacts:
    - path: "docs/user-journey.md"
      provides: "Complete usage manual — install, configure, commands, configuration, troubleshooting"
      min_lines: 200
  key_links:
    - from: "docs/user-journey.md"
      to: "install.sh"
      via: "install documentation references install.sh flags and behavior"
      pattern: "install\\.sh"
    - from: "docs/user-journey.md"
      to: ".synapse/config/"
      via: "configuration section documents all config files"
      pattern: "project\\.toml|trust\\.toml|agents\\.toml"
---

<objective>
Update docs/user-journey.md into a comprehensive usage manual covering installation, all commands, configuration, and troubleshooting.

Purpose: INST-04 requires a usage manual documenting the complete user journey, commands reference, and configuration options. The existing user-journey.md has the right structure but needs expansion to cover install.sh usage, configuration details, and troubleshooting.

Output: Updated docs/user-journey.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-install-script/22-CONTEXT.md
@.planning/phases/22-install-script/22-RESEARCH.md

@docs/user-journey.md
@packages/framework/config/trust.toml
@packages/framework/config/agents.toml
@packages/framework/config/synapse.toml
@packages/framework/commands/synapse/init.md
@packages/framework/commands/synapse/map.md
@packages/framework/commands/synapse/refine.md
@packages/framework/commands/synapse/status.md
@packages/framework/commands/synapse/focus.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand docs/user-journey.md into comprehensive usage manual</name>
  <files>docs/user-journey.md</files>
  <action>
Update docs/user-journey.md to be a comprehensive usage manual. The existing document has a good skeleton — preserve its structure and voice but expand significantly.

**Sections to add/expand:**

1. **Title and intro** — Change subtitle to "Complete Guide — Installation, Commands, Configuration, and Troubleshooting". Brief paragraph: Synapse gives agents context for any task without wasting tokens.

2. **Prerequisites** — Keep existing content. Add version notes: Bun (any version), Ollama (any version, must be running for map + smoke test), Claude Code (latest).

3. **Installation** (NEW — major expansion):
   - `curl -fsSL https://raw.githubusercontent.com/kvdeurzen/synapse/main/install.sh | bash` one-liner
   - What install.sh does (high-level: checks prereqs, copies files, generates config, runs smoke test)
   - Install modes:
     - `--local` (default): installs into current project only
     - `--global`: installs to `~/.synapse/` as shared source, then wires into current project
   - Flags reference: `--quiet`, `--smoke-test`, `--version TAG`, `--help`
   - Re-install/update behavior: prompts before overwriting, preserves config customizations
   - What gets created (directory listing):
     ```
     .claude/
       agents/          # 11 agent definitions
       hooks/           # 7 hook scripts + lib/
       commands/synapse/ # 5 slash commands
       skills/          # 18 skill definitions
       server/          # Synapse MCP server
       settings.json    # Hook configuration (gitignored)
     .synapse/
       config/          # Project configuration (committed)
     .mcp.json          # MCP server entry (gitignored)
     ```
   - Note: `.claude/settings.json` and `.mcp.json` are gitignored — each developer runs install.sh

4. **Steps 1-7** — Keep existing content mostly as-is. Update:
   - Step 1 (Install): reference the new Installation section
   - Remove the "Note: install.sh is part of Phase 17" caveat — it's real now
   - Step 6 (status): add mention of agent pool section showing active agents
   - Step 7 (focus): note the three addressing modes more prominently

5. **Configuration Reference** (NEW section):
   - **project.toml** — Created by `/synapse:init`. Fields: project_id, name, skills (array of active skill names), created_at. Example with explanation.
   - **trust.toml** — RPEV involvement matrix. Explain the 4 hierarchy levels (project, epic, feature, work_package) x 4 RPEV stages (refine, plan, execute, validate). Values: drives, co-pilot, reviews, monitors, autopilot. Include the default matrix. Explain `max_pool_slots` for agent pool. Explain domain overrides.
   - **agents.toml** — Agent role configuration. Explain role_skills assignment. Brief description of the 11 agents and their roles.
   - **synapse.toml** — Server configuration: db_path, ollama_url, embed_model, log_level.

6. **Command Reference** (expand existing table):
   - For each of the 5 commands, provide:
     - Purpose (one sentence)
     - Syntax and arguments
     - What it does (3-5 bullet points)
     - Example session (brief)
   - Commands: `/synapse:init`, `/synapse:map`, `/synapse:refine`, `/synapse:status`, `/synapse:focus`

7. **Agent Pool** (NEW section):
   - What it is: configurable slots for parallel agent execution
   - How to configure: `max_pool_slots` in trust.toml
   - How it works: auto-assignment by priority, finish-first policy
   - Viewing pool state: `/synapse:status` shows active agents, `/synapse:focus agent C` shows agent detail

8. **Troubleshooting** (NEW section):
   - "Ollama not running" — start Ollama, run `install.sh --smoke-test` to verify
   - "Smoke test failed" — check Ollama is running, `nomic-embed-text` is pulled, run `install.sh --smoke-test`
   - "Hooks not firing" — check `.claude/settings.json` exists, hook paths use `$CLAUDE_PROJECT_DIR` prefix, bun is on PATH
   - "MCP server not connecting" — check `.mcp.json` exists, server path is correct, bun is on PATH
   - "Agent doesn't know project context" — run `/synapse:init` if not done, check `project.toml` exists
   - "Skills not loading" — check `skills` array in `project.toml` matches directory names in `.claude/skills/`

9. **Ongoing Use** — Keep existing content. Remove the "Some features are planned for future phases" caveat — agent pool is shipped.

10. **Two Starting Paths** — Keep existing content, update to reference install.sh properly.

**Style guidelines:**
- Keep the existing document's voice: clear, practical, second-person
- Use code blocks for commands and file contents
- Use bullet points for lists
- No emojis in the document
- Aim for ~250-350 lines total
  </action>
  <verify>
    <automated>wc -l docs/user-journey.md && grep -c '##' docs/user-journey.md && grep -q 'install.sh' docs/user-journey.md && grep -q 'trust.toml' docs/user-journey.md && grep -q 'Troubleshooting' docs/user-journey.md && echo "All sections present"</automated>
  </verify>
  <done>
docs/user-journey.md is a comprehensive usage manual with 200+ lines covering: installation (with flags and modes), all 5 commands (with examples), configuration reference (project.toml, trust.toml, agents.toml, synapse.toml), agent pool, and troubleshooting. The document is practical and actionable — a new user can follow it from zero to a working setup.
  </done>
</task>

</tasks>

<verification>
- `grep -c '##' docs/user-journey.md` shows 10+ sections
- `grep 'install.sh' docs/user-journey.md` shows install script references
- `grep 'trust.toml' docs/user-journey.md` shows config references
- `grep 'Troubleshooting' docs/user-journey.md` shows troubleshooting section
- `wc -l docs/user-journey.md` shows 200+ lines
</verification>

<success_criteria>
- docs/user-journey.md covers the complete user journey from install to ongoing RPEV use
- All 5 slash commands are documented with purpose, usage, and examples
- Configuration section documents all 4 config files with field explanations
- Troubleshooting section covers the 6 most common issues
- install.sh flags and modes are documented
</success_criteria>

<output>
After completion, create `.planning/phases/22-install-script/22-02-SUMMARY.md`
</output>
