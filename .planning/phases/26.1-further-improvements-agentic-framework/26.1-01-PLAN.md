---
phase: 26.1-further-improvements-agentic-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - packages/framework/agents/_synapse-protocol.md
  - packages/framework/workflows/decision-draft-flow.md
autonomous: true
requirements: [FI-01, FI-04]

must_haves:
  truths:
    - "CLAUDE.md contains a Synapse Gateway Protocol section that establishes the main session as the gateway"
    - "The gateway protocol specifies the gateway owns ALL user communication"
    - "The gateway protocol defines subagent spawning rules (Product Researcher for refinement, Orchestrator for dispatch)"
    - "The gateway protocol defines failure routing (retry, debug, ask user)"
    - "A decision draft workflow document exists describing the document-based draft->activate pattern"
    - "_synapse-protocol.md references the draft->activate flow for decision-making agents"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Gateway protocol section"
      contains: "Synapse Gateway Protocol"
    - path: "packages/framework/workflows/decision-draft-flow.md"
      provides: "Draft->review->activate decision convention"
      contains: "decision-draft"
    - path: "packages/framework/agents/_synapse-protocol.md"
      provides: "Updated shared protocol with draft decision reference"
      contains: "decision-draft"
  key_links:
    - from: "CLAUDE.md"
      to: "packages/framework/agents/synapse-orchestrator.md"
      via: "Gateway spawns Orchestrator as subagent"
      pattern: "Orchestrator"
    - from: "packages/framework/workflows/decision-draft-flow.md"
      to: "packages/framework/agents/_synapse-protocol.md"
      via: "Protocol references draft flow"
      pattern: "decision-draft-flow"
---

<objective>
Establish the gateway protocol in CLAUDE.md and the decision draft->activate convention that all subsequent agent rewrites depend on.

Purpose: These two foundational artifacts (gateway protocol + decision draft flow) define the new pipeline's boundaries. Every agent rewrite in Plans 02-04 references these conventions. By establishing them first, subsequent plans have clear contracts to implement against.

Output: Updated CLAUDE.md with gateway section, new decision-draft-flow.md workflow, updated _synapse-protocol.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-further-improvements-agentic-framework/26.1-CONTEXT.md
@.planning/phases/26.1-further-improvements-agentic-framework/26.1-RESEARCH.md

@CLAUDE.md
@packages/framework/agents/_synapse-protocol.md
@packages/framework/workflows/decision-protocol.md

<interfaces>
<!-- Key constraint: store_decision only supports ["active", "superseded", "revoked"] statuses -->
<!-- There is NO "draft" status and NO update_decision tool -->
<!-- The decision draft flow MUST use the document-based workaround pattern -->

From packages/server/src/tools/decision-constants.ts:
```typescript
export const VALID_DECISION_STATUSES = ["active", "superseded", "revoked"] as const;
```

From packages/server/src/tools/store-decision.ts:
```typescript
status: z
  .enum(VALID_DECISION_STATUSES)
  .optional()
  .default("active")
  .describe("Decision lifecycle status (default: active)"),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Synapse Gateway Protocol to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Add a new `## Synapse Gateway Protocol` section to CLAUDE.md after the existing content. This section establishes the main Claude session as the Synapse gateway agent. Per the user's locked decisions:

The section must include:

1. **Identity statement:** "You are the Synapse Gateway -- the sole point of contact with the user for all Synapse work."

2. **Gateway responsibilities:**
   - Capture user scope and run refinement sessions (spawn Product Researcher as subagent via Task tool)
   - Present architectural proposals and decision drafts requiring Tier 0-1 user approval
   - Delegate execution dispatch to the Orchestrator subagent
   - Receive completion/failure reports from the Orchestrator and decide next action
   - NEVER allow a subagent to interact directly with the user

3. **Subagent spawning rules:**
   - Gateway spawns Product Researcher for scope research during refinement
   - Gateway spawns Orchestrator to dispatch the execution pipeline (Architect->Planner->Executor stages)
   - All other agents are spawned BY the Orchestrator, not by the gateway directly
   - Exception: Gateway can spawn Debugger directly for failure diagnosis before retrying

4. **Failure routing protocol:** When the Orchestrator reports a failure:
   - Retry: spawn a new Orchestrator with the failure context in the handoff
   - Debug: spawn Debugger directly to diagnose before retrying
   - Ask user: surface the failure and options, wait for user decision

5. **Decision presentation:** When subagents produce decision drafts that require Tier 0-1 user approval:
   - Read the draft decision document from Synapse
   - Present it to the user with rationale and alternatives
   - On user approval: instruct the relevant reviewer agent to activate it
   - On user rejection: relay feedback to the proposing agent

6. **Session continuity:** The gateway protocol applies to all sessions, including resumed ones. On session start, check for in-flight orchestrator work via pool-state document and surface it.

Keep the existing CLAUDE.md content (Bun instructions, monorepo structure, testing, key paths) unchanged above the new section. The gateway protocol is additive.
  </action>
  <verify>
    <automated>grep -c 'Synapse Gateway Protocol' CLAUDE.md && grep -c 'sole point of contact' CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md contains a complete Synapse Gateway Protocol section that establishes the main session as gateway, defines spawning rules (Product Researcher + Orchestrator only), and specifies failure routing</done>
</task>

<task type="auto">
  <name>Task 2: Create decision-draft-flow.md workflow and update _synapse-protocol.md</name>
  <files>packages/framework/workflows/decision-draft-flow.md, packages/framework/agents/_synapse-protocol.md</files>
  <action>
**Part A: Create decision-draft-flow.md**

Create `packages/framework/workflows/decision-draft-flow.md` -- a new workflow document that defines the draft->review->activate convention for decisions. This document is referenced by doer and reviewer agents.

CRITICAL CONSTRAINT: The MCP server's `store_decision` tool does NOT support a "draft" status. Valid statuses are only: `["active", "superseded", "revoked"]`. There is no `update_decision` tool. Therefore, the draft flow MUST use the document-based workaround:

Content structure:

```markdown
# Decision Draft Flow

Referenced by: doer agents (architect, planner, task-designer), reviewer agents (architecture-auditor, plan-auditor, task-auditor)

## Principle

No decision becomes active without passing through a reviewer. Doer agents propose decisions as draft documents. Reviewer agents verify quality, then store the real decision via store_decision (which defaults to active status).

## Why Document-Based Drafts

The store_decision MCP tool only supports ["active", "superseded", "revoked"] statuses. There is no "draft" status. Therefore:
- Doers store decision proposals as documents (not decisions)
- Reviewers promote approved proposals to real decisions via store_decision

## Doer Protocol (proposing a decision)

1. Check precedent: `check_precedent(description: "{topic}")` -- verify no conflicting active decision exists
2. Store as draft document:
   ```
   store_document(
     doc_id: "decision-draft-{slug}",
     category: "decision_draft",
     title: "DRAFT: {decision title}",
     content: JSON with: { tier, subject, choice, context, rationale, proposed_by, decision_type, tags },
     actor: "{doer-agent-slug}"
   )
   ```
3. Report draft to orchestrator/gateway: "Stored decision draft: decision-draft-{slug}. Needs reviewer activation."
4. NEVER call store_decision directly. All decisions go through the reviewer gate.

Exception: Tier 3 (execution) decisions during implementation -- executors can store Tier 3 decisions directly as active (reviewer validates post-hoc). This is unchanged from the existing pattern.

## Reviewer Protocol (activating a decision)

1. Read the draft document: `query_documents(category: "decision_draft")`
2. Verify:
   - Rationale quality: clear alternatives considered, trade-offs documented
   - Precedent conflicts: `check_precedent` against the draft's subject
   - Tier correctness: draft tier matches the agent's authority
   - Scope: decision addresses the right level of concern
3. If APPROVED:
   - Call `store_decision(...)` with all fields from the draft document, adding `actor: "{reviewer-slug}"`
   - Delete or mark the draft document as consumed: `store_document(doc_id: "decision-draft-{slug}", status: "archived", ...)`
   - Report: "Decision activated: {title}"
4. If REJECTED:
   - Store rejection document: `store_document(doc_id: "rejection-decision-draft-{slug}", category: "decision_draft", content: "Rejection reason...")`
   - Report: "Decision draft rejected: {reason}"

## Tier-Based Routing

| Decision Tier | Proposed By | Reviewed By | User Approval |
|---------------|-------------|-------------|---------------|
| 0 (product) | Gateway captures from user | N/A -- user IS the decision-maker | Always |
| 1 (architecture) | Architect | Architecture Auditor | Per involvement mode (Tier 0-1) |
| 2 (functional) | Planner or Task Designer | Plan Auditor or Task Auditor | Per involvement mode |
| 3 (execution) | Executor (direct active) | Validator (post-hoc) | Never |

## Anti-Patterns

- Calling store_decision(status: "draft") -- this status does not exist; the tool defaults to "active"
- Doer calling store_decision directly (except Tier 3 executors)
- Reviewer creating new decisions instead of activating drafts
- Skipping check_precedent before proposing
```

**Part B: Update _synapse-protocol.md**

Add a new subsection "### Decision Draft Convention" after the existing "### Level-Aware Behavior" section. Content:

```markdown
### Decision Draft Convention

Decisions follow a draft->review->activate flow. See `@packages/framework/workflows/decision-draft-flow.md` for the full protocol.

- **Doer agents** (architect, planner, task-designer): Store decision proposals as documents (`category: "decision_draft"`), NEVER call `store_decision` directly (except Tier 3 executors).
- **Reviewer agents** (architecture-auditor, plan-auditor, task-auditor): Verify draft quality and call `store_decision` to activate approved proposals.
- **Tier 3 exception:** Executors store Tier 3 decisions directly as active. Validators check post-hoc.
```

Keep all existing _synapse-protocol.md content (Attribution, Principles, Error Handling, Level-Aware Behavior) unchanged.
  </action>
  <verify>
    <automated>test -f packages/framework/workflows/decision-draft-flow.md && grep -c 'decision_draft' packages/framework/workflows/decision-draft-flow.md && grep -c 'Decision Draft Convention' packages/framework/agents/_synapse-protocol.md</automated>
  </verify>
  <done>decision-draft-flow.md exists with document-based workaround (no "draft" status in store_decision), _synapse-protocol.md has Decision Draft Convention subsection referencing the workflow</done>
</task>

</tasks>

<verification>
1. `grep -c 'Synapse Gateway Protocol' CLAUDE.md` returns 1
2. `grep -c 'sole point of contact' CLAUDE.md` returns at least 1
3. `test -f packages/framework/workflows/decision-draft-flow.md` succeeds
4. `grep -c 'decision_draft' packages/framework/workflows/decision-draft-flow.md` returns at least 3
5. `grep -c 'Decision Draft Convention' packages/framework/agents/_synapse-protocol.md` returns 1
6. `bun run test:framework` passes (no regressions from protocol updates)
</verification>

<success_criteria>
- CLAUDE.md establishes the main session as gateway with spawning rules, failure routing, and decision presentation
- Decision draft workflow uses document-based workaround (NOT the non-existent "draft" status on store_decision)
- _synapse-protocol.md references the draft convention so all agents that include it get the rule
- Existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-further-improvements-agentic-framework/26.1-01-SUMMARY.md`
</output>
