---
phase: 19-agent-prompts-level-awareness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/framework/agents/synapse-orchestrator.md
  - packages/framework/agents/decomposer.md
  - packages/framework/agents/architect.md
  - packages/framework/agents/product-strategist.md
  - packages/framework/agents/plan-reviewer.md
  - packages/framework/agents/executor.md
  - packages/framework/agents/validator.md
  - packages/framework/agents/integration-checker.md
  - packages/framework/agents/debugger.md
  - packages/framework/agents/researcher.md
  - packages/framework/agents/codebase-analyst.md
autonomous: true
requirements: [AGENT-01, AGENT-03, AGENT-08]

must_haves:
  truths:
    - "Every agent .md file has mcpServers: [\"synapse\"] in its YAML frontmatter"
    - "Every agent .md file has a 'Synapse MCP as Single Source of Truth' section with query-first principles"
    - "Every agent .md file has an error handling protocol with two-tier halt/warn behavior"
    - "Agents with multiple hierarchy levels have a level-aware behavior section"
  artifacts:
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "mcpServers frontmatter, shared MCP header, error handling, full 4-level section"
      contains: "mcpServers"
    - path: "packages/framework/agents/executor.md"
      provides: "mcpServers frontmatter, shared MCP header, error handling, short 2-tier section"
      contains: "mcpServers"
    - path: "packages/framework/agents/validator.md"
      provides: "mcpServers frontmatter, shared MCP header, error handling, short 2-tier section"
      contains: "mcpServers"
  key_links:
    - from: "all 11 agent .md files"
      to: "Claude Code mcpServers field"
      via: "YAML frontmatter mcpServers: [\"synapse\"]"
      pattern: "mcpServers.*synapse"
---

<objective>
Add mcpServers frontmatter, the shared "Synapse MCP as Single Source of Truth" section, error handling protocol, and level-aware behavior sections to all 11 agent markdown files.

Purpose: Without mcpServers in frontmatter, subagents silently lose MCP tool access (GitHub issues #5465, #13605). Without the shared MCP header, agents have no consistent instruction to query Synapse first. Without error handling protocol, agents may silently lose data on write failures.

Output: All 11 agent .md files updated with three new sections each (MCP header, error handling, level-aware behavior) plus mcpServers frontmatter.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-agent-prompts-level-awareness/19-CONTEXT.md
@.planning/phases/19-agent-prompts-level-awareness/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mcpServers frontmatter and shared MCP header to all 11 agents</name>
  <files>
    packages/framework/agents/synapse-orchestrator.md,
    packages/framework/agents/decomposer.md,
    packages/framework/agents/architect.md,
    packages/framework/agents/product-strategist.md,
    packages/framework/agents/plan-reviewer.md,
    packages/framework/agents/executor.md,
    packages/framework/agents/validator.md,
    packages/framework/agents/integration-checker.md,
    packages/framework/agents/debugger.md,
    packages/framework/agents/researcher.md,
    packages/framework/agents/codebase-analyst.md
  </files>
  <action>
For each of the 11 agent `.md` files in `packages/framework/agents/`:

**1. Add `mcpServers` to YAML frontmatter:**
Add `mcpServers: ["synapse"]` as the last line before the closing `---`. Example for executor.md:
```yaml
---
name: executor
description: Implements leaf tasks (depth=3)...
tools: Read, Write, Edit, Bash, ...
skills: [typescript, bun]
model: sonnet
color: green
mcpServers: ["synapse"]
---
```

**2. Insert shared "Synapse MCP as Single Source of Truth" section:**
Insert AFTER the `## Attribution` section and BEFORE the first substantive section (e.g., `## Core Behaviors`, `## Core Responsibilities`, `## Session Startup Protocol`). Use this exact template:

```markdown
## Synapse MCP as Single Source of Truth

Synapse stores project decisions and context. Query it first to avoid wasting tokens re-discovering what's already known.

**Principles:**
- Fetch context from Synapse (get_smart_context, query_decisions, get_task_tree) before reading filesystem for project context
- Read and write source code via filesystem tools (Read, Write, Edit, Bash, Glob, Grep)
- Use search_code or get_smart_context when file locations are unknown; go straight to filesystem when paths are specified in the task spec or handoff
- Write findings and summaries back to Synapse at end of task -- builds the audit trail

**Your Synapse tools:**
| Tool | Purpose | When to use |
|------|---------|-------------|
{AGENT-SPECIFIC TOOL TABLE - see below}

**Error handling:**
- WRITE failure (store_document, update_task, create_task, store_decision returns success: false): HALT. Report tool name + error message to orchestrator. Do not continue.
- READ failure (get_smart_context, query_decisions, search_code returns empty or errors): Note in a "Warnings" section of your output document. Continue with available information.
- Connection error on first MCP call: HALT with message "Synapse MCP server unreachable -- cannot proceed without data access."
```

**3. Per-agent tool reference table:**
Build the tool table from each agent's `tools:` frontmatter, including ONLY tools that start with `mcp__synapse__`. Strip the `mcp__synapse__` prefix for readability. Map each tool to its purpose:
- `get_smart_context` -> "Fetch decisions, docs, and code context" -> "Start of every task"
- `get_task_tree` -> "Load task spec, subtasks, and status" -> "Start of every task to read the spec"
- `update_task` -> "Update task status" -> "Mark task done/failed after completion"
- `create_task` -> "Create new tasks in hierarchy" -> "During decomposition"
- `store_decision` -> "Record architectural/design decisions" -> "After making decisions within your tier"
- `query_decisions` -> "Search existing decisions" -> "Before making new decisions"
- `check_precedent` -> "Find related past decisions" -> "Before any decision"
- `search_code` -> "Search indexed codebase" -> "When file locations are unknown"
- `store_document` -> "Store findings/reports/summaries" -> "End of task to record output"
- `link_documents` -> "Connect documents to tasks/decisions" -> "After storing a document"
- `query_documents` -> "Search stored documents" -> "Finding RPEV stage docs or prior findings"
- `semantic_search` -> "Full-text semantic search" -> "Broad knowledge base queries"
- `update_document` -> "Update existing document" -> "Revising prior findings"
- `project_overview` -> "Get project-level summary" -> "Session start, strategic decisions"
- `index_codebase` -> "Re-index code" -> "After implementation batches"
- `get_index_status` -> "Check index freshness" -> "Before/after indexing"

Only include tools the agent actually has in its `tools:` frontmatter. Classify each tool as WRITE (store_document, update_task, create_task, store_decision, update_document, link_documents) or READ (everything else) in the table for error handling clarity. For example, add a note column or mark with (W) suffix.

**4. Insert level-aware behavior section:**
Insert AFTER the MCP header section and BEFORE `## Core Behaviors` / `## Core Responsibilities`.

**Full 4-level section** for: synapse-orchestrator, decomposer, architect, product-strategist, plan-reviewer, integration-checker:
```markdown
## Level-Aware Behavior

Your behavior adjusts based on `hierarchy_level` from the handoff block:

| Level | Scope | Context to Fetch | Decision Tier |
|-------|-------|-----------------|---------------|
| epic | Full capability delivery | Broad: project decisions, all features (max_tokens 8000+) | Tier 0-1 |
| feature | Cohesive set of tasks | Feature decisions, related features (max_tokens 6000) | Tier 1-2 |
| component | Implementation grouping | Component decisions, sibling components (max_tokens 4000) | Tier 2 |
| task | Single implementation unit | Targeted: task spec + direct decisions (max_tokens 2000-4000) | Tier 3 |

At higher levels: fetch broader context, surface cross-cutting concerns, make wider-reaching decisions.
At lower levels: use targeted context, focus on spec-following, avoid scope creep.

Check the domain mode for this task's domain from your injected context. Adjust behavior per the Domain Autonomy Modes section.
```

**Short 2-tier section** for: executor, validator, debugger, researcher, codebase-analyst:
```markdown
## Level Context

You operate at:
- **task level** (depth=3): single implementation unit -- use targeted context (max_tokens 2000-4000)
- **feature level** (depth=1/2): cross-task analysis -- use broader context (max_tokens 6000+), examine integration seams

The `hierarchy_level` field in the handoff block tells you which applies.
```

**Agent-specific notes for level section:**
- integration-checker: Add note "You operate at feature and epic level only. Task-level validation is the Validator's job."
- executor: Add note "You are always spawned for leaf tasks (depth=3). Feature/epic-level execution is coordination handled by the orchestrator."
- debugger: Add note "At task level, examine single-file code bugs. At feature level, examine cross-task interactions and integration failures."
- validator: Add note "At higher levels, use the RPEV stage document as validation source -- it defines what each level should deliver."

**Section ordering for each agent file after edits:**
1. Frontmatter (with mcpServers added)
2. Opening paragraph (existing)
3. `## Attribution` (existing)
4. `## Synapse MCP as Single Source of Truth` (NEW)
5. `## Level-Aware Behavior` or `## Level Context` (NEW)
6. Remaining existing sections in their original order
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "mcpServers" packages/framework/agents/*.md | grep -v ":0$" | wc -l</automated>
  </verify>
  <done>
All 11 agent .md files have: (1) mcpServers: ["synapse"] in frontmatter, (2) "Synapse MCP as Single Source of Truth" section with per-agent tool table and error handling, (3) level-aware behavior section (4-level for decision-makers, 2-tier for executors). grep for "mcpServers" returns 11 matches. grep for "Single Source of Truth" returns 11 matches. grep for "HALT" returns 11 matches (error handling present in all).
  </done>
</task>

</tasks>

<verification>
1. `grep -c "mcpServers" packages/framework/agents/*.md` -- all 11 files show count >= 1
2. `grep -l "Single Source of Truth" packages/framework/agents/*.md` -- all 11 files listed
3. `grep -l "HALT" packages/framework/agents/*.md` -- all 11 files have error handling
4. `grep -l "Level-Aware Behavior\|Level Context" packages/framework/agents/*.md` -- all 11 files
5. Verify section ordering: Attribution -> MCP header -> Level section -> Core section in at least 3 files (orchestrator, executor, validator)
</verification>

<success_criteria>
- All 11 agent .md files contain `mcpServers: ["synapse"]` in frontmatter
- All 11 agent .md files have the shared MCP header section with per-agent tool table
- All 11 agent .md files have error handling protocol (halt on write failure, warn on read failure)
- 6 decision-maker agents have full 4-level behavior section
- 5 executor-tier agents have short 2-tier level context section
- Section ordering follows the prescribed pattern in all files
</success_criteria>

<output>
After completion, create `.planning/phases/19-agent-prompts-level-awareness/19-01-SUMMARY.md`
</output>
