---
phase: 26.3-tdd
plan: 03
type: execute
wave: 2
depends_on: ["26.3-01"]
files_modified:
  - packages/framework/agents/synapse-orchestrator.md
autonomous: true
requirements: [TDD-06]

must_haves:
  truths:
    - "Orchestrator dispatches test-designer after task-designer and before task-auditor in the per-task pipeline"
    - "Orchestrator handles BLOCKED escalation from executor by routing to appropriate agent"
    - "Orchestrator's upstream context routing table includes test-designer row"
  artifacts:
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "Orchestrator with test-designer dispatch step"
      contains: "test-designer"
  key_links:
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "packages/framework/agents/test-designer.md"
      via: "task dispatch in per-feature pipeline"
      pattern: "Test Designer"
---

<objective>
Update the synapse-orchestrator to dispatch the test-designer in the per-task pipeline and handle TDD-specific escalation routing.

Purpose: The orchestrator is the dispatcher that wires agents into the pipeline. It needs to know about the test-designer's position (after task-designer, before task-auditor) and handle the new BLOCKED/DONE_WITH_CONCERNS status reporting from executors. This completes the pipeline wiring.

Output: Updated synapse-orchestrator.md with test-designer dispatch step and escalation routing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.3-tdd/26.3-CONTEXT.md
@.planning/phases/26.3-tdd/26.3-RESEARCH.md
@.planning/phases/26.3-tdd/26.3-01-SUMMARY.md

<interfaces>
<!-- test-designer contract from Plan 01 -->

test-designer Output Contract:
- Test files on disk
- doc_id: `test-designer-test-contract-{task_id}`, provides: test-contract
- Tags: `"|test-designer|test-contract|provides:test-contract|{task_id}|stage:PLANNING|"`

Executor Status Values: DONE, DONE_WITH_CONCERNS, BLOCKED, NEEDS_CONTEXT

<!-- Current orchestrator structure (key sections that need updating) -->

Progressive Decomposition Protocol — For Each Feature (current, lines 76-83):
```
7. Spawn Task Designer to write detailed task specs
8. Spawn Task Auditor to verify specs
9. If Task Auditor rejects: respawn Task Designer (max 3 cycles)
10. Proceed to wave execution (Executor + Validator pairs)
```

RPEV Agent Roles (current list of 11 roles, lines 87-97)

Upstream Context Routing table (current, lines 336-347):
| Task Auditor | -- (reads spec field directly) | -- |
| Executor | task's context_doc_ids | task's context_decision_ids |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update orchestrator with test-designer dispatch and escalation routing</name>
  <files>packages/framework/agents/synapse-orchestrator.md</files>
  <action>
Update `packages/framework/agents/synapse-orchestrator.md` with the following changes. Preserve ALL existing content; these are targeted additions and modifications.

**1. Update "For Each Feature" section in Progressive Decomposition Protocol (~line 76-83):**

The current pipeline is:
```
7. Spawn Task Designer to write detailed task specs with mock code, file paths, integration points
8. Spawn Task Auditor to verify specs are execution-ready
9. If Task Auditor rejects: respawn Task Designer with feedback (max 3 cycles)
10. Proceed to wave execution (Executor + Validator pairs, unchanged)
```

Change to:
```
7. Spawn **Task Designer** to write detailed task specs with mock code, file paths, integration points
8. Spawn **Test Designer** to write executable failing tests from the spec and planner test expectations
9. If Test Designer reports plan deficiency (requirement too vague to test): escalate to Planner
10. Spawn **Task Auditor** to triangulate spec + tests + requirements and verify execution-readiness
11. If Task Auditor rejects: route to the agent specified in the rejection (test-designer, task-designer, or planner) — max 3 cycles per reject target
12. Proceed to wave execution (Executor + Validator pairs)
```

**2. Add "Test Designer" to RPEV Agent Roles list (~line 87-97):**

Add after the Task Designer entry:
```
- **Test Designer** — Writes executable failing tests from task-designer specs and planner test expectations; verifies RED state
```

This makes the list 12 roles total.

**3. Update Upstream Context Routing table (~line 336-347):**

Add a new row for test-designer between Task Designer and Task Auditor:

```
| Test Designer | task-designer spec rationale doc_id | task's context_decision_ids |
```

Update the Task Auditor row to include the test contract:
```
| Task Auditor | test-designer-test-contract-{task_id} (reads spec field directly from task) | -- |
```

Update the Executor row to include test contract awareness:
```
| Executor | task's context_doc_ids (set by Planner/Task Designer) | task's context_decision_ids |
```
(Executor reads test file paths from task description, not from context_doc_ids — no change needed here.)

Update the Validator row:
```
| Validator | executor's output_doc_ids from task field, test-designer-test-contract-{task_id} | -- |
```

**4. Add "## TDD Escalation Routing" section after the existing "Failure Reporting" section (~line 148):**

```markdown
## TDD Escalation Routing

When an executor reports a non-DONE status in its output document:

### BLOCKED

Read the executor's output document to understand the block:

1. **Implementation gap** (executor says "I cannot find an approach" but test seems correct): Respawn executor with additional guidance. If still blocked after retry: spawn Debugger to investigate.
2. **Wrong test assumption** (executor says "test encodes incorrect requirement"): Escalate to Planner — the planner owns the requirement contract. The planner revises the requirement, then test-designer produces updated tests. Resume from task-auditor review.
3. **Ambiguous** (executor is unsure): Spawn Task Auditor to evaluate. Task Auditor inspects the test, runs it independently, and determines routing (executor retry vs planner revision).

### DONE_WITH_CONCERNS

Proceed to Validator as normal. Include the executor's concern in the validator's handoff context. The validator reviews the concern alongside the implementation:
- If concern is cosmetic or non-blocking: validator confirms PASS
- If concern reveals a plan gap: validator notes it in findings for the next planning cycle (does not block current task completion)
- If concern is critical (e.g., discovered security issue): validator reports FAIL and escalates

### NEEDS_CONTEXT

Spawn the Researcher with the executor's context request. Feed researcher findings back to the executor for retry.
```

**5. Update the Wave Execution Protocol "Executing a Wave" section (~line 119-129):**

After step 1 ("For each task in the wave, spawn an Executor subagent...") add a note:

```
   - Before dispatching executor: verify test-designer tests exist for this task (check task description for appended test file paths). If no tests: HALT — test-designer step was skipped.
```

After step 3 ("For each completed task: spawn Validator subagent...") add:

```
   - Include test-contract doc_id (test-designer-test-contract-{task_id}) in validator's handoff doc_ids so validator can verify test immutability
```

**6. Update the "On Task Completion" section in Pool Manager Protocol (~line 254):**

After step 1 ("Identify which slot completed"), add:

```
1b. Check executor output document status: if BLOCKED or NEEDS_CONTEXT, do NOT proceed to validator — follow TDD Escalation Routing instead
```
  </action>
  <verify>
    <automated>grep -c "Test Designer" packages/framework/agents/synapse-orchestrator.md && grep "TDD Escalation Routing" packages/framework/agents/synapse-orchestrator.md && grep "BLOCKED" packages/framework/agents/synapse-orchestrator.md | head -3</automated>
  </verify>
  <done>synapse-orchestrator.md has: (1) test-designer as step 8 in per-feature pipeline (between task-designer and task-auditor), (2) "Test Designer" in RPEV Agent Roles list (12 roles), (3) test-designer and test-contract in upstream context routing table, (4) TDD Escalation Routing section handling BLOCKED/DONE_WITH_CONCERNS/NEEDS_CONTEXT, (5) pre-dispatch test verification check, (6) test-contract in validator handoff, (7) executor status check before proceeding to validator.</done>
</task>

</tasks>

<verification>
1. Orchestrator's per-feature pipeline has test-designer between task-designer (step 7) and task-auditor (step 10)
2. RPEV Agent Roles list includes "Test Designer" with correct description
3. Upstream Context Routing table has test-designer row and updated task-auditor/validator rows
4. TDD Escalation Routing section covers BLOCKED, DONE_WITH_CONCERNS, and NEEDS_CONTEXT flows
5. Wave execution includes pre-dispatch test verification and test-contract in validator handoff
6. Pool Manager checks executor status before routing to validator
</verification>

<success_criteria>
- Orchestrator pipeline correctly sequences: Task Designer -> Test Designer -> Task Auditor -> Executor -> Validator
- Escalation routing handles all four executor status values with clear agent routing
- Upstream context routing table is complete for all 12 agent roles
- Existing orchestrator behavior (pool management, stage gates, PR workflow) is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/26.3-tdd/26.3-03-SUMMARY.md`
</output>
