---
phase: 18-rpev-orchestration
plan: 03
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - packages/framework/commands/synapse/refine.md
  - packages/framework/commands/synapse/status.md
  - packages/framework/commands/synapse/focus.md
  - packages/framework/config/agents.toml
autonomous: true
requirements: [RPEV-01, RPEV-06, RPEV-07]

must_haves:
  truths:
    - "When /synapse:refine confirms readiness, it creates an RPEV stage document marking the item as PLANNING"
    - "/synapse:status queries stage documents and shows items with pending_approval=true in a Needs Your Input section"
    - "/synapse:focus implements two-tier approval UX: summary + approve/reject/discuss, with discuss deeper option"
    - "Failed items with exhausted retries appear as flagged in /synapse:status"
    - "agents.toml grants orchestrator store_document, link_documents, and query_documents"
  artifacts:
    - path: "packages/framework/commands/synapse/refine.md"
      provides: "Bridge from Refine readiness to RPEV stage document creation"
      contains: "rpev-stage"
    - path: "packages/framework/commands/synapse/status.md"
      provides: "Stage document query for pending approvals display"
      contains: "rpev-stage"
    - path: "packages/framework/commands/synapse/focus.md"
      provides: "Two-tier approval UX with proposal loading"
      contains: "pending_approval"
    - path: "packages/framework/config/agents.toml"
      provides: "Updated tool permissions for orchestrator"
      contains: "store_document"
  key_links:
    - from: "packages/framework/commands/synapse/refine.md"
      to: "store_document with rpev-stage-[task_id]"
      via: "Step 9 creates stage document on readiness"
      pattern: "rpev-stage"
    - from: "packages/framework/commands/synapse/status.md"
      to: "query_documents with tag rpev-stage"
      via: "Step 4 queries for pending approvals"
      pattern: "rpev-stage"
    - from: "packages/framework/commands/synapse/focus.md"
      to: "get_smart_context with proposal_doc_id"
      via: "Step 7 loads proposal for approval UX"
      pattern: "proposal_doc_id"
---

<objective>
Update /synapse:refine, /synapse:status, and /synapse:focus commands to integrate with the RPEV stage document system, and update agents.toml to grant the orchestrator stage document management tools.

Purpose: These commands are the user-facing surface of the RPEV engine. Refine needs to bridge to the orchestrator (create stage documents on readiness). Status needs to surface pending approvals. Focus needs the two-tier approval UX. Without these updates, the RPEV engine has no user-facing interface.

Output: Updated refine.md with stage document creation, status.md with stage document querying, focus.md with approval UX, agents.toml with updated tool permissions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-rpev-orchestration/18-CONTEXT.md
@.planning/phases/18-rpev-orchestration/18-RESEARCH.md
@.planning/phases/18-rpev-orchestration/18-02-SUMMARY.md

<interfaces>
<!-- Key interfaces from Plan 02 that this plan depends on -->

From pev-workflow.md (updated in 18-02):
Stage Document Schema:
```
doc_id: "rpev-stage-[task_id]"
category: "plan"
tags: "|rpev-stage|[level]|[stage_lowercase]|"
content (JSON): {
  stage: "REFINING" | "PLANNING" | "EXECUTING" | "VALIDATING" | "DONE",
  level: "project" | "epic" | "feature" | "work_package",
  task_id: string,
  involvement: string (resolved mode),
  pending_approval: boolean,
  proposal_doc_id: string | null,
  last_updated: ISO string,
  notes: string | null
}
```

From synapse-orchestrator.md (updated in 18-02):
- Now has store_document, link_documents, query_documents in tools list
- Involvement Matrix section defines all 5 modes
- Stage Document Management section defines doc_id pattern

From status.md (current):
- Step 2 already extracts RPEV stages from task tree for display
- "Needs Your Input" section exists but relies on blocked status only

From focus.md (current):
- Step 5 already offers contextual actions based on item state
- Step 6 handles blocked item decision engagement

From agents.toml (current):
- synapse-orchestrator is NOT listed separately in agents.toml
  (it's the main agent, not a subagent — but its subagent spawn ability means the orchestrator agent itself needs store_document/link_documents/query_documents in its slash commands)
- plan-reviewer and integration-checker currently lack store_document and link_documents

From query_documents tool (verified from source):
- Supports `tags` filter: single tag exact match via pipe-delimited format
- Example: tags="rpev-stage" matches documents with "|rpev-stage|" in tags
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bridge /synapse:refine to RPEV stage documents</name>
  <files>packages/framework/commands/synapse/refine.md</files>
  <action>
Update `packages/framework/commands/synapse/refine.md` to create RPEV stage documents when readiness is confirmed. This bridges the Refine command to the orchestrator's RPEV engine.

**Frontmatter changes:**
- Add `mcp__synapse__query_documents` to `allowed-tools` list (needed for checking existing stage documents)

**Step 7 update** (Check readiness): After the existing readiness assessment, add involvement matrix awareness:
- Add a note after the readiness summary: "The involvement mode for the Plan stage at this level determines what happens next. The orchestrator (or startup hook) injects the involvement matrix — the mode for `{level}_plan` controls whether user approval is needed before planning begins."

**Step 8 update** (Persist refinement state): No changes needed — this already stores the refinement document.

**Step 9 replacement** (Close session): Replace the current Phase 18 stub message with actual RPEV stage document creation. When readiness is confirmed (not when pausing):

Replace the current step 9 "If readiness confirmed" block with:

```markdown
9. **Bridge to RPEV orchestrator:** When readiness is confirmed in step 7:

   a. **Create/update RPEV stage document** to signal the orchestrator that this item is ready for planning. Call `mcp__synapse__store_document` with:
      - `project_id`: from session context
      - `doc_id`: `"rpev-stage-[task_id]"` (where task_id is the item's task tree ID — look it up from step 2's get_task_tree call. If this is a new concept with no task tree entry yet, create one first via `mcp__synapse__create_task`)
      - `title`: `"RPEV Stage: [Item Title] ([Level])"`
      - `category`: `"plan"`
      - `status`: `"active"`
      - `tags`: `"|rpev-stage|[level]|planning|"`
      - `content`: JSON string with:
        ```json
        {
          "stage": "PLANNING",
          "level": "[detected level from step 2]",
          "task_id": "[task_id]",
          "involvement": "[resolved involvement mode for {level}_plan from matrix]",
          "pending_approval": [true if involvement is "drives" or "co-pilot", false otherwise],
          "proposal_doc_id": null,
          "last_updated": "[ISO timestamp]",
          "notes": "Refinement complete. [N] decisions stored. Ready for planning."
        }
        ```
      - `actor`: `"synapse-orchestrator"`

   b. **Present transition message:**
      ```
      ## Refinement Complete: [Item Title] ([Level])

      [N] decisions stored in Synapse (Tiers 0-N)
      All required decisions captured for this level.

      RPEV stage document created — this item is now queued for planning.
      [If pending_approval=true:]
        The involvement mode for planning at [level] level is "[mode]" — the orchestrator
        will prepare a plan proposal for your review.
      [If pending_approval=false:]
        The involvement mode for planning at [level] level is "[mode]" — the orchestrator
        will proceed with planning automatically.

      Run `/synapse:status` to see all items in the RPEV pipeline.
      ```

   c. If pausing (OPEN items remain) — keep existing behavior:
      ```
      Session saved. Run `/synapse:refine` again to resume — your DECIDED, OPEN,
      and EMERGING items will be loaded automatically from where you left off.
      ```
```

**Also update step 1** (Determine scope): When searching for in-progress sessions, also check for existing stage documents:
- Add: "Also call `mcp__synapse__query_documents` with category `"plan"`, tags `"rpev-stage"` to check if this item already has an RPEV stage document. If it does, note the current stage — the user may want to resume refinement on an item that's already in the pipeline."
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "rpev-stage" packages/framework/commands/synapse/refine.md && grep -c "store_document" packages/framework/commands/synapse/refine.md && grep -c "PLANNING\|pending_approval" packages/framework/commands/synapse/refine.md && grep "query_documents" packages/framework/commands/synapse/refine.md | head -1 && echo "Refine bridge keywords present"</automated>
  </verify>
  <done>refine.md creates an RPEV stage document with stage=PLANNING and appropriate pending_approval flag when readiness is confirmed. The transition message tells the user what the orchestrator will do next based on involvement mode. The old Phase 18 stub message is replaced with actual RPEV integration. query_documents is in allowed-tools.</done>
</task>

<task type="auto">
  <name>Task 2: Update /synapse:status and /synapse:focus for stage document integration + agents.toml</name>
  <files>packages/framework/commands/synapse/status.md, packages/framework/commands/synapse/focus.md, packages/framework/config/agents.toml</files>
  <action>
Update three files to complete the RPEV user-facing surface.

---

**FILE 1: packages/framework/commands/synapse/status.md**

Add `mcp__synapse__query_documents` to `allowed-tools` in frontmatter.

**Replace step 4** (Check for active refinement sessions) with a broader RPEV stage document query:

```markdown
4. **Query RPEV stage documents:** Call `mcp__synapse__query_documents` with:
   - `category`: `"plan"`
   - `tags`: `"rpev-stage"`
   - `actor`: `"synapse-orchestrator"`

   Parse each returned document's content (JSON) to extract:
   - Items with `pending_approval: true` — these need user attention
   - Items with `stage: "REFINING"` — active refinement sessions
   - Items with notes containing failure/retry information — these are flagged failures
   - Items with `stage: "EXECUTING"` or `"VALIDATING"` — in-progress work

   Also still check for active refinement sessions via `mcp__synapse__semantic_search` with query `"refinement state"`, category `"plan"`, status `"active"` — refinement state documents are separate from stage documents.
```

**Update step 5** (Present RPEV dashboard) — enhance the "Needs Your Input" section:

Replace the current "Needs Your Input" block with:

```markdown
### Needs Your Input

[If any stage documents have pending_approval=true:]
N items need your attention:
  1. [Item Title] [[LEVEL]] [STAGE] — [describe what's needed based on stage doc notes]
     Involvement mode: [mode] | Use `/synapse:focus "[title]"` to review
  2. ...

[If any stage documents have failure notes:]
N items have issues:
  1. [Item Title] [[LEVEL]] [STAGE] — retries exhausted, needs guidance
     Use `/synapse:focus "[title]"` to see diagnostic report

[If no pending items:]
All clear — no items currently need your input.
```

Also update the epic display in step 5 to use stage document data when available (stage document is more authoritative than task tree status for RPEV stage):
- If a stage document exists for an epic, use its `stage` field for the RPEV stage badge
- If no stage document exists, fall back to inferring stage from task tree status (existing behavior)

---

**FILE 2: packages/framework/commands/synapse/focus.md**

Add `mcp__synapse__query_documents` and `mcp__synapse__update_task` to `allowed-tools` in frontmatter (query_documents for stage doc lookup, update_task for approval processing).

**Add new step 7** (between current steps 6 and the Deferred Features note) — Two-Tier Approval UX:

```markdown
7. **Handle pending approval (two-tier UX):** After presenting item context (step 4), check if the item has an RPEV stage document with `pending_approval: true`. To check: call `mcp__synapse__query_documents` with category `"plan"`, tags `"rpev-stage"` and look for a document with doc_id matching `"rpev-stage-[task_id]"`.

   If `pending_approval` is true and `proposal_doc_id` is set:

   a. **Load the proposal:** Call `mcp__synapse__get_smart_context` with the `proposal_doc_id` to retrieve the full proposal document.

   b. **Present summary-first view (Tier 1 — quick triage):**
      ```
      ## Proposal: [Item Title]

      **Stage:** [current stage] | **Level:** [level] | **Involvement mode:** [mode]
      **Quality check:** [If Plan Reviewer ran: "Passed" or note]

      ### Summary
      [2-3 sentence summary extracted from the proposal document]

      ### Key decisions in this proposal
      - [Decision 1]
      - [Decision 2]

      ### Options
      A) **Approve** — proceed with this plan
      B) **Reject** — send back with your feedback
      C) **Discuss deeper** — conversational review of the full proposal
      ```

   c. **If user chooses Approve:**
      - Update stage document: set `pending_approval: false`, advance `stage` to next phase (e.g., PLANNING -> EXECUTING)
      - Update task status if needed: call `mcp__synapse__update_task` with `status: "in_progress"`
      - Confirm: "Approved. The orchestrator will proceed with [next stage]."

   d. **If user chooses Reject:**
      - Ask for specific feedback: "What should be changed? Your guidance will be passed to the [Decomposer/Planner] for revision."
      - Update stage document: set `notes` to rejection feedback, keep `pending_approval: true`
      - The orchestrator will detect this on next session start and re-run the specialist agent with feedback (max 3 review cycles per WFLOW-06)

   e. **If user chooses Discuss Deeper (Tier 2 — conversational review):**
      - Load and display the full proposal document content
      - Switch to conversational mode: ask clarifying questions, explore concerns, let the user probe details
      - After discussion, re-present options A (Approve) and B (Reject)

   If `pending_approval` is true but NO `proposal_doc_id`:
   - This is a "drives" mode item waiting for user initiation, or a failed item
   - If stage notes indicate failure: present the Debugger's diagnostic report (fetch via get_smart_context) plus options:
     ```
     ### Failed Item: [Title]

     **Diagnostic:** [summary from debugger report]
     **Retries used:** [N] of [max]

     Options:
     A) Retry with guidance — provide direction for the next attempt
     B) Redefine the task — rethink the approach
     C) Skip and continue — mark as skipped, proceed with other work
     D) Escalate to parent — push this up to [parent level] for re-evaluation
     ```
   - If stage is REFINING/waiting: "This item is waiting for you to refine it. Run `/synapse:refine \"[title]\"` to start."
```

---

**FILE 3: packages/framework/config/agents.toml**

Add `mcp__synapse__store_document`, `mcp__synapse__link_documents`, and `mcp__synapse__query_documents` to the following agents' `allowed_tools` lists:

1. **plan-reviewer**: Add `mcp__synapse__store_document` and `mcp__synapse__link_documents` (AGENT-06 prep: persist findings as linked documents). These tools are needed for the Plan Reviewer to store its review results as documents linked to the task, rather than only updating task status.

2. **integration-checker**: Add `mcp__synapse__store_document` and `mcp__synapse__link_documents` (AGENT-06 prep: persist findings as linked documents).

3. **executor**: Add `mcp__synapse__store_document` and `mcp__synapse__link_documents` (AGENT-07 prep: store implementation summaries as documents).

4. **validator**: Add `mcp__synapse__store_document` and `mcp__synapse__link_documents` (AGENT-05 prep: store findings as linked documents instead of overwriting task description).

Note: The synapse-orchestrator is the parent agent (not a subagent spawned via Task), so its tools are defined in its .md frontmatter, not in agents.toml. The frontmatter was updated in Plan 18-02.

Keep all existing tools for each agent — only ADD the new ones. Do not remove any existing entries.
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && echo "=== refine.md ===" && grep -c "rpev-stage" packages/framework/commands/synapse/refine.md && echo "=== status.md ===" && grep -c "rpev-stage\|pending_approval" packages/framework/commands/synapse/status.md && echo "=== focus.md ===" && grep -c "pending_approval\|Discuss deeper\|proposal_doc_id" packages/framework/commands/synapse/focus.md && echo "=== agents.toml ===" && grep -c "store_document" packages/framework/config/agents.toml && grep -c "link_documents" packages/framework/config/agents.toml && echo "All integration keywords present"</automated>
  </verify>
  <done>refine.md creates stage documents on readiness confirmation (bridges to orchestrator). status.md queries stage documents and displays pending approvals, failures, and in-progress items. focus.md implements two-tier approval UX with summary-first view and discuss-deeper option. agents.toml grants store_document and link_documents to plan-reviewer, integration-checker, executor, and validator.</done>
</task>

</tasks>

<verification>
1. refine.md step 9 calls store_document with doc_id "rpev-stage-[task_id]" and stage "PLANNING"
2. status.md step 4 queries query_documents with tag "rpev-stage" and displays pending approvals
3. focus.md step 7 implements two-tier approval UX (summary-first + discuss deeper option)
4. agents.toml has store_document and link_documents for plan-reviewer, integration-checker, executor, and validator
5. All three commands have query_documents in their allowed-tools frontmatter
6. No references to "Phase 18 stub" or "RPEV orchestrator not yet available" remain in any command
</verification>

<success_criteria>
- /synapse:refine creates an RPEV stage document when readiness is confirmed, with correct pending_approval based on involvement mode
- /synapse:status surfaces pending approvals from stage documents (not just blocked status from task tree)
- /synapse:focus provides two-tier approval UX: quick triage (summary + options) and deep review (conversational)
- Failed items with exhausted retries show diagnostic report + structured options in /synapse:focus
- agents.toml grants store_document and link_documents to 4 specialist agents (prep for Phase 19 AGENT-05/06/07)
- The old "Phase 18 stub" message in refine.md step 9 is replaced with real RPEV integration
</success_criteria>

<output>
After completion, create `.planning/phases/18-rpev-orchestration/18-03-SUMMARY.md`
</output>
