---
phase: 21-agent-pool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/framework/config/trust.toml
  - packages/framework/hooks/synapse-startup.js
  - packages/framework/agents/synapse-orchestrator.md
  - packages/framework/workflows/pev-workflow.md
autonomous: true
requirements: [POOL-01, POOL-02]
must_haves:
  truths:
    - "trust.toml defines max_pool_slots=3 in [rpev] section and max_parallel_executors is removed"
    - "synapse-startup.js injects max_pool_slots into the RPEV context block so the orchestrator sees it in session context"
    - "The orchestrator has a Pool Manager Protocol section with dispatch loop, slot tracking, priority queue, token capture, and session recovery"
    - "pev-workflow.md Wave Execution delegates to pool dispatch instead of issuing all Task calls in one turn"
    - "The finish-first policy is explicit: validators for completed tasks get priority over new execution"
    - "Cross-epic slot fill is defined: when highest-priority epic has fewer unblocked tasks than slots, lower-priority epics contribute"
    - "Pool state document schema is defined with doc_id pool-state-[project_id]"
    - "Token capture protocol is defined: extract usage from Task tool result, store via update_task tags"
  artifacts:
    - path: "packages/framework/config/trust.toml"
      provides: "max_pool_slots config, replacing max_parallel_executors"
      contains: "max_pool_slots = 3"
    - path: "packages/framework/hooks/synapse-startup.js"
      provides: "Pool config injection into session context"
      contains: "max_pool_slots"
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "Pool Manager Protocol with dispatch loop, slot tracking, priority queue, token capture"
      min_lines: 350
    - path: "packages/framework/workflows/pev-workflow.md"
      provides: "Pool-mediated wave execution replacing direct Task calls"
      contains: "pool"
  key_links:
    - from: "packages/framework/config/trust.toml"
      to: "packages/framework/hooks/synapse-startup.js"
      via: "synapse-startup.js reads max_pool_slots from trustToml.rpev"
      pattern: "max_pool_slots"
    - from: "packages/framework/hooks/synapse-startup.js"
      to: "packages/framework/agents/synapse-orchestrator.md"
      via: "injected context provides max_pool_slots value that orchestrator references"
      pattern: "max_pool_slots"
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "packages/framework/workflows/pev-workflow.md"
      via: "orchestrator references pev-workflow for RPEV execution; pev-workflow delegates to pool dispatch"
      pattern: "pool.*dispatch"
---

<objective>
Wire up the agent pool infrastructure: config, startup injection, pool manager protocol in the orchestrator, and pool-mediated wave execution.

Purpose: This plan establishes the core pool machinery. The orchestrator gains a Pool Manager Protocol that caps concurrent Task tool calls at max_pool_slots, dispatches work via a finish-first priority algorithm, tracks pool state via a Synapse document, and captures token usage after each task completes. The pev-workflow is updated to delegate to the pool instead of issuing all Task calls in one turn.

Output: Updated trust.toml, synapse-startup.js, synapse-orchestrator.md, and pev-workflow.md with pool manager logic.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-agent-pool/21-CONTEXT.md
@.planning/phases/21-agent-pool/21-RESEARCH.md

@packages/framework/config/trust.toml
@packages/framework/hooks/synapse-startup.js
@packages/framework/agents/synapse-orchestrator.md
@packages/framework/workflows/pev-workflow.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/framework/config/trust.toml (current [rpev] section to modify):
```toml
[rpev]
explicit_gate_levels = ["project", "epic"]
proactive_notifications = false
max_parallel_executors = 3
max_retries_task = 3
max_retries_feature = 2
max_retries_epic = 1
```

From packages/framework/hooks/synapse-startup.js (rpevContext build block, around line 178-225):
```javascript
// The rpevContext block reads trustToml.rpev and builds rpevLines[]
// Add max_pool_slots injection to this block
if (trustToml && trustToml.rpev) {
  const involvement = trustToml.rpev.involvement || {};
  // ... existing involvement matrix injection ...
  rpevContext = rpevLines.join("\n");
}
```

From packages/framework/agents/synapse-orchestrator.md (Wave Execution Protocol section, line ~181-210):
```markdown
## Wave Execution Protocol
### Identifying Waves
1. Analyze task dependencies within the active feature
2. Tasks with no unmet dependencies = Wave 1; their dependents = Wave 2; and so on
3. Cap parallel executors at `rpev.max_parallel_executors` concurrent subagents per wave
### Executing a Wave
1. For each task in the wave, spawn an Executor subagent via Task tool with `isolation: "worktree"`
   - Issue all Task tool calls in a single turn for true parallel execution
```

Pool state document schema (to be defined in orchestrator):
```json
{
  "project_id": "string",
  "max_slots": 3,
  "slots": {
    "A": { "task_id": "string", "task_title": "string", "agent_type": "string",
            "epic_title": "string", "epic_id": "string", "started_at": "ISO string",
            "rpev_stage": "string", "recent_tool_calls": [] } | null,
    "B": null,
    "C": null
  },
  "queue": [{ "task_id": "string", "task_title": "string", "epic_id": "string", "type": "string" }],
  "tokens_by_task": { "task_id": "number" },
  "last_updated": "ISO string"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update trust.toml config and synapse-startup.js pool injection</name>
  <files>packages/framework/config/trust.toml, packages/framework/hooks/synapse-startup.js</files>
  <action>
**trust.toml changes:**
In the `[rpev]` section, replace `max_parallel_executors = 3` with:
```toml
# Maximum concurrent agent slots in the pool (all agent types share this limit)
max_pool_slots = 3
```
Remove the `max_parallel_executors` line entirely. Keep all other `[rpev]` keys (explicit_gate_levels, proactive_notifications, max_retries_*) unchanged.

**synapse-startup.js changes:**
In the rpevContext build block (the `if (trustToml && trustToml.rpev)` section around line 178), add pool config injection. After the involvement matrix display lines are built and after the domain overrides section, add:

```javascript
// Pool config
const maxPoolSlots = trustToml.rpev.max_pool_slots ?? 3;
rpevLines.push(
  "",
  "## Agent Pool Config",
  `  max_pool_slots: ${maxPoolSlots}  (default: 3, all agent types share this limit)`,
);
```

This goes BEFORE the final `rpevContext = rpevLines.join("\n");` line. The pool config will be visible to the orchestrator in its session context alongside the involvement matrix.

Do NOT change any other part of synapse-startup.js. The trustToml variable is already in scope (hoisted to outer scope per Phase 20-03 fix).
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "max_pool_slots" packages/framework/config/trust.toml && grep -c "max_parallel_executors" packages/framework/config/trust.toml | grep "^0$" && grep "max_pool_slots" packages/framework/hooks/synapse-startup.js | head -3</automated>
  </verify>
  <done>trust.toml has max_pool_slots=3 and no max_parallel_executors. synapse-startup.js injects max_pool_slots into rpevContext so the orchestrator sees it in session additionalContext.</done>
</task>

<task type="auto">
  <name>Task 2: Add Pool Manager Protocol to synapse-orchestrator.md</name>
  <files>packages/framework/agents/synapse-orchestrator.md</files>
  <action>
Add a new major section "## Pool Manager Protocol" to synapse-orchestrator.md. Insert it AFTER the existing "## Wave Execution Protocol" section and BEFORE the "## Failure Escalation Protocol" section. This section defines the orchestrator's behavioral contract for managing the agent pool.

**Section structure (all content below goes into the orchestrator markdown):**

### Pool Manager Protocol heading with explanation:
The orchestrator manages a pool of N agent slots (N = max_pool_slots from session context, default 3). Every subagent spawned via Task tool consumes a slot. Slots are named A, B, C (up to N). When all slots are busy, new work queues. The pool replaces the old "issue all Task calls in one turn" pattern.

### Pool State Document subsection:
- doc_id: `pool-state-[project_id]` (fixed pattern, enables upsert versioning)
- Store via `mcp__synapse__store_document` with category "plan", tags "|pool-state|active|", actor "synapse-orchestrator"
- Document the full JSON schema for the pool state (slots map, queue array, tokens_by_task map, last_updated). Use the schema from the research interfaces block above.
- When to write: on every slot assignment change (task assigned, task completed, task cancelled)
- When to read: at session start for recovery, by /synapse:status and /synapse:focus agent X

### Session Start Recovery subsection:
On session start:
1. Read pool-state document via query_documents(category: "plan", tags: "|pool-state|")
2. If it exists and shows slots with non-null task assignments: those Task tool calls are orphaned (previous session ended)
3. For each orphaned slot: check task status via get_task_tree. If task is still "in_progress", mark it as interrupted: call update_task(status: "ready") to re-queue it
4. Clear all slots in pool state (set to null)
5. Emit recovery message: "Found [N] abandoned in-flight tasks from previous session. Re-queuing them."
6. Run dispatch tick to fill slots with available work

### Priority Algorithm subsection (dispatch tick):
Run a dispatch tick when: a slot opens (task completes/cancelled) OR new work arrives (task created/unblocked).

Priority order (finish-first policy):
1. **Pending validators** for tasks with status "done" that have not yet been validated (check for completed tasks without a validator-findings document linked). Finish-first scoped to current wave only -- once all current-wave validations pass, move on.
2. **Pending integration checks** for features where all child tasks are validated complete
3. **Highest-priority epic's next unblocked task** by wave order (lowest wave number first, then creation order within wave). Only pull from features already in EXECUTING stage (already decomposed into tasks).
4. **Cross-epic fill** -- repeat step 3 for lower-priority epics. Only pull tasks from features that are already decomposed and in wave execution. Do NOT trigger JIT decomposition of a lower-priority epic's features to fill slots.
5. **Idle** -- if nothing remains to dispatch, check if all pending work is blocked:
   - All blocked: emit proactive message "All unblocked work assigned. Items waiting for your approval: [list pending_approval items]"
   - All done: emit "All work complete for this epic/project."

### Dispatch Loop Pseudocode subsection:
```
On dispatch tick:
1. available_slots = [s for s in slots if s.value is null]
2. If no available slots: stop
3. Build work_queue via priority algorithm (steps 1-4 above)
4. For each item in work_queue (up to len(available_slots)):
   a. Assign to next available slot (A before B before C)
   b. Determine agent_type from task context:
      - Task needing validation -> "validator"
      - Feature needing integration check -> "integration-checker"
      - Task ready for execution -> "executor"
   c. Spawn Task tool call with SYNAPSE HANDOFF block and appropriate agent
   d. Update pool-state document with slot assignment
5. If work_queue empty after fill: check blocked/done state (step 5 above)
```

### On Task Completion subsection:
When a Task tool call completes:
1. Identify which slot completed (by tracking which Task call maps to which slot)
2. Extract token usage if available: if the Task result contains usage data, compute total = input_tokens + output_tokens
3. Store token count: call update_task with tags updated to include `|tokens_used=[total]|`. Pattern for existing tags: `tags.replace(/\|tokens_used=\d+\|/, '') + '|tokens_used=' + total + '|'` (replace existing if re-run)
4. Update pool-state document: set `tokens_by_task[task_id] = total`, update slot assignment
5. Free the slot (set to null)
6. Run dispatch tick to fill the opened slot

### Token Usage Storage subsection:
- After each Task tool completes, extract `usage.input_tokens + usage.output_tokens` from the result
- Store on the task via `update_task` with the `|tokens_used=N|` tag pattern
- Also write to pool-state document's `tokens_by_task` map for quick aggregate access
- Token parsing regex: `/\|tokens_used=(\d+)\|/` -- extract the number from tags string
- If update_task already has a tokens_used tag (retry scenario), replace the existing value

### Anti-Patterns subsection:
- NEVER spawn Task tool calls outside the pool manager -- ALL Task calls go through pool dispatch
- NEVER issue all wave Task calls in one turn -- the pool caps at max_pool_slots
- Slot letters (A, B, C) map to slot indices, NOT task identity. A = slot 0 always
- Cross-epic fill: only pull from features already in EXECUTING stage. Do NOT trigger JIT decomposition just to fill a slot
- If pool-state shows in-flight tasks at session start, don't wait for them -- they are orphaned. Re-queue and continue.

**Also update the existing "## Wave Execution Protocol" section:**

Change the line "3. Cap parallel executors at `rpev.max_parallel_executors` concurrent subagents per wave" to:
"3. Cap parallel agents at `max_pool_slots` concurrent subagents (managed by the Pool Manager)"

In "### Executing a Wave", replace the instruction "Issue all Task tool calls in a single turn for true parallel execution" with:
"Dispatch wave tasks via the Pool Manager Protocol -- do NOT issue all Task calls in one turn. Assign tasks to available pool slots up to max_pool_slots. When a slot completes, the dispatch tick pulls the next queued task automatically."

Remove or update any remaining references to `max_parallel_executors` in the orchestrator to use `max_pool_slots` instead.
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "Pool Manager Protocol" packages/framework/agents/synapse-orchestrator.md && grep -c "max_parallel_executors" packages/framework/agents/synapse-orchestrator.md | grep "^0$" && grep -c "dispatch tick" packages/framework/agents/synapse-orchestrator.md && grep -c "pool-state" packages/framework/agents/synapse-orchestrator.md && grep -c "tokens_used" packages/framework/agents/synapse-orchestrator.md</automated>
  </verify>
  <done>synapse-orchestrator.md has a complete Pool Manager Protocol section with dispatch loop, priority algorithm (finish-first, cross-epic fill), pool state document schema, session recovery, token capture, and anti-patterns. Wave Execution Protocol updated to delegate to pool dispatch. No references to max_parallel_executors remain.</done>
</task>

<task type="auto">
  <name>Task 3: Update pev-workflow.md for pool-mediated execution</name>
  <files>packages/framework/workflows/pev-workflow.md</files>
  <action>
Update pev-workflow.md to reflect pool-mediated dispatch. This file is the authoritative RPEV workflow document read by the orchestrator.

**Configuration section (top of file):**
Replace `rpev.max_parallel_executors` reference with `rpev.max_pool_slots`:
- Change the line `- \`rpev.max_parallel_executors\`: Maximum concurrent executor subagents per wave (default: 3)` to:
  `- \`rpev.max_pool_slots\`: Maximum concurrent agent slots in the pool -- all agent types share this limit (default: 3)`
- Remove any other references to `max_parallel_executors`

**Phase 3: Wave Execution section:**

In "### Wave Identification":
Change "3. Cap parallel executors at `rpev.max_parallel_executors` per wave" to:
"3. Cap parallel agents at `rpev.max_pool_slots` -- all Task tool calls go through the orchestrator's Pool Manager"

In "### Wave N Processing":
Replace steps 1-3 with pool-mediated dispatch:
```
1. Dispatch wave tasks via the Pool Manager Protocol (defined in synapse-orchestrator.md):
   - For each task in the wave, assign to an available pool slot
   - Pool slots are capped at max_pool_slots (default 3)
   - If more tasks than slots: excess tasks queue and dispatch as slots free up
   - Each executor subagent spawned via Task tool with isolation: "worktree"
   - Include SYNAPSE HANDOFF block in each Task prompt
   - The Pool Manager handles slot assignment, not this workflow
2. As each executor completes:
   - Pool Manager captures token usage from Task result
   - Pool Manager frees the slot and runs dispatch tick
   - Dispatch tick assigns next queued task (or validator per finish-first policy)
3. Finish-first policy: when a task completes, its Validator gets the next available slot before any new execution starts
4. After all wave tasks are executed AND validated: proceed to next wave
```

Keep steps 4-10 (renumbered to 5-11) largely unchanged, but ensure no references to "Issue all Task tool calls in a single turn" remain.

**Subagent Constraints section (bottom of file):**
Add a new bullet: "- All Task tool calls are mediated by the Pool Manager -- the orchestrator never issues Task calls directly outside the pool dispatch loop"

Do NOT change the Stage Document Schema, Involvement Resolution, Phase 0-2, Phase 4-5, or Session Resume sections -- those are unrelated to pool dispatch.
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "max_pool_slots" packages/framework/workflows/pev-workflow.md && grep -c "max_parallel_executors" packages/framework/workflows/pev-workflow.md | grep "^0$" && grep -c "Pool Manager" packages/framework/workflows/pev-workflow.md && grep -c "finish-first" packages/framework/workflows/pev-workflow.md</automated>
  </verify>
  <done>pev-workflow.md uses max_pool_slots terminology, references Pool Manager Protocol for dispatch, includes finish-first policy in wave execution, has no remaining max_parallel_executors references. Configuration, Wave Identification, and Wave N Processing sections all updated.</done>
</task>

</tasks>

<verification>
1. `grep -r "max_parallel_executors" packages/framework/` returns zero matches -- completely replaced
2. `grep -r "max_pool_slots" packages/framework/` returns matches in trust.toml, synapse-startup.js, synapse-orchestrator.md, pev-workflow.md
3. synapse-orchestrator.md contains "Pool Manager Protocol", "dispatch tick", "pool-state", "tokens_used", "finish-first"
4. pev-workflow.md contains "Pool Manager", "finish-first", "max_pool_slots"
5. synapse-startup.js contains "max_pool_slots" injection line
</verification>

<success_criteria>
- trust.toml has max_pool_slots=3, no max_parallel_executors
- synapse-startup.js injects max_pool_slots into session context
- synapse-orchestrator.md has complete Pool Manager Protocol (dispatch loop, priority queue, finish-first, cross-epic fill, pool state document, session recovery, token capture)
- pev-workflow.md delegates wave execution to pool dispatch
- Zero references to max_parallel_executors in the entire packages/framework/ tree
</success_criteria>

<output>
After completion, create `.planning/phases/21-agent-pool/21-01-SUMMARY.md`
</output>
