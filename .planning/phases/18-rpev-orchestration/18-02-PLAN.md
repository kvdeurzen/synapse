---
phase: 18-rpev-orchestration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/framework/agents/synapse-orchestrator.md
  - packages/framework/workflows/pev-workflow.md
autonomous: true
requirements: [RPEV-01, RPEV-03, RPEV-04, RPEV-05, RPEV-08]

must_haves:
  truths:
    - "The orchestrator agent spec describes the full RPEV flow: Refine->Plan->Execute->Validate"
    - "The workflow document defines the stage document schema and lifecycle"
    - "The orchestrator uses the involvement matrix to determine behavior at each stage transition"
    - "Failed items appear in /synapse:status with a flag per the failure escalation protocol"
    - "Readiness from Refine triggers Plan stage via stage document creation"
    - "Decision state from Refine persists via stage documents and feeds into Plan stage"
  artifacts:
    - path: "packages/framework/agents/synapse-orchestrator.md"
      provides: "RPEV orchestrator agent with involvement matrix logic, stage transitions, and failure escalation"
      contains: "RPEV"
    - path: "packages/framework/workflows/pev-workflow.md"
      provides: "RPEV workflow spec with stage document schema, Refine stage, and involvement enforcement"
      contains: "Stage Document"
  key_links:
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "packages/framework/workflows/pev-workflow.md"
      via: "@packages/framework/workflows/pev-workflow.md reference"
      pattern: "pev-workflow"
    - from: "packages/framework/agents/synapse-orchestrator.md"
      to: "packages/framework/config/trust.toml"
      via: "Involvement matrix injected by startup hook into session context"
      pattern: "involvement"
    - from: "packages/framework/workflows/pev-workflow.md"
      to: "store_document with doc_id rpev-stage-[task_id]"
      via: "Stage document creation and update"
      pattern: "rpev-stage"
---

<objective>
Update the synapse-orchestrator agent spec and pev-workflow.md to implement the full RPEV model: add Refine as a tracked stage, define the stage document schema, implement involvement matrix enforcement, and update failure escalation with status flags.

Purpose: This is the core RPEV engine. The orchestrator needs to know how to transition between stages, when to seek user approval, and how to track RPEV state via documents. Without this, the commands (refine/status/focus) have no engine beneath them.

Output: Updated synapse-orchestrator.md with RPEV flow and involvement logic; updated pev-workflow.md with Refine stage, stage document schema, and involvement enforcement.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-rpev-orchestration/18-CONTEXT.md
@.planning/phases/18-rpev-orchestration/18-RESEARCH.md

<interfaces>
<!-- Key existing interfaces the executor needs -->

From packages/framework/agents/synapse-orchestrator.md (current frontmatter):
```yaml
name: synapse-orchestrator
tools: Read, Write, Bash, Glob, Grep, Task, SendMessage, mcp__synapse__create_task, mcp__synapse__update_task, mcp__synapse__get_task_tree, mcp__synapse__store_decision, mcp__synapse__query_decisions, mcp__synapse__check_precedent, mcp__synapse__get_smart_context, mcp__synapse__project_overview
model: opus
color: purple
```
Note: `mcp__synapse__store_document` and `mcp__synapse__link_documents` are NOT in the current tools list and MUST be added for stage document management.

From packages/framework/workflows/pev-workflow.md:
Currently has 5 phases: Goal Intake, Progressive Decomposition, Wave Execution, Failure Escalation, Epic Completion.
Phase 1 trigger: "A user describes a goal (natural language or /synapse:new-goal command)" -- needs updating to reference /synapse:refine.

From 18-RESEARCH.md, the stage document schema:
```
doc_id: "rpev-stage-[task_id]"
category: "plan"
tags: "|rpev-stage|[level]|[stage_lowercase]|"
content (JSON):
  stage: REFINING | PLANNING | EXECUTING | VALIDATING | DONE
  level: project | epic | feature | work_package
  task_id: [task_id]
  involvement: [resolved involvement mode]
  pending_approval: true | false
  proposal_doc_id: [doc_id of proposal if pending]
  last_updated: [ISO timestamp]
```

From 18-CONTEXT.md, involvement resolution:
```
resolved_mode = get_involvement_mode(level, stage, domain_overrides)
drives    → wait for user
co-pilot  → present proposal, wait for approval
reviews   → execute, then surface for review
autopilot → execute, advance stage
monitors  → execute, notify, advance stage
```

Mode strictness ordering (most to least user involvement):
drives > co-pilot > reviews > monitors > autopilot

Retry caps from trust.toml: task=3, feature=2, epic=1
Auto-escalate: task fails -> feature-level, feature fails -> epic-level, epic fails -> stop + flag
Keep successful work on partial wave failure -- only retry the failed task
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pev-workflow.md to RPEV workflow with stage documents</name>
  <files>packages/framework/workflows/pev-workflow.md</files>
  <action>
Rewrite `packages/framework/workflows/pev-workflow.md` to become the RPEV workflow document. This is a full rewrite — the file keeps its name for backward compatibility but the content transitions from PEV to RPEV.

**Structure of the rewritten file:**

1. **Header and intro**: Change title to "Refine-Plan-Execute-Validate (RPEV) Workflow". Note that this is the authoritative workflow document read by the synapse-orchestrator agent.

2. **Configuration section**: Update to reference `[rpev]` and `[rpev.involvement]` sections of trust.toml instead of `[pev]`. List the same config keys but under the rpev namespace. Add the involvement matrix reference.

3. **Stage Document Schema** (NEW SECTION): Define the canonical stage document schema that all RPEV stage tracking uses. This is the single source of truth:
   - `doc_id`: `"rpev-stage-[task_id]"` (fixed pattern for versioning via store_document upsert)
   - `category`: `"plan"`
   - `tags`: `"|rpev-stage|[level]|[stage_lowercase]|"`
   - `status`: `"active"`
   - `content` (JSON string): `{ stage, level, task_id, involvement, pending_approval, proposal_doc_id, last_updated, notes }`
   - Include a concrete example using `store_document` tool call syntax
   - Explain: fixed doc_id enables versioning (new version, not duplicate); `pending_approval` is what `/synapse:status` queries; `proposal_doc_id` lets `/synapse:focus` load proposals

4. **Involvement Resolution** (NEW SECTION): Define the algorithm for resolving involvement mode:
   - Read `rpev.involvement` matrix (injected into session context by startup hook)
   - Look up `{level}_{stage}` key
   - Check `rpev.domain_overrides` for matching domain tags
   - Mode ordering: drives(5) > co-pilot(4) > reviews(3) > monitors(2) > autopilot(1)
   - Take the strictest (highest rank) between base and any domain override
   - Document the behavior for each mode (from CONTEXT.md locked decisions):
     - `drives`: Set stage doc pending_approval=true, status="waiting-for-user". Do not proceed.
     - `co-pilot`: Execute the stage work (e.g., run Decomposer), store proposal as document, set stage doc pending_approval=true with proposal_doc_id. Wait for user approval.
     - `reviews`: Execute the stage work, set stage doc pending_approval=true with output as proposal. User reviews after completion.
     - `autopilot`: Execute the stage work, advance to next stage. No user involvement.
     - `monitors`: Execute the stage work, advance to next stage, set notes="monitoring" in stage doc. User can intervene via /synapse:focus.

5. **Phase 0: Refine** (NEW PHASE — renumber existing phases):
   - Refine is handled by `/synapse:refine` command (not the orchestrator directly)
   - When refine completes with readiness confirmed, `/synapse:refine` creates/updates the stage document:
     - `stage: "REFINING"` -> `"PLANNING"` (transition)
     - `pending_approval`: set based on involvement mode for `{level}_plan`
   - Decisions from Refine (DECIDED items stored via `store_decision`) are automatically available to the Plan stage via `check_precedent` and `get_smart_context`
   - If involvement mode for refine is "drives" or "co-pilot" at the current level, user must explicitly confirm readiness (this is enforced by `/synapse:refine` step 7)

6. **Phase 1: Goal Intake and Epic Creation** (UPDATED):
   - Remove reference to "/synapse:new-goal" — replace with: "The RPEV workflow begins when /synapse:refine confirms readiness for an item, or when the orchestrator detects a stage document with stage=PLANNING"
   - Add: "On session start, check for stage documents in PLANNING state via query_documents with tag |rpev-stage| — these are items ready for planning"
   - Add step: Create stage document (doc_id: rpev-stage-[task_id]) when entering this phase

7. **Phases 2-5** (Progressive Decomposition, Wave Execution, Failure Escalation, Epic Completion):
   - Keep the existing logic largely intact — it is correct
   - Add involvement matrix checks at each approval point instead of the old `approval_threshold` check:
     - Replace `if approval_threshold = "epic"` with: "Resolve involvement mode for {level}_plan. If co-pilot or drives: present for user approval. If reviews: execute then present. If autopilot/monitors: proceed."
   - Add stage document updates at each transition:
     - Entering planning: `stage: "PLANNING"`
     - Entering execution: `stage: "EXECUTING"`
     - Entering validation: `stage: "VALIDATING"`
     - Completion: `stage: "DONE"`
   - In Failure Escalation (Phase 4), add: "Failed items are flagged via stage document: set notes to failure summary, set pending_approval=true if user intervention needed. These appear in /synapse:status as flagged items."
   - Auto-escalation to parent: "When retries exhausted at task level, escalate to feature level. When feature retries exhausted, escalate to epic level. When epic retries exhausted, set stage doc pending_approval=true with notes='retries exhausted — needs user guidance'. This shows in /synapse:status."
   - Add: "Keep successful work on partial wave failure — only retry the failed task. If a revised plan invalidates earlier work, that is handled through replanning, not pre-emptive rollback."

8. **Session Resume** (UPDATED): "On session start, the orchestrator queries for stage documents with tag |rpev-stage| to find items in progress. Resume from the current stage of each active item."

Keep the file readable and well-organized. Use markdown headers for each section. Include the concrete `store_document` call example in the Stage Document Schema section.
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "RPEV\|rpev" packages/framework/workflows/pev-workflow.md && grep -c "Stage Document\|rpev-stage\|store_document" packages/framework/workflows/pev-workflow.md && grep -c "involvement" packages/framework/workflows/pev-workflow.md && echo "All keywords present"</automated>
  </verify>
  <done>pev-workflow.md contains: RPEV title and terminology, Stage Document Schema section with concrete example, Involvement Resolution section with mode behaviors, Refine as Phase 0, updated Phase 1 trigger (no new-goal reference), involvement matrix checks replacing approval_threshold at all decision points, stage document updates at every transition, failure escalation with status flags, and session resume via stage document queries.</done>
</task>

<task type="auto">
  <name>Task 2: Update synapse-orchestrator.md for RPEV flow</name>
  <files>packages/framework/agents/synapse-orchestrator.md</files>
  <action>
Update `packages/framework/agents/synapse-orchestrator.md` to implement the RPEV model. This is a significant update to the agent spec but preserves the file structure.

**Frontmatter changes:**
- Add `mcp__synapse__store_document` and `mcp__synapse__link_documents` to the `tools:` list (needed for stage document management)
- Add `mcp__synapse__query_documents` to the `tools:` list (needed for querying stage documents on session start)

**Content changes:**

1. **Opening paragraph**: Change "You translate user goals into structured work streams" to include RPEV: "You coordinate the Refine-Plan-Execute-Validate (RPEV) workflow. You translate user goals into structured work streams, manage stage transitions using the involvement matrix, and track RPEV state via stage documents."

2. **Core Responsibilities** — Update list:
   - Keep items 1-5
   - Add: "6. **RPEV Stage Management:** Track item state via stage documents, enforce involvement matrix, manage stage transitions"
   - Add: "7. **Failure Escalation:** Coordinate debugging, retries, and escalation per the RPEV failure protocol"

3. **Session Startup Protocol** — Add step between current steps 1 and 2:
   - "1b. Check for active RPEV items: Call `mcp__synapse__query_documents` with category `"plan"`, tag `"rpev-stage"` to find items in RPEV flow. For each active stage document, note the current stage and whether it needs attention (pending_approval=true)."
   - Update step 3 to include: "RPEV items in progress (with current stage)" and "Items needing your input (pending_approval=true)"
   - Update step 4: "If RPEV items need attention, present them first. If work is in progress, offer to continue. If nothing active, ask what the user wants to work on."

4. **Approval Tiers** section: Replace entirely with **Involvement Matrix** section:
   ```markdown
   ## Involvement Matrix

   The RPEV involvement matrix (injected into your context by the startup hook) controls your behavior at each stage transition. See @packages/framework/workflows/pev-workflow.md for the full Involvement Resolution algorithm.

   **Resolving your behavior:**
   1. Identify the item's level (project/epic/feature/work_package) and current stage (refine/plan/execute/validate)
   2. Read the matrix: `{level}_{stage}` gives the base mode
   3. Check domain overrides: if the item has domain tags matching an override, take the stricter mode
   4. Act according to the resolved mode:
      - **drives**: Do not proceed — set stage doc waiting, present in /synapse:status
      - **co-pilot**: Execute the stage work, present proposal for approval, wait
      - **reviews**: Execute the stage work, present output for review, wait
      - **autopilot**: Execute the stage work, advance to next stage, no user involvement
      - **monitors**: Execute the stage work, advance, leave brief notification in stage doc
   ```

5. **PEV Workflow section** — Rename to **RPEV Workflow** and update reference:
   - "See `@packages/framework/workflows/pev-workflow.md` for the authoritative RPEV workflow document."
   - "Your role in RPEV: You are the central coordinator. You manage stage transitions, spawn all subagents, and enforce the involvement matrix."
   - Keep the agent roles list but add a note: "The Refine stage is handled by `/synapse:refine` — it is not an orchestrator-spawned subagent."

6. **Progressive Decomposition Protocol** — Update all approval checks:
   - Replace `If approval_threshold = "epic"` with: "Resolve involvement mode for the item's `{level}_plan`. If mode is `drives` or `co-pilot`: present for user approval before proceeding. If `reviews`: proceed with decomposition, then present. If `autopilot` or `monitors`: proceed without user involvement."
   - Add after each decomposition step: "Update stage document: set stage to PLANNING, record involvement mode."

7. **Wave Execution Protocol** — Add stage document updates:
   - Before wave execution: "Update stage document: set stage to EXECUTING"
   - After feature completion: "Update stage document: set stage to VALIDATING"
   - After validation passes: "Update stage document: set stage to DONE"

8. **Failure Escalation Protocol** — Add status flag behavior per CONTEXT.md:
   - After "Track retry count per task": "Update stage document notes with failure summary. If retries exhausted, set pending_approval=true — this surfaces in /synapse:status as a flagged item."
   - In Feature-Level Failure: "When user focuses on a failed item via /synapse:focus, they see the Debugger's diagnostic report plus structured options: Retry with guidance / Redefine the task / Skip and continue / Escalate to parent level."
   - Add auto-escalation: "Auto-escalate to parent when retries exhausted: task -> feature-level retry, feature -> epic-level retry, epic -> stop and flag for user."
   - Add: "Keep successful work on partial wave failure — only retry the failed task."

9. **Stage Document Management** (NEW SECTION before Checkpoint Format):
   ```markdown
   ## Stage Document Management

   Every item in the RPEV flow gets a stage document. This is the single source of truth for RPEV state.

   **Creating/updating:** Use `mcp__synapse__store_document` with:
   - `doc_id`: `"rpev-stage-[task_id]"` (fixed — enables versioning)
   - `category`: `"plan"`
   - `tags`: `"|rpev-stage|[level]|[stage_lowercase]|"`
   - `content`: JSON with stage, level, task_id, involvement, pending_approval, proposal_doc_id, last_updated, notes

   **Querying:** Use `mcp__synapse__query_documents` with tag `"rpev-stage"` to find all active items.

   **Key rule:** The doc_id is always `rpev-stage-[task_id]`. Using store_document with the same doc_id creates a new version — no duplicates.
   ```

10. **Subagent Handoff Protocol** (NEW SECTION — RPEV-04 decision persistence):
    ```markdown
    ## Subagent Handoff Protocol

    Every Task tool call to a subagent MUST include:
    - `project_id`: from session context
    - `task_id`: the specific task to work on
    - `rpev_stage_doc_id`: `"rpev-stage-[task_id]"` so the subagent can fetch stage context
    - Relevant decision context: "Key decisions constraining this task: [list from check_precedent]"

    This ensures subagents have full context without searching. Decision state from Refine (stored via store_decision during /synapse:refine sessions) is automatically available to subagents via get_smart_context.
    ```

Do NOT change: the model (opus), the color (purple), or the Checkpoint Format section content.
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && grep -c "store_document\|link_documents\|query_documents" packages/framework/agents/synapse-orchestrator.md && grep -c "Involvement Matrix\|involvement" packages/framework/agents/synapse-orchestrator.md && grep -c "rpev-stage\|Stage Document" packages/framework/agents/synapse-orchestrator.md && grep "mcp__synapse__store_document" packages/framework/agents/synapse-orchestrator.md | head -1 && echo "All RPEV keywords present"</automated>
  </verify>
  <done>synapse-orchestrator.md contains: RPEV terminology throughout, mcp__synapse__store_document/link_documents/query_documents in frontmatter tools list, Involvement Matrix section replacing Approval Tiers, Stage Document Management section, Subagent Handoff Protocol, updated session startup with stage document queries, involvement matrix checks at all approval points, failure escalation with status flags, and auto-escalation protocol.</done>
</task>

</tasks>

<verification>
1. pev-workflow.md contains "RPEV" in title, Stage Document Schema section, Involvement Resolution section, Phase 0 (Refine), and no references to "/synapse:new-goal"
2. synapse-orchestrator.md frontmatter includes store_document, link_documents, query_documents tools
3. synapse-orchestrator.md contains Involvement Matrix section, Stage Document Management section, Subagent Handoff Protocol
4. Both files reference rpev-stage-[task_id] doc_id pattern consistently
5. No references to old `approval_threshold` or `[pev]` section remain in either file
</verification>

<success_criteria>
- The RPEV workflow document defines the canonical stage document schema with concrete tool call example
- The involvement resolution algorithm is documented with all 5 modes and their behaviors
- The orchestrator agent spec uses the involvement matrix at every approval decision point
- Failed items are flagged via stage documents (pending_approval + notes) for /synapse:status to surface
- Refine completion triggers Plan via stage document transition (RPEV-01)
- Decision state from Refine persists via store_decision and is available to Plan via get_smart_context (RPEV-04)
- No references to the old PEV-only model remain (no /synapse:new-goal, no approval_threshold)
</success_criteria>

<output>
After completion, create `.planning/phases/18-rpev-orchestration/18-02-SUMMARY.md`
</output>
