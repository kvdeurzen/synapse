---
phase: 16-user-journey-commands
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/framework/commands/synapse/focus.md
  - docs/user-journey.md
autonomous: true
requirements: [CMD-04]

must_haves:
  truths:
    - "/synapse:focus navigates to a specific item by name (semantic fuzzy match) or path shorthand (2.3.1)"
    - "A user journey document describes the complete flow from install to ongoing use as step-by-step instructions"
  artifacts:
    - path: "packages/framework/commands/synapse/focus.md"
      provides: "/synapse:focus slash command for item navigation"
      contains: "synapse:focus"
    - path: "docs/user-journey.md"
      provides: "User journey documentation (CMD-04)"
      contains: "synapse:init"
  key_links:
    - from: "packages/framework/commands/synapse/focus.md"
      to: "mcp__synapse__semantic_search"
      via: "Fuzzy name matching for item navigation"
      pattern: "mcp__synapse__semantic_search"
    - from: "packages/framework/commands/synapse/focus.md"
      to: "mcp__synapse__get_task_tree"
      via: "Path shorthand resolution (positional index into priority-ordered tree)"
      pattern: "mcp__synapse__get_task_tree"
    - from: "docs/user-journey.md"
      to: "packages/framework/commands/synapse/"
      via: "References all five commands in the user flow"
      pattern: "synapse:"
---

<objective>
Create the `/synapse:focus` navigation command and the user journey documentation describing the complete flow from install to ongoing use.

Purpose: `/synapse:focus` lets users navigate to specific items in their project hierarchy — by fuzzy name search or structural path shorthand (2.3.1). The user journey document (CMD-04) ties everything together: install, init, map, refine, status, focus — giving a new user a clear step-by-step path from zero to running their first RPEV workflow.

Output: One command file (focus.md) and one documentation file (docs/user-journey.md).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-user-journey-commands/16-CONTEXT.md
@.planning/phases/16-user-journey-commands/16-RESEARCH.md
@.planning/brainstorm output/recursive-rpev-model.md

@packages/framework/commands/synapse/status.md

<interfaces>
<!-- Existing command pattern: -->
Frontmatter: name, description, allowed-tools list
Body: ## Objective, ## Process (numbered bold-named steps), ## Attribution

<!-- semantic_search MCP tool: -->
Parameters: project_id, query, category (optional), limit, actor
Returns: { success: true, data: { results: [{ doc_id, title, content, score, ... }] } }

<!-- get_task_tree MCP tool: -->
Parameters: project_id, task_id (optional root), depth (optional)
Returns: { success: true, data: { tasks: [{ task_id, title, depth, status, priority, children: [...] }], rollup: { total, done, in_progress, blocked } } }

<!-- get_smart_context MCP tool: -->
Parameters: project_id, query, mode ("overview"|"focused"), actor
Returns: { success: true, data: { documents: [...], decisions: [...], code: [...] } }

<!-- check_precedent MCP tool: -->
Parameters: project_id, query, actor
Returns: { success: true, data: { precedents: [...], has_precedent: boolean } }

<!-- Command set (five commands per locked decision): -->
1. /synapse:init — Project setup with RPEV configuration
2. /synapse:map — Codebase indexing with Ollama health check
3. /synapse:refine — Primary user interaction (brainstorm + decision tracking)
4. /synapse:status — RPEV dashboard with blocked items
5. /synapse:focus — Navigate to specific items (this plan)

<!-- RPEV hierarchy: Project > Epic > Feature > Work Package -->
<!-- Path shorthand: 2.3.1 = Epic 2 (by priority), Feature 3 (by priority), WP 1 (by priority) -->
<!-- Agent-based focus (/synapse:focus agent C) is DEFERRED to Agent Pool phase -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /synapse:focus command</name>
  <files>packages/framework/commands/synapse/focus.md</files>
  <action>
Create `packages/framework/commands/synapse/focus.md` — the navigation command that lets users jump to specific items in their project hierarchy.

**Frontmatter:**
```yaml
name: synapse:focus
description: Navigate to a specific item in the project hierarchy — by name (fuzzy search) or path shorthand (e.g., 2.3.1 for Epic 2, Feature 3, Work Package 1).
allowed-tools:
  - Read
  - mcp__synapse__get_task_tree
  - mcp__synapse__semantic_search
  - mcp__synapse__get_smart_context
  - mcp__synapse__check_precedent
  - mcp__synapse__store_document
  - mcp__synapse__store_decision
  - mcp__synapse__create_task
```

Note: The allowed-tools list is broad because once focused on an item, the user may want to make decisions, refine, or create sub-items — focus acts as a gateway to working on that item.

**Objective:** "Navigate to a specific item in the project hierarchy and present its full context — status, decisions, related documents, and actionable options."

**Process steps:**

1. **Parse input:** The user provides either:
   - **Name-based**: `/synapse:focus "JWT token refresh"` or `/synapse:focus auth strategy`
   - **Path shorthand**: `/synapse:focus 2.3.1` (digits separated by dots)
   - **No argument**: Ask "What would you like to focus on? You can use a name (fuzzy matched) or a path like 2.3 (Epic 2, Feature 3)."

   Detect which mode by checking if the argument matches `/^\d+(\.\d+)*$/` (path shorthand) or is text (name-based).

2. **Resolve item — Path shorthand mode:**
   - Call `mcp__synapse__get_task_tree` to get the full task tree
   - Sort epics by priority (or creation order if no priority set)
   - Parse path: `2.3.1` means the 2nd epic in priority order, its 3rd child (feature), that feature's 1st child (work package)
   - If the index is out of range: "Path 2.3.1 doesn't match — Epic 2 only has 2 features. Here's what's available:" then show the tree section
   - Note in output: "Positions reflect current priority order and may change if items are reprioritized."

3. **Resolve item — Name-based mode:**
   - Call `mcp__synapse__semantic_search` with the user's text as query, limit 5
   - Also call `mcp__synapse__get_task_tree` and search task titles for partial matches
   - Cross-reference: prefer task tree matches (they have hierarchy context) over document-only matches
   - If multiple matches: present them ranked by relevance and let the user choose
   - If no match: "No items found matching '[query]'. Here's what exists:" then show the top-level epic list from status

4. **Present item context:** Once the item is resolved, display a comprehensive view:
   ```
   ## Focus: [Item Title]

   **Level:** [Epic/Feature/Work Package]
   **Status:** [RPEV stage — Refining/Planning/Executing/Done]
   **Priority:** [N] of [total at this level]
   **Parent:** [Parent item title] (if not top-level)

   ### Progress
   [If has children:]
   - Children: X/Y complete
   - Blocked: Z items
   [If leaf:]
   - Status: [current status]

   ### Decisions
   [Relevant decisions from check_precedent and get_smart_context]
   - [Decision title] (Tier N) — [brief rationale]

   ### Related Documents
   [From get_smart_context focused mode]
   - [Document title] ([category])

   ### Open Questions
   [If refinement session exists with OPEN items:]
   - [Open question from refinement state]
   ```

5. **Offer contextual actions:** Based on item state, suggest appropriate next steps:
   - **If item is blocked:** "This item needs a decision. Would you like to explore the options?" Then start an inline refinement session (using the same brainstorming approach as /synapse:refine).
   - **If item is in Refining state:** "Resume refinement? (`/synapse:refine "[title]"`)"
   - **If item is in Planning/Executing:** "This item is being handled by the system. Check `/synapse:status` for overall progress."
   - **If item is Done:** "This item is complete. Review its validation results?"
   - **If item doesn't exist yet but user described it:** "This doesn't exist yet. Would you like to create it as a new [epic/feature/work package]?"

6. **Handle blocked items (decision engagement):** When the focused item needs a decision:
   - Present the decision context clearly:
     ```
     ### Decision Needed: [Decision topic]

     **Context:** [Why this decision matters at this level]
     **Options explored:** [If any from prior sessions]

     What's your thinking on this?
     ```
   - Adapt engagement to decision complexity (per locked decision on focus UX):
     - Binary decisions: Present options directly ("Option A: X, Option B: Y — which direction?")
     - Open-ended decisions: Start conversational engagement ("What factors are most important here?")
   - Store any decision made via `mcp__synapse__store_decision`

**Deferred features (explicitly note in command):**
- Agent-based focus (`/synapse:focus agent C`) — deferred to Agent Pool phase. If user tries this pattern, respond: "Agent-based focus will be available when the Agent Pool is active (Phase 21). For now, use item names or path shorthand."

**Anti-patterns:**
- Do NOT show raw task_ids to the user — show titles and path shorthand
- Do NOT assume path shorthand positions are stable across sessions (they reflect current priority order)
- Always include `actor: "synapse-orchestrator"` in MCP calls
  </action>
  <verify>
    <automated>test -f packages/framework/commands/synapse/focus.md && head -5 packages/framework/commands/synapse/focus.md | grep -q "synapse:focus" && grep -q "semantic_search" packages/framework/commands/synapse/focus.md && grep -q "path shorthand" packages/framework/commands/synapse/focus.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - focus.md exists with correct frontmatter (name, description, allowed-tools including semantic_search and get_task_tree)
    - Process covers: input parsing (name vs path shorthand), path shorthand resolution with priority-ordered indexing, name-based fuzzy search via semantic_search, item context display, contextual action suggestions, blocked item decision engagement
    - Agent-based focus explicitly noted as deferred
    - Attribution section present
  </done>
</task>

<task type="auto">
  <name>Task 2: Write user journey documentation</name>
  <files>docs/user-journey.md</files>
  <action>
Create `docs/user-journey.md` — the complete step-by-step guide from install to ongoing use (CMD-04). First create the `docs/` directory with `mkdir -p docs`.

This document describes the user journey for a developer adopting Synapse, covering both use cases: starting a new project and adding Synapse to an existing project. Written based on the RPEV model vision.

**Document structure:**

```markdown
# Synapse User Journey

A step-by-step guide from installation to running your first RPEV workflow.

## Prerequisites

- **Bun** (runtime): [install link]
- **Ollama** (local embeddings): [install link]
  - Pull embedding model: `ollama pull nomic-embed-text`
- **Claude Code** (AI assistant): [install link]

## Step 1: Install Synapse

[Reference to install.sh from Phase 17 — for now, manual setup instructions]

Run the install script from the Synapse repository:
```bash
bash install.sh
```

This copies agents, hooks, commands, and configuration files into your project's `.claude/` directory and creates `.synapse/config/`.

## Step 2: Initialize Your Project

```
/synapse:init
```

What happens:
1. Synapse detects your project name (from package.json or directory name)
2. You confirm or adjust the project ID
3. A `.synapse/config/project.toml` is created with your project identity
4. You walk through RPEV preferences (how involved you want to be at each level)
5. Synapse registers the project in its database
6. Optionally, a `## Synapse` section is added to your CLAUDE.md

After init, every Claude Code session automatically loads your project context.

## Step 3: Map Your Codebase

```
/synapse:map
```

What happens:
1. Synapse checks that Ollama is running and has the embedding model
2. Your codebase is scanned: source files parsed, AST structures extracted, embeddings generated
3. A summary shows how many files and symbols were indexed

After mapping, agents can find relevant code and documentation using semantic search. Re-run `/synapse:map` periodically as your codebase evolves.

## Step 4: Start Your First Epic

```
/synapse:refine
```

Describe what you want to build. Synapse acts as a thinking partner:
- Asks clarifying questions to understand your vision
- Surfaces related decisions from your project history
- Tracks decisions as DECIDED, OPEN, or EMERGING
- Checks readiness criteria for your hierarchy level

Example session:
```
> /synapse:refine "Add user authentication"

Synapse: "Let's explore that. What kind of authentication does your app need?
  Are we talking about username/password, OAuth, magic links, or something else?"

You: "OAuth with Google and GitHub, plus email magic links as fallback"

Synapse: "Good. That gives us three auth strategies. Let me check if there are
  any prior decisions about auth in this project..."

[Decision stored: "Auth strategies: OAuth (Google, GitHub) + email magic links" — Tier 1]
```

At Project and Epic levels, YOU decide when the foundation is solid enough to move to planning. At Feature and Work Package levels, the transition can be lighter based on your trust config.

## Step 5: Check Your Dashboard

```
/synapse:status
```

See everything at a glance:
- Epics in priority order with their RPEV stage
- Feature progress within each epic
- Blocked items that need your input
- Recent decisions and suggested next actions

The dashboard embodies the "system drives, user unblocks" philosophy — the system handles execution, and you handle the decisions that need human judgment.

## Step 6: Navigate and Unblock

```
/synapse:focus "JWT token refresh"
/synapse:focus 2.3.1
```

Jump to any item by name (fuzzy matched) or path shorthand. When you focus on a blocked item, Synapse presents the decision context and helps you work through it.

## Ongoing Use

### The RPEV Rhythm

Your daily interaction follows a natural rhythm:

1. **Check status** (`/synapse:status`) — see what's happening and what needs you
2. **Unblock items** (`/synapse:focus` on blocked items) — make the decisions only you can make
3. **Shape the next thing** (`/synapse:refine`) — brainstorm the next epic while current work executes
4. **Repeat**

### Key Concepts

- **Recursive RPEV**: Every layer (Project > Epic > Feature > Work Package) goes through Refine-Plan-Execute-Validate
- **Decisions only in Refine + Plan**: Execution follows the plan; if something unexpected comes up, it escalates
- **Everything is persisted**: All decisions, requirements, and refinement state live in the Synapse database — queryable by any agent, survivable across sessions
- **Trust config**: You choose how involved you are at each level. More trust = more autonomy for agents. Less trust = more checkpoints for you.

### Command Reference

| Command | Purpose | When to use |
|---------|---------|-------------|
| `/synapse:init` | Set up project | Once, at the start |
| `/synapse:map` | Index codebase | After init, and periodically as code changes |
| `/synapse:refine` | Brainstorm and shape work | Whenever you're defining or exploring something |
| `/synapse:status` | Project dashboard | To check progress and find blocked items |
| `/synapse:focus` | Navigate to specific item | To drill into or unblock a specific item |

### Two Starting Paths

**New project with Synapse:**
1. Create your project directory
2. Install Synapse (`bash install.sh`)
3. `/synapse:init` (project is empty, that's fine)
4. `/synapse:refine` to start shaping your first epic
5. `/synapse:map` when you have code to index

**Adding Synapse to an existing project:**
1. Install Synapse (`bash install.sh`)
2. `/synapse:init` (detects your existing project name)
3. `/synapse:map` (indexes your existing codebase)
4. `/synapse:refine` to define your next piece of work
5. `/synapse:status` to see the dashboard

---

*This document describes the v3.0 user journey. Some features (agent pool, proactive notifications) are marked as future enhancements.*
```

**Key principles for the document:**
- Written for the primary audience: a developer adopting Synapse (per CONTEXT.md locked decision)
- Covers both use cases: new project and existing project
- Step-by-step with concrete examples (not abstract concepts)
- References all five commands in their natural usage order
- Explains the RPEV model in user terms, not implementation terms
- Notes future features where relevant but doesn't over-promise
  </action>
  <verify>
    <automated>test -f docs/user-journey.md && grep -q "synapse:init" docs/user-journey.md && grep -q "synapse:map" docs/user-journey.md && grep -q "synapse:refine" docs/user-journey.md && grep -q "synapse:status" docs/user-journey.md && grep -q "synapse:focus" docs/user-journey.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - docs/user-journey.md exists with complete step-by-step flow
    - Covers: prerequisites, install, init, map, refine, status, focus, ongoing use
    - References all five commands in natural order
    - Explains RPEV concepts in user-friendly terms
    - Covers both starting paths (new project, existing project)
    - Command reference table included
    - Written for developer audience (per CONTEXT.md)
  </done>
</task>

</tasks>

<verification>
- `packages/framework/commands/synapse/focus.md` exists with name-based and path shorthand navigation
- `docs/user-journey.md` exists and references all five commands
- focus.md follows established frontmatter pattern with Attribution section
- user-journey.md covers both use cases (new project, existing project)
- Agent-based focus is explicitly noted as deferred in focus.md
</verification>

<success_criteria>
- `/synapse:focus` enables navigation by fuzzy name search and structural path shorthand (2.3.1)
- `/synapse:focus` shows item context, decisions, and offers contextual actions including blocked-item decision engagement
- User journey document provides a complete, step-by-step flow from install to ongoing use
- User journey document explains RPEV concepts in accessible terms
- Both artifacts reference the full five-command set consistently
</success_criteria>

<output>
After completion, create `.planning/phases/16-user-journey-commands/16-03-SUMMARY.md`
</output>
