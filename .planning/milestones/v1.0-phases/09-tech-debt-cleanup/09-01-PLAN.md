---
phase: 09-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-embedding-service/03-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - src/tools/delete-project.ts
autonomous: true
requirements:
  - Documentation accuracy

must_haves:
  truths:
    - "03-01-SUMMARY.md frontmatter includes requirements-completed listing EMBED-01, EMBED-02, EMBED-06"
    - "REQUIREMENTS.md DOC-01 description says 12 types, not 17 types"
    - "REQUIREMENTS.md FOUND-04 description says implementation patterns, not coding guidelines"
    - "delete_project tool description says 6 tables and lists doc_chunks"
  artifacts:
    - path: ".planning/phases/03-embedding-service/03-01-SUMMARY.md"
      provides: "requirements-completed frontmatter field"
      contains: "requirements-completed:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected DOC-01 and FOUND-04 descriptions"
      contains: "category (12 types)"
    - path: "src/tools/delete-project.ts"
      provides: "Accurate delete_project tool description"
      contains: "all 6 tables"
  key_links: []
---

<objective>
Fix four stale documentation items identified by the v1 milestone audit: add missing requirements-completed frontmatter to 03-01-SUMMARY.md, correct the DOC-01 category count and FOUND-04 starter document name in REQUIREMENTS.md, and fix the delete_project tool description table count in source code.

Purpose: Closes 5 tech debt items from the v1 audit so documentation accurately reflects implementation. No code behavior changes, no test changes needed.

Output: Three files edited with surgical text replacements — two planning documents and one source file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tech-debt-cleanup/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requirements-completed frontmatter to 03-01-SUMMARY.md</name>
  <files>.planning/phases/03-embedding-service/03-01-SUMMARY.md</files>
  <action>
    Read `.planning/phases/03-embedding-service/03-01-SUMMARY.md`. Confirm `requirements-completed:` is absent from the YAML frontmatter.

    Add the following block immediately BEFORE the closing `---` of the frontmatter (after the `metrics:` block which ends with `files_modified: 2`):

    ```yaml
    requirements-completed:
      - EMBED-01
      - EMBED-02
      - EMBED-06
    ```

    The old_string for the Edit tool should target:
    ```
      files_modified: 2
    ---
    ```

    Replace with:
    ```
      files_modified: 2
    requirements-completed:
      - EMBED-01
      - EMBED-02
      - EMBED-06
    ---
    ```

    **Evidence:** EMBED-01 (Ollama /api/embed call), EMBED-02 (single+batch embedding), and EMBED-06 (dimension assertion) are all verified as SATISFIED in `03-VERIFICATION.md` and attributed to Plan 03-01. EMBED-03, EMBED-04, EMBED-05 belong to Plan 03-02 — do NOT include them.

    **Pitfall:** Do NOT add a duplicate `requirements-completed:` section if one already exists. Read the file first to confirm absence.
  </action>
  <verify>Read the file and confirm the frontmatter now contains `requirements-completed:` with exactly EMBED-01, EMBED-02, EMBED-06</verify>
  <done>03-01-SUMMARY.md frontmatter contains requirements-completed field listing EMBED-01, EMBED-02, EMBED-06 — no other frontmatter fields are altered</done>
</task>

<task type="auto">
  <name>Task 2: Fix DOC-01 category count in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    In `.planning/REQUIREMENTS.md`, find the DOC-01 line (line 31):

    ```markdown
    - [x] **DOC-01**: User can store a document with title, content, category (17 types), and optional metadata via store_document
    ```

    Replace `(17 types)` with `(12 types)`:

    ```markdown
    - [x] **DOC-01**: User can store a document with title, content, category (12 types), and optional metadata via store_document
    ```

    **Evidence:** `src/tools/doc-constants.ts` VALID_CATEGORIES array has exactly 12 entries: architecture_decision, design_pattern, glossary, code_pattern, dependency, plan, task_spec, requirement, technical_context, change_record, research, learning.

    **Pitfall:** Do NOT change the `[x]` checkbox state. The old_string must include the full line to ensure uniqueness but the `- [x] **DOC-01**:` prefix must be preserved exactly.
  </action>
  <verify>Read REQUIREMENTS.md and confirm DOC-01 line says "(12 types)" and checkbox is still [x]</verify>
  <done>DOC-01 description says "category (12 types)" matching the actual VALID_CATEGORIES count — checkbox state unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Fix FOUND-04 starter document name in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    In `.planning/REQUIREMENTS.md`, find the FOUND-04 line (line 15):

    ```markdown
    - [x] **FOUND-04**: init_project seeds starter documents (project charter, ADR log template, coding guidelines, glossary)
    ```

    Replace `coding guidelines` with `implementation patterns`:

    ```markdown
    - [x] **FOUND-04**: init_project seeds starter documents (project charter, ADR log template, implementation patterns, glossary)
    ```

    **Evidence:** `04-CONTEXT.md` locked decision: "Coding guidelines replaced with Implementation Patterns — captures reusable technical decisions, not coding style rules." The init_project code was already implemented correctly with the right document name — this is purely a description accuracy fix.

    **Pitfall:** Do NOT change the `[x]` checkbox state. Preserve the `- [x] **FOUND-04**:` prefix exactly.
  </action>
  <verify>Read REQUIREMENTS.md and confirm FOUND-04 line says "implementation patterns" and checkbox is still [x]</verify>
  <done>FOUND-04 description says "implementation patterns" matching the 04-CONTEXT.md locked decision and actual implementation — checkbox state unchanged</done>
</task>

<task type="auto">
  <name>Task 4: Fix delete_project tool description table count</name>
  <files>src/tools/delete-project.ts</files>
  <action>
    In `src/tools/delete-project.ts`, find the tool description (lines 62-65):

    ```typescript
    description:
      "Delete all data for a project across all tables. " +
      "Removes rows matching the given project_id from all 5 tables " +
      "(documents, code_chunks, relationships, project_meta, activity_log).",
    ```

    Replace with:

    ```typescript
    description:
      "Delete all data for a project across all tables. " +
      "Removes rows matching the given project_id from all 6 tables " +
      "(documents, doc_chunks, code_chunks, relationships, project_meta, activity_log).",
    ```

    Two changes:
    1. `all 5 tables` → `all 6 tables`
    2. Add `doc_chunks` to the list (before `code_chunks`)

    **Evidence:** `src/db/schema.ts` TABLE_NAMES has 6 entries: documents, doc_chunks, code_chunks, relationships, project_meta, activity_log. The doc_chunks table holds embedded document chunk vectors and was added during Phase 4 but omitted from the tool description.

    **Pitfall:** This is a source code change (not just documentation), but it only changes a description string — no behavioral impact. No tests reference this exact string.
  </action>
  <verify>Read src/tools/delete-project.ts and confirm the description says "all 6 tables" and lists doc_chunks</verify>
  <done>delete_project tool description accurately says "6 tables" and lists all 6 table names including doc_chunks</done>
</task>

</tasks>

<verification>
1. Read `.planning/phases/03-embedding-service/03-01-SUMMARY.md` — frontmatter contains `requirements-completed:` with EMBED-01, EMBED-02, EMBED-06
2. Read `.planning/REQUIREMENTS.md` — DOC-01 says "(12 types)", FOUND-04 says "implementation patterns", both checkboxes remain [x]
3. Read `src/tools/delete-project.ts` — description says "all 6 tables" and lists "(documents, doc_chunks, code_chunks, relationships, project_meta, activity_log)"
4. Run `bun test` to confirm no regressions from the description string change:
   ```bash
   cd /home/kanter/code/project_mcp && bun test
   ```
5. Verify each ROADMAP Phase 9 success criterion:
   - SC1: 03-01-SUMMARY.md includes EMBED-01, EMBED-02, EMBED-06 in requirements-completed — verified by reading file
   - SC2: DOC-01 description says 12, not 17 — verified by reading REQUIREMENTS.md
   - SC3: FOUND-04 description says "implementation patterns" — verified by reading REQUIREMENTS.md
   - SC4: delete_project tool description says 6 tables — verified by reading source file
</verification>

<success_criteria>
- 03-01-SUMMARY.md frontmatter has `requirements-completed:` listing exactly EMBED-01, EMBED-02, EMBED-06
- REQUIREMENTS.md DOC-01 says "category (12 types)" (not 17)
- REQUIREMENTS.md FOUND-04 says "implementation patterns" (not "coding guidelines")
- src/tools/delete-project.ts description says "all 6 tables" and includes doc_chunks in the list
- `bun test` passes with 0 failures (no regressions from string change)
- No checkbox states changed in REQUIREMENTS.md
- No files created or deleted — only edits to 3 existing files
</success_criteria>

<output>
After completion, create `.planning/phases/09-tech-debt-cleanup/09-01-SUMMARY.md`
</output>
