---
phase: 05-document-search
plan: 04
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - src/server.ts
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - SRCH-07

must_haves:
  truths:
    - "All four search tools appear in MCP tools/list after server startup"
    - "Tool count incremented correctly for all four new tools"
    - "bun test passes with no regressions across entire test suite"
  artifacts:
    - path: "src/server.ts"
      provides: "Updated server with all 4 search tools registered"
      contains: "registerSemanticSearchTool, registerFulltextSearchTool, registerHybridSearchTool, registerGetSmartContextTool"
  key_links:
    - from: "src/server.ts"
      to: "src/tools/semantic-search.ts"
      via: "registerSemanticSearchTool import and call"
      pattern: "registerSemanticSearchTool"
    - from: "src/server.ts"
      to: "src/tools/fulltext-search.ts"
      via: "registerFulltextSearchTool import and call"
      pattern: "registerFulltextSearchTool"
    - from: "src/server.ts"
      to: "src/tools/hybrid-search.ts"
      via: "registerHybridSearchTool import and call"
      pattern: "registerHybridSearchTool"
    - from: "src/server.ts"
      to: "src/tools/get-smart-context.ts"
      via: "registerGetSmartContextTool import and call"
      pattern: "registerGetSmartContextTool"
---

<objective>
Wire all four new search tools into the MCP server and validate the complete Phase 5 integration. This is the final step that makes the search tools discoverable by MCP clients.

Purpose: Tools exist in isolation until registered in server.ts. This plan connects them to the MCP server and runs the full test suite to validate no regressions.
Output: Updated server.ts with 4 new tool registrations; full test suite passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-document-search/05-02-SUMMARY.md
@.planning/phases/05-document-search/05-03-SUMMARY.md

@src/server.ts

<interfaces>
<!-- Registration functions created in Plans 05-02 and 05-03 -->
```typescript
// From src/tools/semantic-search.ts
export function registerSemanticSearchTool(server: McpServer, config: SynapseConfig): void;

// From src/tools/fulltext-search.ts
export function registerFulltextSearchTool(server: McpServer, config: SynapseConfig): void;

// From src/tools/hybrid-search.ts
export function registerHybridSearchTool(server: McpServer, config: SynapseConfig): void;

// From src/tools/get-smart-context.ts
export function registerGetSmartContextTool(server: McpServer, config: SynapseConfig): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register search tools in server.ts and run full validation</name>
  <files>src/server.ts</files>
  <action>
  Update `src/server.ts` to import and register all four new search tools.

  **Add imports** (after existing tool imports):
  ```typescript
  import { registerSemanticSearchTool } from "./tools/semantic-search.js";
  import { registerFulltextSearchTool } from "./tools/fulltext-search.js";
  import { registerHybridSearchTool } from "./tools/hybrid-search.js";
  import { registerGetSmartContextTool } from "./tools/get-smart-context.js";
  ```

  **Add registrations** in `createServer()` after the existing `registerGetRelatedDocumentsTool` block (line ~67):
  ```typescript
  registerSemanticSearchTool(server, config);
  toolCount++;

  registerFulltextSearchTool(server, config);
  toolCount++;

  registerHybridSearchTool(server, config);
  toolCount++;

  registerGetSmartContextTool(server, config);
  toolCount++;
  ```

  This brings the total tool count from 11 to 15.

  **Validation steps (run in order):**
  1. `bun run build` (or `bun tsc --noEmit`) — confirm no TypeScript errors
  2. `bun test` — full test suite passes with no regressions
  3. Verify the tool registrations exist in server.ts by grep

  **What to AVOID:**
  - Do NOT change the order of existing tool registrations
  - Do NOT modify `startServer()` — only modify `createServer()`
  - Do NOT add any business logic — this is pure wiring
  </action>
  <verify>
    <automated>cd /home/kanter/code/project_mcp && bun tsc --noEmit && bun test</automated>
  </verify>
  <done>server.ts registers all 15 tools (11 existing + 4 new search tools); TypeScript compiles clean; full test suite passes with no regressions</done>
</task>

</tasks>

<verification>
1. `bun tsc --noEmit` passes — no TypeScript errors
2. `bun test` passes — full suite including all Phase 5 tests
3. `grep -c "registerSemanticSearchTool\|registerFulltextSearchTool\|registerHybridSearchTool\|registerGetSmartContextTool" src/server.ts` returns 4 (one per tool)
4. `grep -c "toolCount++" src/server.ts` returns 15
</verification>

<success_criteria>
- All 4 new search tools (semantic_search, fulltext_search, hybrid_search, get_smart_context) are registered in server.ts
- Tool count is 15 (11 existing + 4 new)
- TypeScript compiles without errors
- Complete test suite passes with no regressions
- All Phase 5 requirements (SRCH-01 through SRCH-07) are fully implemented and testable
</success_criteria>

<output>
After completion, create `.planning/phases/05-document-search/05-04-SUMMARY.md`
</output>
