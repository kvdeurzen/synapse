---
phase: 14-quality-gates-and-pev-workflow
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/framework/config/trust.toml
  - packages/framework/src/config.ts
  - packages/framework/test/unit/config.test.ts
  - packages/framework/workflows/pev-workflow.md
autonomous: true
requirements:
  - WFLOW-01
  - WFLOW-02
  - WFLOW-03
  - WFLOW-04
  - WFLOW-07

must_haves:
  truths:
    - "trust.toml has a [pev] section with approval_threshold, max_parallel_executors, max_retries_task, max_retries_feature, max_retries_epic"
    - "TrustConfigSchema in config.ts validates the [pev] section with correct types and defaults"
    - "approval_threshold accepts 'epic', 'feature', 'task', 'none' — maps from existing decomposition='strategic' to approval_threshold='epic'"
    - "config.test.ts verifies the extended TrustConfigSchema accepts valid [pev] sections and rejects invalid ones"
    - "pev-workflow.md exists in workflows/ describing the full PEV lifecycle: goal intake, progressive decomposition, wave execution, validation, and checkpoint format"
    - "PEV loop is documented as capped at 3 iterations with escalation to user on iteration 3 failure"
    - "Wave-based parallel execution is documented: independent leaf tasks in same wave execute concurrently via Claude Code Task tool with isolation 'worktree'"
    - "Wave N+1 starts only after all tasks in wave N are validated complete"
    - "Progressive decomposition is documented: Epic->Features validated upfront, Features->Tasks decomposed JIT when feature starts"
  artifacts:
    - path: "packages/framework/config/trust.toml"
      provides: "Trust config with [pev] section for PEV workflow control"
      contains: "[pev]"
    - path: "packages/framework/src/config.ts"
      provides: "Extended TrustConfigSchema with pev section validation"
      contains: "approval_threshold"
    - path: "packages/framework/test/unit/config.test.ts"
      provides: "Unit tests for PEV config schema"
      contains: "pev"
    - path: "packages/framework/workflows/pev-workflow.md"
      provides: "PEV workflow document for orchestrator agent consumption"
      min_lines: 80
  key_links:
    - from: "packages/framework/config/trust.toml"
      to: "packages/framework/src/config.ts"
      via: "TrustConfigSchema validates [pev] section structure"
      pattern: "pev.*approval_threshold"
    - from: "packages/framework/workflows/pev-workflow.md"
      to: "packages/framework/config/trust.toml"
      via: "Workflow reads pev.approval_threshold to determine approval behavior"
      pattern: "approval_threshold"
    - from: "packages/framework/workflows/pev-workflow.md"
      to: "packages/framework/agents/synapse-orchestrator.md"
      via: "Orchestrator agent references and follows pev-workflow.md"
      pattern: "pev-workflow"
---

<objective>
Add PEV process control settings to trust.toml with schema validation, and create the pev-workflow.md document that defines the Plan-Execute-Validate lifecycle for the orchestrator agent.

Purpose: The PEV workflow is not a separate runtime process -- it lives as structured instructions that the orchestrator agent reads and executes via its own reasoning. This plan creates the config schema that controls approval thresholds and retry caps, and the workflow document that the orchestrator follows. Plan 04 will wire the orchestrator agent to reference this workflow.

Output: Extended trust.toml with [pev] section, validated by Zod schema in config.ts, tested in config.test.ts, and a comprehensive pev-workflow.md in workflows/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-quality-gates-and-pev-workflow/14-CONTEXT.md
@.planning/phases/14-quality-gates-and-pev-workflow/14-RESEARCH.md

@packages/framework/config/trust.toml
@packages/framework/src/config.ts
@packages/framework/test/unit/config.test.ts
@packages/framework/agents/synapse-orchestrator.md
@packages/framework/workflows/.gitkeep

<interfaces>
<!-- Current TrustConfigSchema (from config.ts) -->
```typescript
export const TrustConfigSchema = z.object({
  domains: z.record(z.string(), autonomyLevel).default({}),
  approval: z.object({
    decomposition: z.enum(["always", "strategic", "none"]).default("strategic"),
  }).default({}),
  tier_authority: z.record(z.string(), z.array(z.number().int().min(0).max(3))).default({}),
  agent_overrides: z.record(z.string(), z.object({
    domains: z.record(z.string(), autonomyLevel).optional(),
  })).default({}),
});
```

<!-- Existing trust.toml structure -->
```toml
[domains]
architecture = "co-pilot"
...

[approval]
decomposition = "strategic"

[tier_authority]
product-strategist = [0, 1]
...
```

<!-- CONTEXT.md locked decisions for PEV config -->
- Layered approval model with single threshold: "epic", "feature", "task", "none"
- Current trust.toml decomposition = "strategic" maps to the "epic" threshold
- Suggested 3-4 max parallel executors, configurable
- Retry caps: 3 task, 2 feature, 1 epic
- PEV state persisted for session resume via task tree
- JIT decomposition: Epic->Features upfront, Features->Tasks on demand
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add [pev] section to trust.toml and extend TrustConfigSchema</name>
  <files>
    packages/framework/config/trust.toml
    packages/framework/src/config.ts
    packages/framework/test/unit/config.test.ts
  </files>
  <behavior>
    Schema validation:
    - trust.toml [pev] section is optional (default values applied if missing)
    - pev.approval_threshold accepts "epic" | "feature" | "task" | "none", default "epic"
    - pev.max_parallel_executors is a positive integer, default 3
    - pev.max_retries_task is a non-negative integer, default 3
    - pev.max_retries_feature is a non-negative integer, default 2
    - pev.max_retries_epic is a non-negative integer, default 1
    - Invalid values (e.g., approval_threshold = "invalid") rejected by Zod
    - Missing [pev] section is valid (defaults applied)

    Config tests:
    - Valid trust.toml with [pev] section parses correctly
    - trust.toml without [pev] section still parses (backward compatible)
    - Invalid pev.approval_threshold rejected
    - pev.max_parallel_executors must be positive
    - Default values applied when [pev] is empty object
  </behavior>
  <action>
    **config.ts extension:**
    Add a `pev` field to TrustConfigSchema:
    ```typescript
    pev: z.object({
      approval_threshold: z.enum(["epic", "feature", "task", "none"]).default("epic"),
      max_parallel_executors: z.number().int().min(1).default(3),
      max_retries_task: z.number().int().min(0).default(3),
      max_retries_feature: z.number().int().min(0).default(2),
      max_retries_epic: z.number().int().min(0).default(1),
    }).default({}),
    ```
    Place it after the `agent_overrides` field in TrustConfigSchema. The `.default({})` ensures backward compatibility when the section is missing.

    **trust.toml extension:**
    Add after the existing `[agent_overrides]` comment:
    ```toml
    # PEV workflow control -- approval thresholds and retry caps
    [pev]
    # Approval threshold: "epic" | "feature" | "task" | "none"
    # "epic" = user approves feature breakdown, execution below is autonomous
    # "feature" = user approves task decomposition per feature
    # "task" = user approves each task plan
    # "none" = fully autonomous end-to-end
    approval_threshold = "epic"
    # Maximum parallel executor subagents per wave
    max_parallel_executors = 3
    # Maximum retry attempts per layer before escalation
    max_retries_task = 3
    max_retries_feature = 2
    max_retries_epic = 1
    ```

    **config.test.ts extension:**
    Add test cases to the existing TrustConfig test describe block:
    - test: 'trust.toml with [pev] section parses correctly' -- construct a trust.toml string with all pev fields, parse with loadTrustConfig, assert pev values match
    - test: 'trust.toml without [pev] section applies defaults' -- parse minimal trust.toml, assert pev.approval_threshold === 'epic', pev.max_parallel_executors === 3, etc.
    - test: 'invalid pev.approval_threshold rejected' -- parse trust.toml with approval_threshold = "invalid", expect ConfigError
    - test: 'anti-drift: actual trust.toml validates against schema' -- this test already exists, it will now also validate the [pev] section (no changes needed if the existing test loads the real file)
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && bun test --cwd packages/framework test/unit/config.test.ts</automated>
  </verify>
  <done>trust.toml has [pev] section, TrustConfigSchema validates it, all config tests pass including backward compatibility</done>
</task>

<task type="auto">
  <name>Task 2: Create pev-workflow.md in workflows/</name>
  <files>
    packages/framework/workflows/pev-workflow.md
  </files>
  <action>
    Create a comprehensive PEV workflow document that the orchestrator agent reads and follows. This is the workflow's "source of truth" -- the orchestrator agent (updated in Plan 04) will reference this file.

    **Document structure:**

    ```markdown
    # Plan-Execute-Validate (PEV) Workflow

    This workflow document is read by the synapse-orchestrator agent. It defines the complete lifecycle for executing a user goal through progressive task decomposition, wave-based parallel execution, and validation.

    ## Configuration

    PEV behavior is controlled by `config/trust.toml` under the `[pev]` section:
    - `approval_threshold`: "epic" | "feature" | "task" | "none"
    - `max_parallel_executors`: Maximum concurrent executor subagents per wave (default: 3)
    - `max_retries_task`: Task-level retry cap (default: 3)
    - `max_retries_feature`: Feature-level retry cap (default: 2)
    - `max_retries_epic`: Epic-level retry cap (default: 1)

    ## Trigger

    The PEV workflow is triggered when:
    - A user describes a goal (natural language or /synapse:new-goal command)
    - The orchestrator normalizes the input into a goal statement

    ## Phase 1: Goal Intake and Epic Creation

    1. Normalize trigger into a goal statement
    2. Call `check_precedent` for related existing work
    3. Create root epic via `create_task` (depth=0) with goal as title
    4. Read `trust.toml` pev.approval_threshold to determine approval behavior:
       - "epic": Present feature list for user approval; autonomous below
       - "feature": Present task list per feature for user approval
       - "task": Present each task plan for user approval
       - "none": Fully autonomous end-to-end
    5. Every approval point is conversational: options are approve, provide feedback/refine, or discuss further

    ## Phase 2: Progressive Decomposition

    ### Step 2a: Epic -> Features
    1. Spawn Decomposer subagent via Task tool to decompose epic into features (depth=1 tasks)
    2. Decomposer creates feature tasks with descriptions, acceptance criteria, and mandatory validation tasks:
       - Each feature gets an "integration test" child task
       - The epic gets an "epic integration" task
    3. Spawn Plan Reviewer subagent to verify feature decomposition
       - Max 3 review cycles (Decomposer <-> Plan Reviewer loop per WFLOW-06)
       - If plan rejected after 3 cycles: escalate to user
    4. If approval_threshold requires it, present feature list to user for approval

    ### Step 2b: Feature -> Tasks (JIT / On-Demand)
    Features are decomposed into tasks (depth=2/3) only when the feature is next to execute -- NOT upfront.
    Earlier features' outputs inform later decomposition.

    For each feature when it becomes active:
    1. Spawn Decomposer subagent to decompose feature into component/task-level items
    2. Decomposer creates leaf tasks with:
       - Description and acceptance criteria
       - Unit test expectations
       - Dependencies on other tasks within the feature
    3. Spawn Plan Reviewer to verify task decomposition (max 3 review cycles)
    4. If approval_threshold = "feature" or "task": present to user
    5. Identify execution waves: group independent tasks into parallel waves

    ## Phase 3: Wave Execution

    For each feature:

    ### Wave Identification
    1. Analyze task dependencies within the feature
    2. Group independent tasks into waves (tasks with no unmet dependencies = Wave 1, etc.)
    3. Cap parallel executors at `pev.max_parallel_executors` per wave

    ### Wave N Processing
    1. For each task in the wave, spawn executor subagent via Task tool:
       - Use `isolation: "worktree"` for git isolation
       - Pass task spec, acceptance criteria, and relevant context in the prompt
       - Multiple Task tool calls in a single turn for parallel execution
    2. Await all executor results
    3. For each completed task: spawn Validator subagent to check output against spec
    4. If any validation fails: HALT wave, trigger Failure Escalation (Phase 4)
    5. If all validations pass: proceed to next wave
    6. After all feature waves complete: spawn Integration Checker for feature-level validation
    7. If integration passes: merge feature branch to main (sequential merge of task branches into feature branch first)
    8. Emit wave checkpoint status block:

    ```
    ## Wave {N} Complete -- Feature: {feature_title} ({done}/{total} tasks)

    | Task | Status | Agent | Notes |
    |------|--------|-------|-------|
    | {task_title} | done | executor | {brief summary} |
    ...

    Integration check: PASSED/FAILED
    Next: {next_feature_or_epic_integration} ({task_count} tasks ready)
    ```

    ### Wave N+1
    Wave N+1 starts ONLY after ALL tasks in wave N are validated complete.

    ## Phase 4: Failure Escalation

    ### Task-Level Failure
    1. Spawn Debugger subagent with full context: task spec, what was attempted, error messages, relevant file paths
    2. Debugger produces a diagnostic document (stored via store_document)
    3. Auto-revert failed task's git changes (git revert or worktree discard)
    4. Retry executor with debugger report in context
    5. Track retry count (max: pev.max_retries_task, default 3)
    6. If retries exhausted: escalate to feature level

    ### Feature-Level Failure
    1. Aggregate task failure context
    2. Present findings to user with options: approve revised plan, provide guidance, or abort feature
    3. Max pev.max_retries_feature retries at this level (default 2)
    4. If retries exhausted: escalate to epic level

    ### Epic-Level Failure
    1. Present comprehensive failure report to user
    2. Options: revise epic approach, descope features, or abort
    3. Max pev.max_retries_epic retries (default 1)
    4. If retries exhausted: halt PEV workflow, present all findings to user

    ### PEV Loop Cap (WFLOW-02)
    The overall PEV loop is capped at 3 iterations. On iteration 3 failure, escalate to user rather than silently looping. Present:
    - What was attempted (all 3 iterations)
    - What failed and why
    - Proposed alternatives or manual intervention needed

    ## Phase 5: Epic Completion

    After all features pass feature-level integration:
    1. Spawn Integration Checker for epic-level validation (cross-feature integration)
    2. If epic integration passes: mark epic task as complete
    3. Present final status summary to user
    4. Store completion decision via store_decision for project history

    ## Execution Isolation

    - Each executor runs in an isolated git worktree (`Task` tool with `isolation: "worktree"`)
    - Merge strategy: per-feature
      1. All tasks within a feature complete + integration check passes
      2. Sequential merge of task branches into feature branch
      3. Merge feature branch to main
    - Auto-revert failed tasks (git), keep passing tasks within the feature

    ## Session Resume

    PEV state is persisted in the Synapse task tree:
    - Task status tracks wave progress (pending/in_progress/done/failed)
    - Resume is user-triggered (not automatic on session start)
    - On resume: read task tree, identify current wave position, continue from last incomplete wave

    ## Subagent Constraints

    - Subagents spawned via Task tool CANNOT spawn other subagents -- orchestrator manages all spawning
    - Fresh executor per retry (not resumed original) -- pass debugger's document in context for continuity
    - Decomposer and Plan Reviewer are separate agents (Decomposer cannot review its own work)
    ```

    **IMPORTANT:** Remove the .gitkeep file from workflows/ after creating pev-workflow.md.
  </action>
  <verify>
    <automated>cd /home/kanter/code/synapse && test -f packages/framework/workflows/pev-workflow.md && wc -l packages/framework/workflows/pev-workflow.md | awk '{if ($1 >= 80) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>pev-workflow.md exists in workflows/ with at least 80 lines covering all PEV lifecycle phases, the .gitkeep is removed, and the document is comprehensive enough for the orchestrator to follow</done>
</task>

</tasks>

<verification>
1. `bun test --cwd packages/framework test/unit/config.test.ts` -- all tests pass including new pev tests
2. trust.toml has [pev] section with all 5 fields
3. `bun -e "import { loadTrustConfig } from './packages/framework/src/config.ts'; const c = loadTrustConfig('packages/framework/config/trust.toml'); console.log(JSON.stringify(c.pev))"` -- outputs valid pev config
4. pev-workflow.md exists with complete lifecycle documentation
5. `bun run test:framework` -- all existing framework tests still pass
</verification>

<success_criteria>
- trust.toml [pev] section exists with approval_threshold, max_parallel_executors, and retry caps
- TrustConfigSchema validates [pev] section with correct types and defaults
- Backward compatibility: trust.toml without [pev] still parses (defaults applied)
- Config tests prove validation and backward compatibility
- pev-workflow.md is comprehensive: covers trigger, decomposition, wave execution, validation, failure escalation, session resume
- PEV loop cap (3 iterations) is explicitly documented
- Wave N+1 gating on wave N completion is explicit
- JIT decomposition pattern is documented
</success_criteria>

<output>
After completion, create `.planning/phases/14-quality-gates-and-pev-workflow/14-03-SUMMARY.md`
</output>
